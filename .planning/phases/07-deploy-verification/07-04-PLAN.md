---
phase: 07-deploy-verification
plan: 04
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-03"
files_modified:
  - .planning/phases/07-deploy-verification/07-VERIFICATION.md
  - .planning/phases/07-deploy-verification/lighthouse/about-mobile.png
  - .planning/phases/07-deploy-verification/lighthouse/projects-mobile.png
  - .planning/phases/07-deploy-verification/lighthouse/stack-mobile.png
  - .planning/phases/07-deploy-verification/lighthouse/experience-mobile.png
  - .planning/phases/07-deploy-verification/lighthouse/writing-mobile.png
  - .planning/phases/07-deploy-verification/lighthouse/contact-mobile.png
  - .planning/phases/07-deploy-verification/lighthouse/shipped-mobile.png
autonomous: false
requirements:
  - DEPLOY-02
tags:
  - lighthouse
  - pagespeed-insights
  - performance
  - manual-attestation
  - evidence-capture
must_haves:
  truths:
    - "PageSpeed Insights mobile profile run 3 times per route on https://www.tatibekov.com (7 routes × 3 runs = 21 runs)"
    - "Median run per route recorded in a 7-row × 6-column scores table in 07-VERIFICATION.md"
    - "All 42 cells (7 routes × 6 metrics: Perf, A11y, SEO, LCP, CLS, INP) pass DEPLOY-02 thresholds"
    - "Per-route median PSI dashboard screenshot saved as PNG in lighthouse/ directory"
    - "Any threshold-miss closed via fix-in-place + re-audit loop until 42/42 cells green (D-17)"
  artifacts:
    - path: ".planning/phases/07-deploy-verification/07-VERIFICATION.md"
      provides: "DEPLOY-02 section with 7×6 scores table + threshold inline in headers + per-cell PASS/FAIL marker"
      min_lines: 30
      contains: "DEPLOY-02"
    - path: ".planning/phases/07-deploy-verification/lighthouse/about-mobile.png"
      provides: "PSI dashboard screenshot for / (about) route — median run"
    - path: ".planning/phases/07-deploy-verification/lighthouse/projects-mobile.png"
      provides: "PSI dashboard screenshot for /projects route — median run"
    - path: ".planning/phases/07-deploy-verification/lighthouse/stack-mobile.png"
      provides: "PSI dashboard screenshot for /stack route — median run"
    - path: ".planning/phases/07-deploy-verification/lighthouse/experience-mobile.png"
      provides: "PSI dashboard screenshot for /experience route — median run"
    - path: ".planning/phases/07-deploy-verification/lighthouse/writing-mobile.png"
      provides: "PSI dashboard screenshot for /writing route — median run"
    - path: ".planning/phases/07-deploy-verification/lighthouse/contact-mobile.png"
      provides: "PSI dashboard screenshot for /contact route — median run"
    - path: ".planning/phases/07-deploy-verification/lighthouse/shipped-mobile.png"
      provides: "PSI dashboard screenshot for /shipped route — median run"
  key_links:
    - from: "07-VERIFICATION.md scores table"
      to: "lighthouse/<route>-mobile.png evidence files"
      via: "table cell footnote / inline reference"
      pattern: "lighthouse/"
---

<objective>
Run PageSpeed Insights (D-14 canonical Lighthouse runner) against all 7 production routes on `https://www.tatibekov.com`, mobile profile, 3 runs per route (Google's variability-control methodology per RESEARCH.md Pattern 4), record the median per metric in a 7-row × 6-column scores table in `07-VERIFICATION.md`, save the median run's PSI dashboard as PNG evidence per route (committed to `.planning/phases/07-deploy-verification/lighthouse/`), and close DEPLOY-02 with all 42 cells (7 routes × 6 metrics) passing thresholds. If any cell misses threshold, apply fix-in-place + re-audit per D-17 — no DEFERRED-V1.1 escape hatch.

Purpose: DEPLOY-02 — Lighthouse on production URL (mobile profile) shows LCP < 2.5s, CLS < 0.1, INP < 200ms (or lab TBT proxy per Pitfall 8 if INP field data unavailable for new domain), Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95.

Output: 7 PSI screenshot PNGs in lighthouse/ + 07-VERIFICATION.md DEPLOY-02 section with the populated 7×6 scores table + per-cell PASS/FAIL markers + any remediation commit references inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-deploy-verification/07-CONTEXT.md
@.planning/phases/07-deploy-verification/07-RESEARCH.md
@.planning/phases/07-deploy-verification/07-VALIDATION.md
@.planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md

<interfaces>
<!-- The 7 routes to audit (from lib/routes.ts, in render order). -->

Routes:
- `/` (about — slug=null, OG title "About — Sr. Software Engineer; bio, highlights, contact")
- `/projects` (label `projects/`, OG "Projects — engineering work, sorted by year")
- `/stack` (label `stack.json`, OG "Tech stack — languages, frameworks, cloud, AI")
- `/experience` (label `experience.log`, OG "Experience — roles, companies, periods, highlights")
- `/writing` (label `writing/`, OG "Writing — technical posts and notes")
- `/contact` (label `contact.sh`, OG "Contact — email, GitHub, LinkedIn, social profiles")
- `/shipped` (label `shipped.app`, OG "Shipped apps — App Store and Google Play releases")

DEPLOY-02 thresholds (from REQUIREMENTS.md + ROADMAP Phase 7 Success Criterion 2):
- Performance >= 90
- Accessibility >= 95
- SEO >= 95
- LCP < 2.5s
- CLS < 0.1
- INP < 200ms (or lab TBT proxy if INP field data unavailable for new domain — Pitfall 8)

7×6 scores-table template (modeled on Phase 5 56-cell axe matrix in 05-VERIFICATION.md — same evidence shape):

| Route | Perf (≥90) | A11y (≥95) | SEO (≥95) | LCP (<2.5s) | CLS (<0.1) | INP/TBT (<200ms) |
|-------|-----------|-----------|----------|-------------|-----------|------------------|
| / | | | | | | |
| /projects | | | | | | |
| /stack | | | | | | |
| /experience | | | | | | |
| /writing | | | | | | |
| /contact | | | | | | |
| /shipped | | | | | | |

Each cell: numeric value + `✓` / `✗` marker.
</interfaces>

<canonical_refs>
- RESEARCH.md §Pattern 4 (lines 432-457) — PSI 3-run median methodology + mobile profile semantics
- RESEARCH.md §Pitfall 1 (lines 570-575) — variance near threshold; consider 5 runs for routes within ±3 of threshold
- RESEARCH.md §Pitfall 8 (lines 622-628) — INP field-data unavailable for new domain; lab TBT proxy is the acceptable substitute for v1
- CONTEXT.md D-14, D-15, D-16, D-17 — locked decisions for this plan
- VALIDATION.md DEPLOY-02 rows — evidence shape + threshold-miss policy
- .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md — Phase 5 axe-matrix evidence shape (56 cells in a single table)
</canonical_refs>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run PSI 3× per route × 7 routes (21 runs total); record median + screenshot the median run's dashboard</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Pattern 4 (3-run median methodology) + §Pitfall 1 (variance) + §Pitfall 8 (INP field-data delay)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-14 (PSI web tool canonical), D-15 (all 7 routes), D-16 (evidence shape), D-17 (fix-in-place policy)
  </read_first>
  <what-built>
    Nothing automatable — PSI is an external SaaS that runs Lighthouse against a remote URL. No CLI surface in scope (D-14 picks PSI web tool over `npx lighthouse` for zero-install + same scoring engine).
  </what-built>
  <how-to-verify>
    For each of the 7 routes (`/`, `/projects`, `/stack`, `/experience`, `/writing`, `/contact`, `/shipped`):

    1. Open `https://pagespeed.web.dev/?url=https://www.tatibekov.com<path>` (replace `<path>` with the route, e.g. `https://pagespeed.web.dev/?url=https://www.tatibekov.com/projects`). Open in a fresh browser tab; close other PSI tabs to avoid network contention.

    2. Click "Analyze". Wait for the report. PSI shows BOTH Mobile and Desktop tabs.

    3. Switch to the **Mobile** tab (DEPLOY-02 mobile profile per D-14).

    4. Record the 6 metrics in a scratch buffer:
       - Performance score (0-100) — threshold ≥90
       - Accessibility score — threshold ≥95
       - SEO score — threshold ≥95
       - LCP (Largest Contentful Paint, lab) — threshold <2.5s
       - CLS (Cumulative Layout Shift, lab) — threshold <0.1
       - INP (Interaction to Next Paint) — threshold <200ms; if field data unavailable for new domain (expected per Pitfall 8), use lab **TBT** (Total Blocking Time) as proxy with the same <200ms threshold and document the substitution in the table footnote.

    5. Repeat steps 1-4 two more times for the same route (3 runs total). Wait ~30 seconds between runs to avoid PSI rate-limiting.

    6. Compute the median per metric (the middle of the 3 values; for Performance score where ties are common, the second-highest is fine). Identify which run was the median run for Performance.

    7. Capture a full-page screenshot of the **median run's PSI dashboard** (browser screenshot, full-page). Save to `.planning/phases/07-deploy-verification/lighthouse/<route-slug>-mobile.png` where `<route-slug>` is:
       - `/` → `about-mobile.png`
       - `/projects` → `projects-mobile.png`
       - `/stack` → `stack-mobile.png`
       - `/experience` → `experience-mobile.png`
       - `/writing` → `writing-mobile.png`
       - `/contact` → `contact-mobile.png`
       - `/shipped` → `shipped-mobile.png`

    8. If any route shows Performance score within ±3 of 90 across the 3 runs, do 2 additional runs (5 total per Pitfall 1 high-variance protocol) and recompute the median from all 5.

    9. Move to the next route. Repeat for all 7 routes.

    Estimated wall-clock: 21 runs × ~60s/run + 6 transition pauses × ~30s = ~25 minutes if no high-variance routes; up to ~40 min if 2-3 routes need 5-run protocol.
  </how-to-verify>
  <acceptance_criteria>
    - 7 PNG files exist in `.planning/phases/07-deploy-verification/lighthouse/`: `about-mobile.png`, `projects-mobile.png`, `stack-mobile.png`, `experience-mobile.png`, `writing-mobile.png`, `contact-mobile.png`, `shipped-mobile.png`.
    - Each screenshot shows the PSI mobile-tab dashboard with all 6 metrics visible.
    - Scratch buffer / temporary notes capture the 3-run (or 5-run) raw values per route + the median per metric.
    - High-variance routes (Perf within ±3 of 90) ran the 5-run protocol.
  </acceptance_criteria>
  
  <action>
    See <how-to-verify> below — this is a manual human action with no CLI/API surface in scope. The executor agent halts here; the user performs the steps and signals resume.
  </action>
  <verify>
    <automated>MISSING — this checkpoint produces evidence (screenshot or stateful UI change) that the next automated task asserts. See acceptance_criteria for human verification.</automated>
  </verify>
  <done>
- 7 PNG files exist in `.planning/phases/07-deploy-verification/lighthouse/`: `about-mobile.png`, `projects-mobile.png`, `stack-mobile.png`, `experience-mobile.png`, `writing-mobile.png`, `contact-mobile.png`, `shipped-mobile.png`.
    - Each screenshot shows the PSI mobile-tab dashboard with all 6 metrics visible.
    - Scratch buffer / temporary notes capture the 3-run (or 5-run) raw values per route + the median per metric.
    - High-variance routes (Perf within ±3 of 90) ran the 5-run protocol.
  </done>
  <resume-signal>Type "PSI captured" with confirmation that all 7 PNGs exist and median values are tabulated, OR describe halt condition (e.g., site unreachable, PSI rate-limited).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create 07-VERIFICATION.md and populate DEPLOY-02 scores table from Task 1 medians</name>
  <read_first>
    - .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md (Phase 5 axe-matrix evidence shape — model for the Phase 7 scores table)
    - .planning/phases/07-deploy-verification/07-VALIDATION.md DEPLOY-02 rows
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-16 (7×6 scores table + screenshots committed)
    - Task 1 scratch-buffer notes (median per metric per route)
  </read_first>
  <action>
    Create `.planning/phases/07-deploy-verification/07-VERIFICATION.md` if it does not yet exist (Plan 05/07/08/09 will append additional sections — this is the first creation point).

    File structure for THIS plan's section (later plans append below):

    Frontmatter (if creating fresh):
    ```yaml
    ---
    phase: 07-deploy-verification
    type: verification
    status: in-progress
    created: <today's date YYYY-MM-DD>
    ---
    ```

    Top-level heading: `# Phase 7 — Deploy + Verification`

    First section: `## DEPLOY-02 — Lighthouse (PageSpeed Insights, mobile profile)`

    Inside the DEPLOY-02 section:
    1. One-line preamble: "Methodology: PSI web tool, mobile profile, 3-run median per route (5-run protocol for high-variance routes within ±3 of Performance 90 threshold per Pitfall 1). Run date: YYYY-MM-DD. Production URL: https://www.tatibekov.com."

    2. The 7×6 scores table (template in `<interfaces>` above). Fill each cell with the median value + `✓` (passes threshold) or `✗` (misses threshold). Example row format:
       `| / | 94 ✓ | 100 ✓ | 100 ✓ | 1.8s ✓ | 0.02 ✓ | 120ms ✓ |`
       (Cell layout: `<value> <marker>` — single space separator. For misses use `<value> ✗` then add a footnote pointing at the remediation commit.)

    3. Below the table, an "Evidence" subsection listing all 7 PNG paths:
       - `lighthouse/about-mobile.png` — median run for `/`
       - `lighthouse/projects-mobile.png` — median run for `/projects`
       - `lighthouse/stack-mobile.png` — median run for `/stack`
       - `lighthouse/experience-mobile.png` — median run for `/experience`
       - `lighthouse/writing-mobile.png` — median run for `/writing`
       - `lighthouse/contact-mobile.png` — median run for `/contact`
       - `lighthouse/shipped-mobile.png` — median run for `/shipped`

    4. INP/TBT substitution note (only if INP field data was unavailable per Pitfall 8): "INP column: field data unavailable for new domain (zero CrUX history). Lab TBT (Total Blocking Time) substituted as proxy with same <200ms threshold per RESEARCH.md Pitfall 8."

    5. Outcome marker at end of section:
       - If all 42 cells pass: `**DEPLOY-02 verdict: PASS** (42/42 cells green; first run.)`
       - If remediation required: `**DEPLOY-02 verdict: PASS** (42/42 cells green after N remediation cycles — see Task 3 below for fix-in-place commits.)`

    Conventions:
    - Match Phase 5 axe-matrix evidence shape (05-VERIFICATION.md) — table + evidence-pointer list + verdict line.
    - Use lowercase metric units (`s` for seconds, `ms` for milliseconds — match PSI dashboard).
    - Do NOT abbreviate route paths in the table — full `/projects` not `projects`.

    If all 42 cells pass on first capture, Task 3 is a no-op (skip directly to Task 4 commit). If ANY cell misses, proceed to Task 3.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q '^## DEPLOY-02' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -cE '^\| /[a-z]*' .planning/phases/07-deploy-verification/07-VERIFICATION.md | (read n; test "$n" -ge 7) && grep -q 'lighthouse/about-mobile.png' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'DEPLOY-02 verdict' .planning/phases/07-deploy-verification/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `07-VERIFICATION.md` exists with frontmatter + top-level heading + `## DEPLOY-02` section.
    - Table contains exactly 7 data rows (one per route) and 6 metric columns + header.
    - Each cell contains a numeric value + `✓` or `✗` marker.
    - Evidence subsection lists all 7 PNG paths.
    - Verdict line states "DEPLOY-02 verdict: PASS" (after Task 3 remediation if needed) or "FAIL with N misses" (which triggers Task 3 then re-runs Task 2).
    - INP/TBT substitution note present iff INP field data unavailable.
  </acceptance_criteria>
  <done>
    07-VERIFICATION.md scaffolded with DEPLOY-02 section populated. If verdict is PASS, skip Task 3. If FAIL, Task 3 closes the gap.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix-in-place remediation loop (only if Task 2 surfaces ANY threshold miss)</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-17 (fix-in-place + re-audit; no DEFERRED-V1.1 escape hatch)
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Pitfall 1 (high-variance routes) + §"Anti-Patterns to Avoid" (PSI single-run cherry-pick)
    - The specific PSI dashboard for the failing route — diagnostic recommendations are in PSI's "Opportunities" and "Diagnostics" sections
  </read_first>
  <action>
    SKIP THIS TASK IF Task 2 verdict is PASS (42/42 cells green on first capture).

    IF ANY cell misses threshold:

    1. Identify the smallest fix per PSI's "Opportunities" / "Diagnostics" sections. Common fixes (RESEARCH.md guidance):
       - LCP miss: preload critical font + image, defer non-critical scripts, ensure JetBrains Mono ships with `display: swap` (already does per Phase 5).
       - CLS miss: explicit `width`/`height` on images, reserve space for late-loading elements, avoid layout-shifting CSS animations.
       - INP/TBT miss: dynamic-import heavy client islands, reduce JavaScript on the page, audit `<CommandPalette>` and `<TopBar>` for unnecessary re-renders.
       - Performance score miss: a combination of the above; PSI dashboard usually highlights the biggest contributor.
       - Accessibility score miss: should not happen — Phase 5 56-cell axe matrix already validates WCAG 2.1 AA at source level. If it does, treat as a regression and prioritize.
       - SEO score miss: check robots.txt allow rule, sitemap accessibility, metadata title/description presence.

    2. Apply the smallest fix in a focused commit. Commit message format:
       `perf(07): <fix description> for <route> LCP miss (DEPLOY-02 remediation)`
       or `fix(07): <a11y / SEO issue> on <route> (DEPLOY-02 remediation)`
       Reference the original commit SHA and the failing metric in the commit body if helpful.

    3. Push the commit. Wait for Vercel redeploy (~60-90s).

    4. Re-run PSI 3× on the affected route(s). Update the row in 07-VERIFICATION.md scores table. Replace the old screenshot with the new median run's screenshot (overwrite the PNG with same filename). Add a footnote under the table: "[^remediation-N]: Route `/foo` Performance bumped from 87 to 92 via <fix description> (commit <sha>)."

    5. Repeat until all 42 cells pass. No DEFERRED-V1.1 escape — Phase 7 doesn't close until 42/42 green.

    If the same metric on the same route fails 3 remediation cycles, halt and surface to the user as a checkpoint:decision (something architectural is wrong; needs human judgment on whether to accept a documented deviation).
  </action>
  <verify>
    <automated>! grep -q '✗' .planning/phases/07-deploy-verification/07-VERIFICATION.md || (echo "DEPLOY-02 still has threshold misses — re-run Task 3" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - 07-VERIFICATION.md DEPLOY-02 scores table contains zero `✗` markers (all 42 cells `✓`).
    - Each remediation cycle has a corresponding commit referenced in a footnote under the table.
    - Updated screenshots overwrite stale ones (same filenames; git tracks the binary diff).
    - DEPLOY-02 verdict line reads "PASS (42/42 cells green after N remediation cycles)".
  </acceptance_criteria>
  <done>
    All DEPLOY-02 cells pass thresholds. Verification table + evidence + remediation footnotes consistent.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit DEPLOY-02 evidence + verification section</name>
  <read_first>
    - git status (verify only Plan 04 outputs are staged)
  </read_first>
  <action>
    Stage and commit:
    `git add .planning/phases/07-deploy-verification/07-VERIFICATION.md .planning/phases/07-deploy-verification/lighthouse/`
    `git commit -m "docs(07): record DEPLOY-02 Lighthouse mobile scores + 7 PSI screenshots (PASS)"`

    Push:
    `git push origin main`

    Do NOT trigger a redeploy commit — Plan 04 produces evidence only; no source-code changes. (If Task 3 introduced remediation commits, those commits + their redeploy commits already pushed during Task 3; this commit lands only the evidence + verification.)
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q 'DEPLOY-02 Lighthouse' && git diff --stat HEAD~1 HEAD | grep -E 'lighthouse|07-VERIFICATION\.md' | wc -l | (read n; test "$n" -ge 8)</automated>
  </verify>
  <acceptance_criteria>
    - Single commit touches 07-VERIFICATION.md + 7 PNG files (8 files total — or 1 + n PNGs if any remediation overwrote some).
    - Commit subject references DEPLOY-02 and Lighthouse.
    - Push completes; working tree clean.
  </acceptance_criteria>
  <done>
    DEPLOY-02 evidence committed and pushed. Plan 04 complete; ready for parallel Plans 05 + 06 (same Wave 3) and downstream Wave 4 plans (07, 08, 09).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PSI external service | Google-operated; runs Lighthouse against the production URL from Google-controlled infra. No auth required; results are public if URL is shared. |
| Screenshot files in repo | PNG binary content; non-executable; no XSS / script-injection vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Information Disclosure | PSI report URL (auto-generated by PSI) | accept | PSI reports are public-by-default if the URL is shared. We're publishing the production URL anyway. No new exposure. |
| T-07-16 | Tampering | Manual transcription of PSI scores into 07-VERIFICATION.md | mitigate | Screenshots committed alongside the numeric table — future readers can verify the recorded values against the PNG evidence. Median methodology documented to prevent cherry-pick anti-pattern. |
| T-07-17 | Repudiation | Single-run claim ("Perf = 87") cherry-picked | mitigate | D-14/D-16/D-17 mandate 3-run median (or 5-run for high-variance). Pitfall 1 documents the rule explicitly. Reviewer can re-run PSI any time and confirm. |

ASVS L1: PASS. No new auth, no PII, no secrets, no new endpoints. Evidence files are non-executable.
</threat_model>

<verification>
- Task 1: 21 PSI runs (7 routes × 3 runs, plus any 5-run high-variance cases); 7 PNG screenshots saved in lighthouse/.
- Task 2: 07-VERIFICATION.md created with DEPLOY-02 section, 7×6 table populated with medians + threshold markers, evidence list with 7 PNG paths.
- Task 3 (conditional): zero `✗` cells remaining after fix-in-place + re-audit cycles.
- Task 4: single commit lands 07-VERIFICATION.md + 7 PNGs; pushed to origin/main.
</verification>

<success_criteria>
This plan succeeds when:
1. All 42 cells (7 routes × 6 metrics) in the DEPLOY-02 scores table show `✓` (passing threshold).
2. 7 PSI dashboard PNGs committed to `.planning/phases/07-deploy-verification/lighthouse/`.
3. 07-VERIFICATION.md DEPLOY-02 verdict is "PASS (42/42 cells green)".
4. Any remediation cycles have inline footnotes referencing fix commits.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deploy-verification/07-04-SUMMARY.md` capturing:
- Total PSI runs executed (21 + extras for high-variance routes)
- Per-route median Performance + LCP + CLS + INP/TBT
- Any remediation commits (with SHAs)
- Commit SHA for the evidence + verification commit
- Note on INP field-data substitution (if applicable per Pitfall 8)
</output>
