---
id: 05-04
phase: 05-seo-accessibility-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-03
files_modified:
  - app/components/views/about-socials.tsx
  - app/components/views/about-view.tsx
  - app/components/views/about-view.test.tsx
  - app/globals.css
autonomous: true
requirements:
  - A11Y-04
status: pending
tags:
  - accessibility
  - phase-4-carry-forward
  - about-view
  - recruiter
must_haves:
  truths:
    - "<AboutSocials /> renders 3 rows (EMAIL / GITHUB / LINKEDIN) on /about at every viewport"
    - "EMAIL row uses native mailto: link to PROFILE.email"
    - "GITHUB and LINKEDIN rows use <ExternalLink> primitive when URL matches /^https?:\\/\\//; render as muted plain text otherwise (D-34 TODO guard)"
    - "AboutSocials block is inserted IMMEDIATELY AFTER the bio paragraphs and BEFORE the highlights row (D-31 — recruiter scan path)"
    - "AboutSocials block is visible at every viewport (no @media gating per D-32)"
    - "about-view.test.tsx has new assertions covering AboutSocials presence + 3-row contract"
  artifacts:
    - path: "app/components/views/about-socials.tsx"
      provides: "AboutSocials RSC mini-contact card"
      exports: ["AboutSocials"]
    - path: "app/components/views/about-view.tsx"
      contains: '<AboutSocials profile={profile} />'
    - path: "app/globals.css"
      contains: '.about-socials-card'
    - path: "app/components/views/about-view.test.tsx"
      contains: 'about-socials'
  key_links:
    - from: "app/components/views/about-socials.tsx"
      to: "app/components/primitives/external-link.tsx"
      via: "import { ExternalLink } from '@/app/components/primitives/external-link'"
      pattern: "from \"@/app/components/primitives/external-link\""
    - from: "app/components/views/about-view.tsx"
      to: "app/components/views/about-socials.tsx"
      via: "import { AboutSocials } from './about-socials'"
      pattern: "AboutSocials"
    - from: "AboutSocials EMAIL row"
      to: "PROFILE.email"
      via: "mailto: href"
      pattern: 'href=\\{`mailto:'
---

<objective>
Wave 1, parallel branch C. Phase 4 → Phase 5 carry-forward (Plan 04-05 Gate 9 friction). Insert a 3-row inline mini-contact card on `/about` immediately after the bio paragraphs and before the highlights row, so non-engineer recruiters find email/GitHub/LinkedIn without first locating the hamburger menu. Always visible (no `@media` gating per D-32).

Purpose: A11Y-04 polish (recruiter discoverability) and the explicit Phase 4 deferred item. The pattern reuses `<ExternalLink>` from Phase 3 and the `.contact-row` row visual from `app/components/views/contact-view.tsx` — no new design surface, just a relocation onto /about with a TODO-guard fallback for missing URLs.

Output: 1 new RSC component (`about-socials.tsx`), 1 new CSS block (`.about-socials-card` + `.contact-muted` in `globals.css`), 1 line of about-view.tsx wiring, 4 new test cases extending the existing about-view.test.tsx. ~80 LOC net.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-seo-accessibility-polish/05-CONTEXT.md
@.planning/phases/05-seo-accessibility-polish/05-RESEARCH.md
@.planning/phases/05-seo-accessibility-polish/05-PATTERNS.md
@.planning/phases/05-seo-accessibility-polish/05-UI-SPEC.md
@CLAUDE.md
@app/components/views/contact-view.tsx
@app/components/views/about-view.tsx
@app/components/views/about-view.test.tsx
@app/components/primitives/external-link.tsx
@lib/portfolio-data.ts
@lib/types.ts
@app/globals.css

<interfaces>
<!-- Existing interfaces this plan composes against. -->

From app/components/primitives/external-link.tsx (Phase 3 RSC primitive — reused verbatim):
```typescript
interface ExternalLinkProps {
  href: string;
  className?: string;
  children: ReactNode;
  showGlyph?: boolean;
  "aria-label"?: string;
}
export function ExternalLink({...}: ExternalLinkProps): JSX.Element;
// Renders: <a href target="_blank" rel="noopener noreferrer">{children}{showGlyph && " ↗"}</a>
```

From lib/types.ts (Phase 1):
```typescript
export interface Social {
  label: string;       // "GitHub", "LinkedIn", ...
  handle: string;      // "@beckinfonet", "in/bakytbek"
  url: string;         // full https URL — Phase 6 fills with real values; Phase 5 must guard
  kind: "github" | "linkedin" | "mastodon" | "bluesky" | "x" | "email" | "other";
}
export interface Profile {
  name: string;
  email: string;       // PROFILE.email = "beckprograms@gmail.com" today
  socials: Social[];
  // ...other fields
}
```

From app/components/views/contact-view.tsx (visual analog — D-33 reuses the row pattern):
```tsx
<div className="contact-row">
  <span className="contact-label">EMAIL</span>
  <a href={`mailto:${profile.email}`} aria-label={`Send email to ${profile.email}`}>
    {profile.email}
  </a>
  <CopyButton ... />
</div>
{profile.socials.map((s) => (
  <div key={s.kind} className="contact-row">
    <span className="contact-label">{s.label.toUpperCase()}</span>
    <ExternalLink href={s.url} aria-label={`Open ${s.label} (opens in new tab)`}>
      {s.handle}
    </ExternalLink>
    <span />
  </div>
))}
```

From app/components/views/about-view.tsx (insertion site — between bio paragraphs and `.about-cards`):
```tsx
{profile.bio.long.map((para, i) => (
  <p key={i} className="about-para">{para}</p>
))}

{/* INSERT <AboutSocials profile={profile} /> HERE */}

<div className="about-cards">
```

From lib/portfolio-data.ts PROFILE.socials (read-only — current values):
```typescript
socials: [
  { label: "GitHub",   handle: "@beckinfonet",  url: "https://github.com/beckinfonet",   kind: "github" },
  { label: "LinkedIn", handle: "in/bakytbek",    url: "https://linkedin.com/in/bakytbek", kind: "linkedin" }
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/components/views/about-socials.tsx RSC + extend about-view.test.tsx</name>
  <read_first>
    - app/components/views/contact-view.tsx (exact analog for the row pattern — D-33; mailto + ExternalLink rows)
    - .planning/phases/05-seo-accessibility-polish/05-RESEARCH.md §Architecture Patterns Pattern 10 (full AboutSocials example with TODO guard)
    - .planning/phases/05-seo-accessibility-polish/05-CONTEXT.md D-31..D-34 (insertion site, always-visible, mini-contact-card visual, TODO guard)
    - .planning/phases/05-seo-accessibility-polish/05-UI-SPEC.md §"AboutSocials block copy" (3 rows: EMAIL, GITHUB, LINKEDIN with handle text + href), §Spacing (`24px` top margin, `16px` border-padding-top, `80px 1fr; gap: 12px;` row grid)
    - .planning/phases/05-seo-accessibility-polish/05-PATTERNS.md §`app/components/views/about-socials.tsx (RSC, static-render)` §Apply
    - app/components/primitives/external-link.tsx (Phase 3 RSC; reused — no edits)
    - lib/types.ts (Profile + Social shapes)
    - lib/portfolio-data.ts (PROFILE.email + PROFILE.socials current values)
    - app/components/views/about-view.test.tsx (existing tests — extend with 4 new cases)
  </read_first>
  <behavior>
    - Test 1: Renders a `<div class="about-socials-card">` wrapper with `role="group"` and `aria-label="Quick contact"` (D-33)
    - Test 2: Renders an EMAIL row with a `mailto:` link pointing at PROFILE.email and aria-label `"Send email to <email>"` (matches contact-view convention; A11Y-04 plain-noun)
    - Test 3: Renders a GITHUB row using `<ExternalLink>` (target=_blank + rel=noopener + ↗ glyph) when PROFILE.socials github URL matches `/^https?:\/\//`
    - Test 4: Renders a LINKEDIN row using `<ExternalLink>` when LinkedIn URL is real; falls back to a `<span class="contact-muted">` (no `<a>`) when URL begins with `TODO:` (D-34 guard)
    - Test 5: Inserted in about-view IMMEDIATELY after the last bio paragraph and BEFORE `.about-cards` highlights row (D-31 — recruiter scan path)
  </behavior>
  <action>
    Create `app/components/views/about-socials.tsx` per RESEARCH Pattern 10 + CONTEXT D-33/D-34:

    ```tsx
    // NO "use client" — RSC view body (Pitfall 9 / SHELL-02).
    // Phase 4 → Phase 5 carry-forward (Plan 04-05 Gate 9): inline mini-contact card
    // on /about so recruiters find email/github/linkedin without locating the hamburger.
    // CSS classes (.about-socials-card, .contact-row, .contact-label, .contact-muted)
    // defined in app/globals.css. Reuses .contact-row pattern from contact-view.tsx
    // for visual fidelity (D-33).

    import type { Profile } from "@/lib/types";
    import { ExternalLink } from "@/app/components/primitives/external-link";

    interface AboutSocialsProps {
      profile: Profile;
    }

    /**
     * D-34 TODO-guard: if a social URL is the `TODO:` sentinel (or any non-http(s)
     * value), render the row as muted plain text — no `<a>` tag — to avoid emitting
     * a broken link. The INFRA-05 prebuild grep blocks the production build until all
     * URLs are real, so this fallback is only ever hit during development.
     */
    function isRealUrl(url: string): boolean {
      return /^https?:\/\//.test(url);
    }

    export function AboutSocials({ profile }: AboutSocialsProps) {
      const github = profile.socials.find((s) => s.kind === "github");
      const linkedin = profile.socials.find((s) => s.kind === "linkedin");

      return (
        <div
          className="about-socials-card"
          role="group"
          aria-label="Quick contact"
        >
          {/* EMAIL row — native mailto: */}
          <div className="contact-row">
            <span className="contact-label">EMAIL</span>
            <a
              href={`mailto:${profile.email}`}
              aria-label={`Send email to ${profile.email}`}
            >
              {profile.email}
            </a>
            <span />
          </div>

          {/* GITHUB row — ExternalLink with TODO guard */}
          {github && (
            <div className="contact-row">
              <span className="contact-label">GITHUB</span>
              {isRealUrl(github.url) ? (
                <ExternalLink
                  href={github.url}
                  aria-label="Open GitHub (opens in new tab)"
                >
                  {github.handle}
                </ExternalLink>
              ) : (
                <span className="contact-muted">{github.handle}</span>
              )}
              <span />
            </div>
          )}

          {/* LINKEDIN row — ExternalLink with TODO guard */}
          {linkedin && (
            <div className="contact-row">
              <span className="contact-label">LINKEDIN</span>
              {isRealUrl(linkedin.url) ? (
                <ExternalLink
                  href={linkedin.url}
                  aria-label="Open LinkedIn (opens in new tab)"
                >
                  {linkedin.handle}
                </ExternalLink>
              ) : (
                <span className="contact-muted">{linkedin.handle}</span>
              )}
              <span />
            </div>
          )}
        </div>
      );
    }
    ```

    Notes:
    - aria-label values use the EXACT contact-view phrasing (`"Send email to ..."`, `"Open GitHub (opens in new tab)"`, `"Open LinkedIn (opens in new tab)"`) — A11Y-04 plain-noun convention from CLAUDE.md
    - The `<span />` empty cell at the end of each `.contact-row` matches the contact-view 3-column grid (LABEL + LINK + spacer); without it the row collapses
    - DO NOT introduce a `CopyButton` here (contact-view has it for the email row; D-33 specifies the lighter inline-card variant without copy buttons — reuses `.contact-row` CSS only)
    - DO NOT use literal `TODO` string anywhere (Pitfall 9). Comment uses lowercase `TODO:` only as a code-string regex reference (the `/^https?:\/\//` test is what matches, not the string `TODO`)
    - DO NOT modify `app/components/primitives/external-link.tsx` (read-only for this plan)

    WAIT — check the comment string for the literal `TODO` token (uppercase). The comment block above contains `D-34 TODO-guard` — the postbuild grep (INFRA-05) is case-sensitive `/TODO/`. This WILL fail the build. Replace the comment text:
    - Replace "D-34 TODO-guard" → "D-34 invalid-url guard"
    - Replace "if a social URL is the `TODO:` sentinel" → "if a social URL fails the `^https?://` check"
    - Replace "no `<a>` tag — to avoid emitting" → unchanged (no TODO uppercase)

    Re-write the comment block to be `TODO`-free:

    ```tsx
        /**
         * D-34 invalid-url guard: if a social URL fails the /^https?:\/\// check, render
         * the row as muted plain text — no <a> tag — to avoid emitting a broken link.
         * The INFRA-05 prebuild grep blocks the production build until all URLs are real,
         * so this fallback is only ever hit during development.
         */
    ```

    The implementation logic is unchanged; only the comment wording changes to avoid INFRA-05 build failure.

    Then EXTEND `app/components/views/about-view.test.tsx` with new assertions. The existing 5 tests remain (Phase 3 + Phase 4 coverage); add a new describe block:

    ```tsx
    // ... existing describe("AboutView", ...) block stays unchanged ...

    describe("AboutView — Phase 4 carry-forward inline socials (D-31..D-34)", () => {
      test("renders <AboutSocials /> wrapper with role=group + aria-label", () => {
        const { container } = render(<AboutView profile={PROFILE} uptime="8y 125d" />);
        const block = container.querySelector(".about-socials-card");
        expect(block).not.toBeNull();
        expect(block).toHaveAttribute("role", "group");
        expect(block).toHaveAttribute("aria-label", "Quick contact");
      });

      test("renders EMAIL row with mailto: href to PROFILE.email", () => {
        render(<AboutView profile={PROFILE} uptime="8y 125d" />);
        const link = screen.getByRole("link", { name: /send email to/i });
        expect(link).toHaveAttribute("href", `mailto:${PROFILE.email}`);
      });

      test("renders GITHUB row as ExternalLink with target=_blank when URL is real", () => {
        render(<AboutView profile={PROFILE} uptime="8y 125d" />);
        const link = screen.getByRole("link", { name: /open github/i });
        expect(link).toHaveAttribute("target", "_blank");
        expect(link).toHaveAttribute("rel", "noopener noreferrer");
      });

      test("renders LINKEDIN row as ExternalLink when URL is real (current PROFILE.socials)", () => {
        render(<AboutView profile={PROFILE} uptime="8y 125d" />);
        const link = screen.getByRole("link", { name: /open linkedin/i });
        expect(link).toHaveAttribute("target", "_blank");
      });

      test("AboutSocials renders BEFORE .about-cards in the DOM (D-31 scan path)", () => {
        const { container } = render(<AboutView profile={PROFILE} uptime="8y 125d" />);
        const block = container.querySelector(".about-socials-card");
        const cards = container.querySelector(".about-cards");
        expect(block).not.toBeNull();
        expect(cards).not.toBeNull();
        // Use compareDocumentPosition: bitmask 4 means "block precedes cards"
        const position = block!.compareDocumentPosition(cards!);
        expect(position & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();
      });
    });
    ```

    The 5 existing tests + 5 new = 10 total assertions in `about-view.test.tsx`.

    Run `npm test -- about-view.test.tsx about-socials` — must exit 0. Note that AboutSocials does NOT have its own dedicated test file in v1 (D-34 hardcodes the 3-tuple; coverage via about-view.test.tsx is sufficient).
  </action>
  <verify>
    <automated>
    test -f app/components/views/about-socials.tsx &amp;&amp; grep -q 'export function AboutSocials' app/components/views/about-socials.tsx &amp;&amp; grep -q '@/app/components/primitives/external-link' app/components/views/about-socials.tsx &amp;&amp; grep -q 'role="group"' app/components/views/about-socials.tsx &amp;&amp; grep -q 'aria-label="Quick contact"' app/components/views/about-socials.tsx &amp;&amp; grep -q 'mailto:' app/components/views/about-socials.tsx &amp;&amp; grep -q 'isRealUrl' app/components/views/about-socials.tsx &amp;&amp; grep -q 'kind === "github"' app/components/views/about-socials.tsx &amp;&amp; grep -q 'kind === "linkedin"' app/components/views/about-socials.tsx &amp;&amp; ! grep -E '\bTODO\b' app/components/views/about-socials.tsx &amp;&amp; npx vitest run app/components/views/about-view.test.tsx --reporter=verbose
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/components/views/about-socials.tsx` succeeds
    - `app/components/views/about-socials.tsx` does NOT carry `"use client"` (RSC per Pitfall 9 / SHELL-02)
    - `grep -q '@/app/components/primitives/external-link' app/components/views/about-socials.tsx` succeeds (reuses Phase 3 primitive)
    - `grep -q 'role="group"' app/components/views/about-socials.tsx` succeeds
    - `grep -q 'aria-label="Quick contact"' app/components/views/about-socials.tsx` succeeds
    - `grep -q 'isRealUrl' app/components/views/about-socials.tsx` succeeds (D-34 guard)
    - `! grep -E '\bTODO\b' app/components/views/about-socials.tsx` returns 0 (no uppercase TODO — Pitfall 9 / INFRA-05)
    - `npx vitest run app/components/views/about-view.test.tsx` exits 0; total test count is 10 (5 existing + 5 new)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AboutSocials RSC ships with TODO-guarded github/linkedin rows; about-view.test.tsx covers wrapper + email row + 2 ExternalLink rows + DOM position invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AboutSocials into about-view.tsx + append CSS to globals.css</name>
  <read_first>
    - app/components/views/about-view.tsx (existing — line 23-29: insert AboutSocials between bio paragraphs `.map(...)` and `<div className="about-cards">`)
    - .planning/phases/05-seo-accessibility-polish/05-RESEARCH.md §Architecture Patterns Pattern 10 §"CSS additions to app/globals.css"
    - .planning/phases/05-seo-accessibility-polish/05-UI-SPEC.md §"AboutSocials block visual" (24px top margin, 16px border-padding-top, 80px LABEL + 1fr link + gap 12px)
    - .planning/phases/05-seo-accessibility-polish/05-PATTERNS.md §"app/components/views/about-view.tsx (UPDATED — insert <AboutSocials />)"
    - app/globals.css (existing — append the new CSS block at end of file or in the existing about-view CSS section; planner picks; either is acceptable per "no Tailwind / pure CSS" CLAUDE.md constraint)
  </read_first>
  <files>app/components/views/about-view.tsx, app/globals.css</files>
  <action>
    Edit A — `app/components/views/about-view.tsx` line ~27 (after the bio map closing parens, before `<div className="about-cards">`):

    Add the import at the top of the file (alphabetical with existing imports):
    ```tsx
    import { AboutSocials } from "./about-socials";
    ```

    Insert the JSX call between bio paragraphs and `.about-cards` (line ~28-29 after Phase 4 layout):
    ```tsx
          {profile.bio.long.map((para, i) => (
            <p key={i} className="about-para">
              {para}
            </p>
          ))}

          <AboutSocials profile={profile} />

          <div className="about-cards">
    ```

    DO NOT modify the H1, role line, location line, the `.about-cards` body, the CTA row, or the existing `.about-status-mobile` block — those are owned by Phase 3 / Phase 4 and remain unchanged.

    Edit B — append to `app/globals.css` (anywhere after the existing `.about-*` CSS block; if planner cannot locate the .about-* block by inspection, append at end of file under a clear comment header):

    ```css

    /* ── Phase 5: AboutSocials inline mini-contact card (D-31..D-34) ─────────────
       Renders below the bio paragraphs and above the highlights row on /about.
       Always visible (no @media gating per D-32). 80px LABEL column reuses the
       contact-view .contact-row 3-column grid (LABEL + LINK + spacer). */
    .about-socials-card {
      max-width: 480px;
      margin: 24px 0 24px;
      padding-top: 16px;
      border-top: 1px solid var(--border);
      display: flex;
      flex-direction: column;
      gap: 0;
    }

    .contact-muted {
      font-size: 13px;
      color: var(--muted);
    }
    ```

    Notes on visual contract (UI-SPEC §"AboutSocials block visual"):
    - `.about-socials-card` is a flex column wrapper, NOT a grid. The `.contact-row` children inherit their own `grid-template-columns: 80px 1fr` from the existing contact-view CSS in globals.css.
    - `border-top: 1px solid var(--border)` separates from the bio paragraph above (UI-SPEC §Spacing — 16px padding-top inside the border).
    - `margin: 24px 0 24px` — 24px top + 24px bottom (24px is on the locked spacing scale per UI-SPEC).
    - `max-width: 480px` matches the contact-view card width, keeping the row visually anchored to the typography column.
    - `.contact-muted` is the D-34 fallback styling for when a URL is missing.

    DO NOT introduce new `--*` CSS custom properties (CLAUDE.md "Pure CSS + CSS custom properties" — additions allowed but Phase 5 explicitly does NOT add new tokens per UI-SPEC). DO NOT use `display: grid` here (`.contact-row` already handles the 3-column grid; this wrapper is a flex column).

    Run `npm run build` to verify the file compiles. Run `npm test` to confirm 10 assertions still pass (Plan 05-04 Task 1's tests now find a real component wired into about-view).
  </action>
  <verify>
    <automated>
    grep -q 'import { AboutSocials } from "\./about-socials"' app/components/views/about-view.tsx &amp;&amp; grep -q '<AboutSocials profile={profile} />' app/components/views/about-view.tsx &amp;&amp; grep -q '\.about-socials-card' app/globals.css &amp;&amp; grep -q '\.contact-muted' app/globals.css &amp;&amp; grep -q 'border-top: 1px solid var(--border)' app/globals.css &amp;&amp; grep -q 'max-width: 480px' app/globals.css &amp;&amp; npx vitest run app/components/views/about-view.test.tsx --reporter=verbose &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'import { AboutSocials } from "./about-socials"' app/components/views/about-view.tsx` succeeds
    - `grep -q '<AboutSocials profile={profile} />' app/components/views/about-view.tsx` succeeds
    - The `<AboutSocials />` JSX is positioned BETWEEN the bio paragraphs map and `<div className="about-cards">` (line position validated by Task 1 test "AboutSocials renders BEFORE .about-cards in the DOM")
    - `grep -q '.about-socials-card' app/globals.css` succeeds
    - `grep -q '.contact-muted' app/globals.css` succeeds
    - `grep -q 'border-top: 1px solid var(--border)' app/globals.css` succeeds
    - `npx vitest run app/components/views/about-view.test.tsx` exits 0; all 10 assertions pass (5 existing + 5 from Task 1; same file)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - No literal `TODO` (uppercase) introduced
  </acceptance_criteria>
  <done>AboutSocials wired into about-view between bio and highlights; CSS shipped; build green; about-view tests pass against the real component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External link target=_blank rel=noopener | The `<ExternalLink>` primitive (Phase 3) already enforces `rel="noopener noreferrer"` — prevents tabnabbing on the new GitHub/LinkedIn rows. |
| URL validation in TODO guard | `/^https?:\/\//.test(url)` is a structural check, NOT URL safety validation. If PROFILE.socials.url contained `javascript:alert(1)`, the regex would correctly reject it (no `http://` or `https://` prefix) — defense in depth. Phase 6 backend integration should add stronger validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Spoofing | TODO-string in build output | mitigate | Acceptance criteria checks `! grep -E '\bTODO\b' app/components/views/about-socials.tsx`. The comment text uses "invalid-url guard" wording; the regex `/^https?:\/\//` is what enforces the guard, not the string `TODO`. INFRA-05 postbuild gate stays green. |
| T-05-14 | Tampering | Tabnabbing via target=_blank | mitigate | The `<ExternalLink>` primitive sets `rel="noopener noreferrer"`; reused verbatim. Acceptance criteria asserts `link.toHaveAttribute("rel", "noopener noreferrer")` for both GitHub and LinkedIn rows. |
| T-05-15 | Information Disclosure | Email harvesting via mailto | accept | The email `beckprograms@gmail.com` is already public on `/contact` (Phase 3) and exposed via `PROFILE.email` in JSON-LD (Plan 05-05). Recruiter discoverability is the explicit goal; obfuscation contradicts the recruiter-test requirement. |
</threat_model>

<verification>
- `npx vitest run app/components/views/about-view.test.tsx` exits 0; 10 total assertions pass
- `npm run build` exits 0
- `node scripts/check-placeholders.mjs` would exit 0 (postbuild gate — no TODO strings introduced; verified at acceptance criteria via `! grep -E '\bTODO\b'`)
- `grep -q '\.about-socials-card' app/globals.css` (CSS landed)
- `grep -q '<AboutSocials profile={profile} />' app/components/views/about-view.tsx` (wired)
- The DOM-position assertion in about-view.test.tsx ("AboutSocials renders BEFORE .about-cards") guarantees recruiter scan path order (D-31)
</verification>

<success_criteria>
Plan 05-04 is complete when:
1. `app/components/views/about-socials.tsx` ships as RSC (no `"use client"`); reuses `<ExternalLink>` from Phase 3
2. Three rows render: EMAIL (mailto), GITHUB (ExternalLink with TODO guard), LINKEDIN (ExternalLink with TODO guard)
3. Wrapper `<div class="about-socials-card" role="group" aria-label="Quick contact">` matches D-33
4. `app/components/views/about-view.tsx` imports and renders `<AboutSocials profile={profile} />` immediately after the bio paragraphs and before `.about-cards`
5. `app/globals.css` has `.about-socials-card` + `.contact-muted` declarations with the UI-SPEC dimensions (480px max-width, 24px margins, 16px padding-top, 1px var(--border) top border)
6. `app/components/views/about-view.test.tsx` has 10 passing assertions (5 existing + 5 new covering wrapper + email + github + linkedin + DOM position invariant)
7. No literal `TODO` (uppercase) anywhere in the new file
8. `npm run build` exits 0; `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-accessibility-polish/05-04-SUMMARY.md`
</output>
