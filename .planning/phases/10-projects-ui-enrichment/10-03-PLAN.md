---
phase: 10-projects-ui-enrichment
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - tests/contrast.spec.ts
autonomous: true
requirements: [DETAIL-08]

must_haves:
  truths:
    - "The axe contrast matrix scans the expanded Tech highlights panel selectors, not just the collapsed-page selectors"
    - "The strip and panel pass WCAG 2.1 AA color-contrast under all 4 accent hues x 2 themes"
    - "The amber-on-light tight pair (--muted at 12px on --bg) is covered for the strip"
  artifacts:
    - path: "tests/contrast.spec.ts"
      provides: "56-cell axe matrix extended with a panel-visible path on the /projects route"
      contains: "projects-row-trigger"
  key_links:
    - from: "tests/contrast.spec.ts"
      to: "app/components/project-row.tsx"
      via: "page.click on .projects-row-trigger expands the panel before axe analyze"
      pattern: "projects-row-trigger"
---

<objective>
Extend the v1.0 Phase 5 56-cell axe contrast matrix so the new Phase 10 surfaces are actually scanned: the collapsed stat strip (scanned automatically once it renders) and — critically — the expanded Tech highlights panel, which is `hidden` by default and therefore invisible to axe unless a row is expanded first.

Purpose: DETAIL-08 requires the new strip and panel selectors to pass WCAG 2.1 AA color-contrast under all 4 accent hues × 2 themes. axe skips `hidden` / `display:none` content, so without a panel-expansion step the matrix would stay green while silently never checking the panel colors — a false pass. This plan closes that gap.

Output: a modified `tests/contrast.spec.ts` that expands a project panel on the `/projects` route before `analyze()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-projects-ui-enrichment/10-CONTEXT.md
@.planning/phases/10-projects-ui-enrichment/10-RESEARCH.md
@.planning/phases/10-projects-ui-enrichment/10-UI-SPEC.md
@.planning/phases/10-projects-ui-enrichment/10-02-SUMMARY.md

<interfaces>
<!-- The contrast spec already exists. This plan modifies it minimally. -->

Current tests/contrast.spec.ts structure (4 hues × 2 themes × N routes loop):
```typescript
const HUES = [145, 75, 200, 340] as const;   // matrix, amber, cyan, magenta
const THEMES = ["dark", "light"] as const;
for (const theme of THEMES) for (const hue of HUES) for (const route of ROUTES) {
  test(`contrast: theme=${theme} accent=${hue} route=${route.pathname}`, async ({ page }) => {
    await page.addInitScript(/* seed localStorage before paint */);
    await page.goto(route.pathname);
    await expect(page.locator("html")).toHaveAttribute("data-theme", theme); // Pitfall 10 canary
    const results = await new AxeBuilder({ page }).withTags(["wcag2aa","wcag21aa"]).analyze();
    // filter color-contrast violations, assert []
  });
}
```

New selectors that ship in Plan 02 (must be scanned):
- `.projects-row-stats` / `.gh-token` — the collapsed strip (visible on /projects automatically)
- `.projects-row-trigger` — the disclosure button (the click target to expand a panel)
- `.projects-tech-panel` / `.projects-commit-stat` — the expanded panel (hidden by default — REQUIRES expansion)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a panel-visible path so the contrast matrix scans the Tech highlights panel</name>
  <read_first>
    - tests/contrast.spec.ts (the full current 56-cell matrix — the loop, the pre-paint localStorage seed, the Pitfall 10 `data-theme` canary, the violation filter)
    - app/components/project-row.tsx (Plan 02 — the `.projects-row-trigger` button selector and the `.projects-tech-panel` id-controlled panel; confirm the exact class names shipped)
    - app/globals.css (Plan 02 — the `.projects-tech-panel` / `.projects-row-stats` / `.gh-token` / `.projects-commit-stat` rules; confirm color tokens)
    - .planning/phases/10-projects-ui-enrichment/10-RESEARCH.md (Pitfall 4 — axe skips hidden content; Open Question 1 — the planner-chosen mechanism is a `page.click` before `analyze()`)
    - .planning/phases/10-projects-ui-enrichment/10-UI-SPEC.md (Color contract — `--muted` at 12px on `--bg` is the known tight pair to verify under amber-on-light)
  </read_first>
  <action>
    Modify `tests/contrast.spec.ts`. Preserve everything structural: the `HUES = [145, 75, 200, 340]` × `THEMES = ["dark","light"]` × `ROUTES` triple loop, the pre-paint `addInitScript` localStorage seed, the `data-theme` canary assertion (Pitfall 10 — do NOT remove or reorder it relative to `page.goto`), the `AxeBuilder` tags, and the color-contrast violation filter + `expect([]).toEqual([])` assertion.

    Add ONE conditional step: after the `data-theme` canary assertion and BEFORE `new AxeBuilder(...).analyze()`, when `route.pathname` is the `/projects` route, expand the first project panel so axe sees the panel selectors:
    `if (route.pathname === "/projects") { await page.locator(".projects-row-trigger").first().click(); await expect(page.locator(".projects-tech-panel").first()).toBeVisible(); }`.
    Use the exact selectors shipped by Plan 02 (`.projects-row-trigger` for the click target, `.projects-tech-panel` for the visibility assert — verify the names against `project-row.tsx`). The `toBeVisible()` assertion is a canary: if the panel does not become visible the test fails loudly here rather than silently producing a hidden-panel false pass. Pick a project known to have repo-backed stats so the panel actually renders the Tech highlights block (the breakdown / commit-stat / CTA selectors) — if no project has live stats during the test run (no `GITHUB_TOKEN` in CI), the minimal panel still renders and `.projects-tech-panel` is still visible, so the assertion holds either way.

    Add a short comment above the new block explaining WHY it exists (axe skips `hidden` content — Pitfall 4 — so the panel must be expanded for DETAIL-08 coverage). Do not change the cell count math or the test-title template. The strip (`.projects-row-stats` / `.gh-token`) needs no special handling — it is server-rendered and visible on `/projects` so axe scans it automatically.
  </action>
  <verify>
    <automated>npx playwright test tests/contrast.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contrast.spec.ts` contains a `page.locator(".projects-row-trigger").first().click()` step gated to the `/projects` route, placed after the `data-theme` canary and before `AxeBuilder(...).analyze()`.
    - The new block asserts `.projects-tech-panel` becomes visible after the click (a canary against a silent hidden-panel false pass).
    - The `HUES` / `THEMES` / `ROUTES` loop, the pre-paint `addInitScript` seed, and the Pitfall 10 `data-theme` canary are all preserved unchanged.
    - `npx playwright test tests/contrast.spec.ts` exits 0 — every cell green, including the amber-on-light (`hue=75`, `theme=light`) `/projects` cell with the panel expanded.
    - A comment in the file explains the panel-expansion step exists because axe skips `hidden` content (Pitfall 4 / DETAIL-08).
  </acceptance_criteria>
  <done>`tests/contrast.spec.ts` expands a project panel on the `/projects` route before axe runs; the full matrix passes WCAG 2.1 AA across 4 hues × 2 themes with the strip and panel selectors covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → rendered `/projects` page | Playwright drives a real browser against the built app; no untrusted input — this is a verification-only plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Repudiation | DETAIL-08 contrast coverage | mitigate | Without the panel-expansion step the matrix would pass while never scanning the `hidden` panel — a silent unverified accessibility claim. The `toBeVisible()` canary on `.projects-tech-panel` makes a non-expanded panel fail the test loudly, so a green matrix genuinely attests panel-selector contrast. |
| T-10-08 | — | Test-only change | accept | This plan modifies only `tests/contrast.spec.ts` — no production code, no runtime surface, no new dependency. No production threat. |
</threat_model>

<verification>
- `npx playwright test tests/contrast.spec.ts` — full matrix green, panel scanned on `/projects`.
- `npm test` — Vitest suite still green (test-only change, no regressions expected).
- `npm run build` — production build still clean.
</verification>

<success_criteria>
- The contrast matrix expands the Tech highlights panel on the `/projects` route before axe analyzes the page.
- The strip and panel selectors pass WCAG 2.1 AA color-contrast under all 4 accent hues × 2 themes (DETAIL-08).
- A `toBeVisible()` canary guards against a silent hidden-panel false pass.
- No production code is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/10-projects-ui-enrichment/10-03-SUMMARY.md`.
</output>
