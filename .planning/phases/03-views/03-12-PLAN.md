---
phase: 03-views
plan: 12
type: execute
wave: 5
depends_on: [03-05, 03-06, 03-07, 03-08, 03-09, 03-10, 03-11]
files_modified:
  - app/(terminal)/views.test.tsx
autonomous: true
requirements: [ROUTE-02, TEST-05]
tags: [test, cross-view, metadata-uniqueness, phase-3]

must_haves:
  truths:
    - "A single Vitest spec at app/(terminal)/views.test.tsx imports metadata from each of the 7 page modules and asserts title uniqueness via new Set(titles).size === 7."
    - "The spec also asserts every metadata exports a non-empty description and an alternates.canonical matching the route's pathname."
    - "Test runs green against the 7 enriched page modules from plans 03-05..03-11; no mocking is needed since metadata is a static export."
  artifacts:
    - path: "app/(terminal)/views.test.tsx"
      provides: "Cross-view metadata audit (TEST-05 set-deduplication test, ROUTE-02 enforcement)"
  key_links:
    - from: "app/(terminal)/views.test.tsx"
      to: "metadata exports of all 7 page.tsx files"
      via: "static import of { metadata } from each page module"
      pattern: "import.*metadata.*from.*page"
---

<objective>
Ship the single cross-view metadata-uniqueness spec that closes RESEARCH §Pitfall 1 mitigation and the ROUTE-02 / Phase-3 success criterion (`Vitest set-deduplication test passes`).

Purpose: Per-view smoke specs (plans 05–11) each assert their own LOCKED title, but they cannot detect a regression where two views drift to the same title. This single cross-view spec is the load-bearing audit. It also verifies that every page module exports the enriched `metadata.description` and `metadata.alternates.canonical` added in Wave 3.

Output: 1 new test file. Zero source code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-views/03-CONTEXT.md
@.planning/phases/03-views/03-PATTERNS.md
@.planning/phases/03-views/03-RESEARCH.md
@app/(terminal)/page.tsx
@app/(terminal)/projects/page.tsx
@app/(terminal)/stack/page.tsx
@app/(terminal)/experience/page.tsx
@app/(terminal)/writing/page.tsx
@app/(terminal)/contact/page.tsx
@app/(terminal)/shipped/page.tsx
@lib/routes.ts
@app/sitemap.test.tsx

<interfaces>
<!-- Metadata shape every page module exports (post-Wave-3) -->
```ts
export const metadata: Metadata = {
  title: `${route.label} — Bakytbek Tatibekov`,
  description: route.description,
  alternates: { canonical: route.pathname }
};
```

<!-- ROUTES order in lib/routes.ts (single source of truth) -->
```ts
ROUTES[0] // about      (pathname: "/")
ROUTES[1] // projects   (pathname: "/projects")
ROUTES[2] // stack      (pathname: "/stack")
ROUTES[3] // experience (pathname: "/experience")
ROUTES[4] // writing    (pathname: "/writing")
ROUTES[5] // contact    (pathname: "/contact")
ROUTES[6] // shipped    (pathname: "/shipped")
```

<!-- LOCKED titles (Phase 2 D-12 / D-13 carry-forward) -->
- "about.md — Bakytbek Tatibekov"
- "projects/ — Bakytbek Tatibekov"
- "stack.json — Bakytbek Tatibekov"
- "experience.log — Bakytbek Tatibekov"
- "writing/ — Bakytbek Tatibekov"
- "contact.sh — Bakytbek Tatibekov"
- "shipped.app — Bakytbek Tatibekov"

<!-- Test conventions (Phase 2 inheritance) -->
- vitest.config.ts has `globals: true` — DO NOT import describe/test/expect from "vitest"
- File location matches the route group: `app/(terminal)/views.test.tsx`
- Static metadata exports do not require mocks — they are plain JS exports
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create cross-view metadata-uniqueness spec</name>
  <files>app/(terminal)/views.test.tsx</files>
  <read_first>
    - app/sitemap.test.tsx (analog — page-module default-export assertion, globals-enabled vitest convention)
    - app/(terminal)/page.tsx (about — verify metadata export shape after Plan 03-05)
    - app/(terminal)/projects/page.tsx (verify metadata export after Plan 03-06)
    - app/(terminal)/stack/page.tsx (verify metadata export after Plan 03-07)
    - app/(terminal)/experience/page.tsx (verify metadata export after Plan 03-08)
    - app/(terminal)/writing/page.tsx (verify metadata export after Plan 03-09)
    - app/(terminal)/contact/page.tsx (verify metadata export after Plan 03-10)
    - app/(terminal)/shipped/page.tsx (verify metadata export after Plan 03-11)
    - lib/routes.ts (ROUTES order — drives the canonical-pathname assertion)
    - .planning/phases/03-views/03-RESEARCH.md §"Pitfall 1: Per-route metadata uniqueness" (mitigation specifies this exact spec)
  </read_first>
  <action>
    Create `app/(terminal)/views.test.tsx` with EXACTLY this content:

    ```tsx
    import { ROUTES } from "@/lib/routes";

    import { metadata as aboutMeta } from "./page";
    import { metadata as projectsMeta } from "./projects/page";
    import { metadata as stackMeta } from "./stack/page";
    import { metadata as experienceMeta } from "./experience/page";
    import { metadata as writingMeta } from "./writing/page";
    import { metadata as contactMeta } from "./contact/page";
    import { metadata as shippedMeta } from "./shipped/page";

    /* Cross-view metadata audit — closes RESEARCH §Pitfall 1 mitigation
       and ROUTE-02 / Phase-3 success criterion (set-deduplication test). */

    const VIEWS = [
      { route: ROUTES[0], metadata: aboutMeta },
      { route: ROUTES[1], metadata: projectsMeta },
      { route: ROUTES[2], metadata: stackMeta },
      { route: ROUTES[3], metadata: experienceMeta },
      { route: ROUTES[4], metadata: writingMeta },
      { route: ROUTES[5], metadata: contactMeta },
      { route: ROUTES[6], metadata: shippedMeta }
    ] as const;

    describe("Cross-view metadata audit (TEST-05 / ROUTE-02)", () => {
      test("all 7 view metadata.title strings are unique", () => {
        const titles = VIEWS.map((v) => v.metadata.title);
        expect(new Set(titles).size).toBe(7);
      });

      test("every view exports a non-empty metadata.description", () => {
        for (const { route, metadata } of VIEWS) {
          expect(
            typeof metadata.description === "string" && metadata.description.length > 0,
            `expected metadata.description on ${route.pathname}`
          ).toBe(true);
        }
      });

      test("every view exports alternates.canonical equal to its route pathname", () => {
        for (const { route, metadata } of VIEWS) {
          expect(metadata.alternates?.canonical).toBe(route.pathname);
        }
      });

      test("count of audited views equals ROUTES length (drift guard)", () => {
        expect(VIEWS.length).toBe(ROUTES.length);
        expect(ROUTES.length).toBe(7);
      });
    });
    ```

    Notes:
    - `globals: true` is set in `vitest.config.ts` — DO NOT import `describe`/`test`/`expect` from "vitest".
    - Each `metadata` export is a static `Metadata` object (D-17 — no `generateMetadata`), so plain ES imports work; no module mocks needed.
    - The `metadata.title` field type is `Metadata["title"]` which is `string | TemplateString | null | undefined`. After Wave 3, every plan ships a plain string title, so `new Set(titles).size === 7` is meaningful. If a future change makes a title a `TemplateString`, this test will fail loudly — that is intentional (D-17 forbids that drift).
    - The drift-guard test at the bottom protects against a future regression where someone adds an 8th route to `ROUTES` but forgets to import its metadata here. The `VIEWS.length === ROUTES.length` assertion catches that.
    - DO NOT add render-based assertions — TEST-05 D-18 scopes those to per-view smoke specs (plans 05–11).
    - DO NOT mock `next/navigation` — these imports are static-only and never touch navigation hooks.
  </action>
  <verify>
    <automated>test -f 'app/(terminal)/views.test.tsx' &amp;&amp; grep -c 'from "vitest"' 'app/(terminal)/views.test.tsx' | grep -qx 0 &amp;&amp; grep -q 'new Set(titles).size' 'app/(terminal)/views.test.tsx' &amp;&amp; grep -q 'metadata.alternates' 'app/(terminal)/views.test.tsx' &amp;&amp; grep -q 'metadata.description' 'app/(terminal)/views.test.tsx' &amp;&amp; grep -q 'VIEWS.length' 'app/(terminal)/views.test.tsx' &amp;&amp; npm test -- 'app/(terminal)/views.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f 'app/(terminal)/views.test.tsx'` exits 0
    - `grep -c 'from "vitest"' 'app/(terminal)/views.test.tsx'` returns `0` (globals enabled — no vitest import per project convention)
    - `grep -c "import { metadata" 'app/(terminal)/views.test.tsx'` returns `7` (one import per route)
    - `grep -q 'new Set(titles).size' 'app/(terminal)/views.test.tsx'` succeeds (set-deduplication assertion present)
    - `grep -q 'metadata.alternates' 'app/(terminal)/views.test.tsx'` succeeds (canonical assertion present)
    - `grep -q 'metadata.description' 'app/(terminal)/views.test.tsx'` succeeds (description assertion present)
    - `grep -q 'VIEWS.length' 'app/(terminal)/views.test.tsx'` succeeds (drift-guard against ROUTES additions)
    - `npm test -- 'app/(terminal)/views.test.tsx'` exits 0 with 4 passing tests
    - `npm test` (full suite) exits 0 — no regressions in any per-view smoke spec
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Cross-view spec green. Set-dedup audit present. Pitfall 1 mitigation closed. ROUTE-02 success criterion satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new trust boundaries — it is a static-import audit spec.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Tampering (metadata title collision regression) | All 7 page modules | mitigate | This spec is the mitigation. `new Set(titles).size === 7` assertion catches any regression where two routes drift to the same title — the success-criterion guard for ROUTE-02. |
| T-03-20 | Tampering (route additions without metadata audit) | `ROUTES` registry vs spec import set | mitigate | The `VIEWS.length === ROUTES.length` drift guard fails loudly if a future change adds an 8th route to `ROUTES` without updating this spec — preventing a silent metadata audit gap. |

</threat_model>

<verification>
- `npm test -- 'app/(terminal)/views.test.tsx'` exits 0 with 4 tests passing
- `npm test` (full suite) exits 0 — no regressions in plans 05–11 specs
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `grep -c 'import { metadata' 'app/(terminal)/views.test.tsx'` returns `7`
- `grep -q 'new Set(titles).size).toBe(7)' 'app/(terminal)/views.test.tsx'` succeeds
</verification>

<success_criteria>
- [ ] `app/(terminal)/views.test.tsx` exists with 4 passing tests
- [ ] All 7 metadata imports present
- [ ] Set-deduplication assertion satisfies ROUTE-02 success criterion
- [ ] Description and canonical assertions satisfy Wave 3 metadata-enrichment guarantees
- [ ] Drift guard satisfies ROUTES additions guard
- [ ] No imports from "vitest" (project convention)
- [ ] Build, lint, typecheck, full test suite all green
</success_criteria>

<output>
After completion, create `.planning/phases/03-views/03-12-SUMMARY.md` per `summary.md` template — note that this is the load-bearing cross-view audit that protects ROUTE-02 from per-view drift.
</output>
