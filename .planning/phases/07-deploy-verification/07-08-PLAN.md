---
phase: 07-deploy-verification
plan: 08
type: execute
wave: 4
depends_on:
  - "07-01"
  - "07-03"
files_modified:
  - .planning/phases/07-deploy-verification/07-VERIFICATION.md
  - .planning/phases/07-deploy-verification/screenshots/375/about.png
  - .planning/phases/07-deploy-verification/screenshots/375/projects.png
  - .planning/phases/07-deploy-verification/screenshots/375/stack.png
  - .planning/phases/07-deploy-verification/screenshots/375/experience.png
  - .planning/phases/07-deploy-verification/screenshots/375/writing.png
  - .planning/phases/07-deploy-verification/screenshots/375/contact.png
  - .planning/phases/07-deploy-verification/screenshots/375/shipped.png
autonomous: false
requirements:
  - DEPLOY-07
tags:
  - 375px-shell-review
  - mobile
  - devtools-emulation
  - manual-attestation
  - evidence-capture
must_haves:
  truths:
    - "Chrome DevTools 375px viewport screenshot captured for each of the 7 production routes on https://www.tatibekov.com"
    - "Per-route manual eyeball pass confirms: TopBar resume button visible above fold, no horizontal overflow, hamburger trigger accessible, no clipped text, theme + accent picker reachable"
    - "Real-device carry-forwards (Phase 4 Gates 7+8 iPhone Safari + Android Chrome; Phase 5 Gate 3 reduce-motion real-device) close via DevTools emulation per D-18"
    - "Consciously-accepted limitations documented (Safari dvh/svh, soft-keyboard behavior, address-bar overlap, -webkit-overflow-scrolling NOT physically validated)"
    - "07-VERIFICATION.md DEPLOY-07 section documents 7-route review verdict + closes real-device carry-forwards explicitly"
  artifacts:
    - path: ".planning/phases/07-deploy-verification/07-VERIFICATION.md"
      provides: "DEPLOY-07 section with per-route verdict + Known Limitations subsection"
      contains: "DEPLOY-07"
    - path: ".planning/phases/07-deploy-verification/screenshots/375/about.png"
      provides: "375px screenshot of /"
    - path: ".planning/phases/07-deploy-verification/screenshots/375/projects.png"
      provides: "375px screenshot of /projects"
    - path: ".planning/phases/07-deploy-verification/screenshots/375/stack.png"
      provides: "375px screenshot of /stack"
    - path: ".planning/phases/07-deploy-verification/screenshots/375/experience.png"
      provides: "375px screenshot of /experience"
    - path: ".planning/phases/07-deploy-verification/screenshots/375/writing.png"
      provides: "375px screenshot of /writing"
    - path: ".planning/phases/07-deploy-verification/screenshots/375/contact.png"
      provides: "375px screenshot of /contact"
    - path: ".planning/phases/07-deploy-verification/screenshots/375/shipped.png"
      provides: "375px screenshot of /shipped"
  key_links:
    - from: "Phase 4 mobile redistribution (hamburger drawer, top-bar resume, AboutSocials inline)"
      to: "375px eyeball pass on production"
      via: "Chrome DevTools viewport emulation"
      pattern: "screenshots/375/"
    - from: "Phase 4 Gates 7+8 + Phase 5 Gate 3 real-device carry-forwards"
      to: "DEPLOY-07 closure via DevTools emulation (D-18)"
      via: "Consciously-accepted limitation documentation"
      pattern: "Known Limitations"
---

<objective>
Run the 375px mobile-viewport shell review on https://www.tatibekov.com (production URL, NOT localhost per D-20) using Chrome DevTools device emulation across all 7 routes. Capture a screenshot per route under `screenshots/375/`. Manual eyeball-pass confirms: TopBar resume button visible above the fold, no horizontal overflow, hamburger trigger accessible, no clipped text, theme + accent picker reachable. Close the Phase 4 Gates 7+8 + Phase 5 Gate 3 real-device carry-forwards via DevTools emulation per D-18, with consciously-accepted limitations explicitly documented in a Known Limitations subsection of 07-VERIFICATION.md.

Purpose: DEPLOY-07 — all shell elements verified at 375px viewport on production; every element accessible with no overflow. Plus the multi-phase real-device carry-forwards close via the same DevTools emulation per D-18.

Output: 7 screenshot PNGs in `screenshots/375/` + 07-VERIFICATION.md DEPLOY-07 section with per-route review table + Known Limitations subsection + verdict PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-deploy-verification/07-CONTEXT.md
@.planning/phases/07-deploy-verification/07-RESEARCH.md
@.planning/phases/07-deploy-verification/07-VALIDATION.md
@.planning/phases/04-mobile-responsive/04-VERIFICATION.md
@.planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md

<canonical_refs>
- CONTEXT.md D-18 — real-device closure via DevTools emulation; consciously-accepted limitations documented
- CONTEXT.md D-20 — DEPLOY-07 protocol: Chrome DevTools 375px × 7 routes × production URL + manual eyeball criteria
- Phase 4 04-VERIFICATION.md §Gates 7+8 — DEFERRED-PHASE-7 markers (iPhone Safari + Android Chrome real-device)
- Phase 5 05-VERIFICATION.md §Gate 3 — DEFERRED-PHASE-7 marker (reduce-motion real-device)
- VALIDATION.md DEPLOY-07 evidence-capture + manual-attestation rows
</canonical_refs>

<interfaces>
<!-- 375px shell review checklist (D-20) — single source of truth. -->

Per-route manual eyeball criteria (apply to each of 7 routes):
1. TopBar resume button (`↓ resume.pdf`) is visible WITHOUT scrolling (above the fold). SHELL-03 / Risk 3 prevention.
2. No horizontal overflow — page does NOT have a horizontal scrollbar; no element bleeds past the 375px viewport edge.
3. Hamburger trigger (`☰`) is visible and tappable in the TopBar (left side at <=960px per Phase 4).
4. No clipped text — every visible text node fits the available width OR wraps to a new line gracefully (no `text-overflow: hidden` with no continuation, no text-cut-by-overflow).
5. Theme toggle button (`☼ light` / `☾ dark`) is reachable in the TopBar.
6. Accent picker / command palette trigger (`⌘K` button) is reachable in the TopBar — at 375px the palette opens as a bottom-sheet per Phase 4 PALETTE-05.

Route paths:
- `/` (about) → screenshot file: `about.png`
- `/projects` → `projects.png`
- `/stack` → `stack.png`
- `/experience` → `experience.png`
- `/writing` → `writing.png`
- `/contact` → `contact.png`
- `/shipped` → `shipped.png`

Real-device carry-forwards to close in this plan (per D-18):
- Phase 4 Gate 7: iPhone Safari real-device test → closes via DevTools emulation here
- Phase 4 Gate 8: Android Chrome real-device test → closes via DevTools emulation here
- Phase 5 Gate 3: reduce-motion real-device test → closes via DevTools emulation + Phase 5 source-level reduce-motion CSS already validated

Consciously-accepted limitations (D-18) — must be DOCUMENTED in Known Limitations subsection:
- Safari-specific `dvh`/`svh` viewport units NOT physically validated
- Soft-keyboard overlap behavior in mobile palette NOT physically validated
- Mobile address-bar overlap at TopBar NOT physically validated
- `-webkit-overflow-scrolling: touch` NOT physically validated
- If a v1.1 user reports a Safari-only `dvh/svh` / soft-keyboard / address-bar issue, that triggers physical-device validation post-v1
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Chrome DevTools 375px viewport screenshots × 7 routes on production</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-20 (full protocol)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-18 (DevTools emulation accepted as closure surface)
  </read_first>
  <what-built>
    The shell at https://www.tatibekov.com is built to redistribute below ~960px per Phase 4:
    - Sidebar → hamburger drawer (ExplorerDrawer client island)
    - STATUS block → about-view footer (StatusBlock RSC)
    - Resume CTA → persistent top-bar button
    - Command palette → bottom-sheet at 375px
    All 7 routes inherit these behaviors via the persistent shell at app/(terminal)/layout.tsx.
  </what-built>
  <how-to-verify>
    Setup Chrome DevTools 375px emulation:
    1. Open https://www.tatibekov.com in Chrome (regular tab, not incognito — sometimes incognito alters default zoom).
    2. Open DevTools (F12 or right-click → Inspect).
    3. Click the "Toggle device toolbar" icon (top-left of DevTools panel; keyboard shortcut Cmd+Shift+M on Mac, Ctrl+Shift+M on Win/Linux).
    4. From the device dropdown, choose "iPhone SE" (375 × 667) OR set Responsive mode width to exactly 375. Confirm the viewport ruler reads `375` at the top.
    5. Set zoom to 100%.
    6. Optional but helpful: in device-toolbar settings, enable "Show device frame" off for cleaner screenshots.

    For each of the 7 routes:
    1. Navigate to `https://www.tatibekov.com<path>` (replace `<path>` with `/`, `/projects`, `/stack`, `/experience`, `/writing`, `/contact`, `/shipped`).
    2. Wait for the route to fully render (RSC content loaded; client islands hydrated — give it 2-3 seconds on a fresh load).
    3. Apply the 6-point eyeball criteria from `<interfaces>`. Note any failures in a scratch buffer.
    4. Capture screenshot. Two acceptable approaches:
       - **Full-viewport capture** (recommended): DevTools "Capture full size screenshot" command (Cmd+Shift+P → "Capture full size screenshot") — captures the entire page including content below the fold.
       - **Viewport-only**: Native OS screenshot of just the DevTools-emulated browser frame (Cmd+Shift+4 on macOS, Snipping Tool on Windows) — captures only what's visible above the fold.
       Recommend the full-size capture so reviewers can verify above-fold AND below-fold layout in one image.
    5. Save to `.planning/phases/07-deploy-verification/screenshots/375/<route-slug>.png` where `<route-slug>` is `about` / `projects` / `stack` / `experience` / `writing` / `contact` / `shipped`.
    6. Move to the next route. Repeat.

    Estimated wall-clock: ~5 minutes per route × 7 = ~35 minutes including eyeball-pass + screenshot save.

    If any route fails an eyeball-criterion:
    - Note the specific failure (e.g. "/projects: project-grid cards overflow horizontally at 375px — needs `min-width: 0` on flex children").
    - This is a fix-in-place candidate. Apply the smallest CSS fix in app/globals.css. Commit (`fix(07): correct <route> 375px overflow (DEPLOY-07 remediation)`). Push. Wait for redeploy. Re-screenshot. Update the row.
    - Do NOT proceed to Task 2 with any FAIL routes unrecorded — either fix-in-place or escalate to checkpoint:decision.
  </how-to-verify>
  <acceptance_criteria>
    - 7 PNG files exist in `.planning/phases/07-deploy-verification/screenshots/375/`: `about.png`, `projects.png`, `stack.png`, `experience.png`, `writing.png`, `contact.png`, `shipped.png`.
    - Each screenshot shows the route at 375px viewport on production URL (not localhost, not Vercel-hostname alias).
    - Per-route eyeball criteria notes captured in scratch buffer (6 checks × 7 routes = 42 micro-verdicts).
    - All 42 micro-verdicts are PASS (or any FAIL was fix-in-place remediated before screenshotting).
  </acceptance_criteria>
  
  <action>
    See <how-to-verify> below — this is a manual human action with no CLI/API surface in scope. The executor agent halts here; the user performs the steps and signals resume.
  </action>
  <verify>
    <automated>MISSING — this checkpoint produces evidence (screenshot or stateful UI change) that the next automated task asserts. See acceptance_criteria for human verification.</automated>
  </verify>
  <done>
- 7 PNG files exist in `.planning/phases/07-deploy-verification/screenshots/375/`: `about.png`, `projects.png`, `stack.png`, `experience.png`, `writing.png`, `contact.png`, `shipped.png`.
    - Each screenshot shows the route at 375px viewport on production URL (not localhost, not Vercel-hostname alias).
    - Per-route eyeball criteria notes captured in scratch buffer (6 checks × 7 routes = 42 micro-verdicts).
    - All 42 micro-verdicts are PASS (or any FAIL was fix-in-place remediated before screenshotting).
  </done>
  <resume-signal>Type "375px captured" with confirmation that all 7 PNGs exist and the 42-cell mental table is all PASS, OR describe halt condition (e.g. "/projects has horizontal overflow that needs CSS fix").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Append DEPLOY-07 section + close real-device carry-forwards in 07-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-VERIFICATION.md (existing; append section)
    - Task 1 scratch-buffer notes (42 micro-verdicts)
    - .planning/phases/04-mobile-responsive/04-VERIFICATION.md (Gate 7 + Gate 8 DEFERRED-PHASE-7 markers — Phase 7 traceability)
    - .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md (Gate 3 DEFERRED-PHASE-7 marker)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-18 (full list of consciously-accepted limitations)
  </read_first>
  <action>
    Append a `## DEPLOY-07 — 375px shell review (production URL) + real-device carry-forward closure` section to 07-VERIFICATION.md.

    Section content:

    ```markdown
    ## DEPLOY-07 — 375px shell review (production) + real-device carry-forward closure

    **Methodology:** D-20 — Chrome DevTools 375px viewport (iPhone SE preset or Responsive 375 width) on each of 7 production routes. Manual eyeball-pass on 6 criteria per route. Production URL: https://www.tatibekov.com.

    **Review date:** YYYY-MM-DD

    ### Per-route review

    | Route | TopBar resume above fold | No horiz overflow | Hamburger reachable | No clipped text | Theme toggle reachable | Palette trigger reachable | Verdict |
    |-------|---|---|---|---|---|---|---------|
    | / | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | PASS |
    | /projects | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | PASS |
    | /stack | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | PASS |
    | /experience | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | PASS |
    | /writing | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | PASS |
    | /contact | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | PASS |
    | /shipped | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | PASS |

    ### Evidence

    7 screenshots captured at 375px viewport on production:
    - `screenshots/375/about.png` — /
    - `screenshots/375/projects.png` — /projects
    - `screenshots/375/stack.png` — /stack
    - `screenshots/375/experience.png` — /experience
    - `screenshots/375/writing.png` — /writing
    - `screenshots/375/contact.png` — /contact
    - `screenshots/375/shipped.png` — /shipped

    **DEPLOY-07 verdict: PASS** (42/42 cells across 7 routes × 6 criteria).

    ### Real-device carry-forwards closed (per D-18)

    Per D-18, the following real-device gates close via the DevTools-emulation + 56-cell axe matrix (Phase 5) + PSI mobile profile (Plan 04 Wave 3) combination:

    - **Phase 4 Gate 7 (iPhone Safari real-device):** closed via DevTools iPhone SE emulation across 7 routes + Phase 5 axe 56-cell matrix (4 hues × 2 themes × 7 routes WCAG 2.1 AA).
    - **Phase 4 Gate 8 (Android Chrome real-device):** closed via DevTools Pixel-equivalent emulation across 7 routes + same axe matrix.
    - **Phase 5 Gate 3 (reduce-motion real-device):** closed via DevTools "Emulate CSS prefers-reduced-motion: reduce" + Phase 5 universal-selector reduced-motion reset in app/globals.css (Plan 05-03) + scripts/check-reduced-motion.mjs smoke gate.

    ### Known Limitations (consciously accepted per D-18)

    The following Safari-specific behaviors are NOT physically validated in v1. If a v1.1 user reports a Safari-only issue along any of these dimensions, that triggers physical-device validation:

    - **Safari `dvh`/`svh` viewport units** — Phase 4 uses `dvh` in some places; behavior under iOS Safari address-bar dynamics not physically tested.
    - **Soft-keyboard behavior** — mobile palette / contact input fields with iOS soft-keyboard active not physically tested.
    - **Mobile address-bar overlap at TopBar** — when iOS Safari address bar slides, behavior at the persistent TopBar not physically tested.
    - **`-webkit-overflow-scrolling: touch`** — momentum-scroll behavior in mobile sheets/drawers not physically tested.
    ```

    Fill the per-route table with actual `✓` markers per the scratch buffer from Task 1. If any cell was a FAIL that didn't get fix-in-place remediation, the row reads `✗` for that criterion and the Verdict cell reads FAIL with a footnote — but Task 1 acceptance gating should have prevented this.

    Replace `YYYY-MM-DD` with the actual review date.

    Commit:
    `git add .planning/phases/07-deploy-verification/07-VERIFICATION.md .planning/phases/07-deploy-verification/screenshots/375/`
    `git commit -m "docs(07): record DEPLOY-07 375px shell review + close Ph4 G7/G8 + Ph5 G3 real-device carries (PASS)"`
    `git push origin main`
  </action>
  <verify>
    <automated>grep -q '^## DEPLOY-07' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'TopBar resume above fold' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'screenshots/375/about.png' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'screenshots/375/shipped.png' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'Known Limitations' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'Phase 4 Gate 7' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'Phase 5 Gate 3' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'DEPLOY-07 verdict: PASS' .planning/phases/07-deploy-verification/07-VERIFICATION.md && for f in about projects stack experience writing contact shipped; do test -f .planning/phases/07-deploy-verification/screenshots/375/$f.png || exit 1; done && git log -1 --pretty=%s | grep -q 'DEPLOY-07'</automated>
  </verify>
  <acceptance_criteria>
    - 07-VERIFICATION.md contains a populated DEPLOY-07 section with 7-row × 6-criteria per-route table.
    - Each cell shows `✓` (or `✗` with footnote if FAIL); each Verdict cell is PASS.
    - Evidence subsection lists all 7 PNG paths.
    - Real-device carry-forwards subsection explicitly closes Phase 4 Gate 7, Phase 4 Gate 8, Phase 5 Gate 3.
    - Known Limitations subsection enumerates the 4 consciously-accepted Safari-specific items.
    - Verdict line: `**DEPLOY-07 verdict: PASS** (42/42 cells)`.
    - Single commit lands verification section + 7 PNGs; pushed.
  </acceptance_criteria>
  <done>
    DEPLOY-07 evidence + real-device carry-forwards both closed in one section. Ready for Plan 09 close-out.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chrome DevTools emulation | Local browser feature; runs against production URL. No risk surface beyond what the production URL itself exposes. |
| Screenshot files | PNG binary; non-executable. Captured screenshots show production UI only — no secrets, no auth surface (no auth exists). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-30 | Repudiation | DevTools emulation vs real device behavior gap | accept | D-18 consciously accepts the gap. Task 2 documents the 4 specific gap dimensions (dvh/svh, soft-keyboard, address-bar, -webkit-overflow-scrolling) in Known Limitations. v1.1 carry path exists if real-world issues surface. |
| T-07-31 | Tampering | Manual eyeball-pass subjectivity | mitigate | Screenshots committed alongside the per-route table — a reviewer can re-verify the 6 criteria against the PNGs without re-running DevTools. The 6 criteria are concrete (button visible, no overflow, etc.), not abstract. |
| T-07-32 | Information Disclosure | Screenshots reveal full production UI | accept | Production UI is already public at https://www.tatibekov.com. Screenshots are evidence of what's already shipped; no leak. |

ASVS L1: PASS. No new attack surface, no auth, no PII, no secrets.
</threat_model>

<verification>
- Task 1: 7 screenshots at 375px on production captured; 42-cell eyeball pass complete.
- Task 2: DEPLOY-07 section appended to 07-VERIFICATION.md with full per-route table + Known Limitations + carry-forward closures; 8-file commit lands on origin/main.
- Phase 4 Gates 7+8 + Phase 5 Gate 3 markers now traceably closed.
</verification>

<success_criteria>
This plan succeeds when:
1. All 7 PNG screenshots exist under `screenshots/375/` captured at 375px viewport on production.
2. 42-cell per-route × per-criterion table is all `✓`.
3. Known Limitations subsection enumerates the 4 consciously-accepted Safari-specific gaps.
4. Phase 4 Gate 7, Phase 4 Gate 8, Phase 5 Gate 3 are explicitly closed in the section.
5. Single commit lands verification + 7 PNGs; verdict line reads PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deploy-verification/07-08-SUMMARY.md` capturing:
- Review date
- Screenshot paths (7 files)
- Any fix-in-place commits triggered during Task 1 (with SHAs)
- Commit SHA for the docs commit
- Explicit traceability: closes Phase 4 Gate 7, Phase 4 Gate 8, Phase 5 Gate 3 via D-18 DevTools emulation
- 4 Known Limitations items documented
</output>
