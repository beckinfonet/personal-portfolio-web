---
id: 05-01
phase: 05-seo-accessibility-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - assets/JetBrainsMono-Bold.ttf
  - assets/JetBrainsMono-Medium.ttf
  - assets/JETBRAINS-MONO-LICENSE.txt
  - playwright.config.ts
  - tests/contrast.spec.ts
  - app/components/shell/json-ld-person.test.tsx
  - app/components/shell/console-signature.test.tsx
  - lib/json-ld.test.ts
  - app/layout.test.tsx
  - scripts/check-og-files.mjs
  - scripts/check-reduced-motion.mjs
  - scripts/check-head-comment.mjs
  - scripts/check-headers.mjs
autonomous: true
requirements:
  - SEO-03
  - A11Y-07
  - SEO-01
  - SEO-02
  - SEO-04
  - A11Y-03
  - DEV-01
  - DEV-02
  - DEV-03
status: pending
tags:
  - seo
  - accessibility
  - foundation
  - test-scaffold
must_haves:
  truths:
    - "Devdeps @axe-core/playwright@^4.11.3 + playwright@^1.59.1 installed; chromium binary fetched"
    - "JetBrains Mono Bold + Medium TTFs committed to assets/ with OFL-1.1 LICENSE file (out of public/ surface)"
    - "Empty test files exist with one passing trivial assertion each so npm test stays green during Wave 1"
    - "Empty smoke-script files exist with usage-printed-and-exit-0 stub so package.json wiring does not break"
    - "playwright.config.ts boots next dev as webServer at http://localhost:3000"
    - "package.json has test:contrast script wired to playwright test"
  artifacts:
    - path: "package.json"
      contains: '"@axe-core/playwright"'
    - path: "package.json"
      contains: '"playwright"'
    - path: "package.json"
      contains: '"test:contrast"'
    - path: "playwright.config.ts"
      contains: 'webServer'
    - path: "assets/JetBrainsMono-Bold.ttf"
      provides: "OG font binary (Bold 700)"
    - path: "assets/JetBrainsMono-Medium.ttf"
      provides: "OG font binary (Medium 500)"
    - path: "assets/JETBRAINS-MONO-LICENSE.txt"
      contains: 'OFL'
    - path: "tests/contrast.spec.ts"
      provides: "Playwright contrast spec scaffold"
    - path: "scripts/check-og-files.mjs"
      provides: "Smoke: 8 OG files exist (scaffold, real check filled here)"
    - path: "scripts/check-reduced-motion.mjs"
      provides: "Smoke: globals.css universal-selector reset (scaffold, fails-loud until 05-03 ships)"
    - path: "scripts/check-head-comment.mjs"
      provides: "Smoke: HEAD comment in source (scaffold, fails-loud until 05-05 ships)"
    - path: "scripts/check-headers.mjs"
      provides: "Smoke: x-built-with header in next.config.ts engineerHeaders (real check ok today)"
  key_links:
    - from: "package.json devDependencies"
      to: "@axe-core/playwright + playwright"
      pattern: '"@axe-core/playwright": "\\^4\\.11\\.3"'
    - from: "playwright.config.ts"
      to: "npm run dev"
      via: "webServer.command"
      pattern: 'command:\\s*"npm run dev"'
---

<objective>
Wave 0 foundation. Install devdeps, commit JetBrains Mono TTFs (with license), wire playwright config + test:contrast script, and scaffold every empty test/smoke-script file referenced by Waves 1–4. After this plan: `npm test` is still green (zero new red), `npm run test:contrast` runs (and is allowed to fail because the contrast spec body is a single placeholder pass), and Wave 1 can write production code into files whose verifications already exist as MISSING-flagged tests.

Purpose: prevent every later plan from carrying its own scaffolding load. By the end of Wave 0, every later task can `<verify>` against a file path that already exists.

Output: 4 devdeps in package.json + 1 new script + 2 font binaries + 1 license file + 5 test/spec scaffolds + 4 smoke-script scaffolds + 1 playwright config = 14 file changes, zero source-of-truth code yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-accessibility-polish/05-CONTEXT.md
@.planning/phases/05-seo-accessibility-polish/05-RESEARCH.md
@.planning/phases/05-seo-accessibility-polish/05-VALIDATION.md
@CLAUDE.md
@package.json
@vitest.config.ts
@scripts/check-print-rules.mjs

<interfaces>
<!-- Existing devdep + script shape this plan extends. -->

From package.json (existing):
```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "postbuild": "node scripts/check-placeholders.mjs",
  "start": "next start",
  "lint": "eslint .",
  "test": "vitest run",
  "typecheck": "tsc --noEmit",
  "knip": "knip",
  "check:mobile": "node scripts/check-sidebar-redistribution.mjs && node scripts/check-print-rules.mjs && node scripts/check-mobile-palette-css.mjs"
}
```

From scripts/check-print-rules.mjs (analog for new smoke scripts):
```js
import { readFileSync } from "node:fs";
const CSS_FILE = "app/globals.css";
const contents = readFileSync(CSS_FILE, "utf8");
// ...checks array...failures filter...exit 0 or 1...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install devdeps + commit JetBrains Mono TTFs + license</name>
  <read_first>
    - package.json (existing scripts + dependencies + devDependencies)
    - .planning/phases/05-seo-accessibility-polish/05-RESEARCH.md §Standard Stack §Supporting (versions verified 2026-05-10)
    - .planning/phases/05-seo-accessibility-polish/05-RESEARCH.md §Common Pitfalls Pitfall 4 (next/font/google does NOT expose binary path; commit TTFs to assets/ NOT public/)
    - CLAUDE.md §"Stack constraints" (no new prod deps; devdeps only)
  </read_first>
  <files>
    package.json,
    assets/JetBrainsMono-Bold.ttf,
    assets/JetBrainsMono-Medium.ttf,
    assets/JETBRAINS-MONO-LICENSE.txt
  </files>
  <action>
    Step A — install devdeps (one command):
    `npm install --save-dev @axe-core/playwright@^4.11.3 playwright@^1.59.1`
    This adds two entries to package.json `devDependencies`. The verified-stable versions (per RESEARCH §Standard Stack §Supporting, npm registry 2026-05-10) MUST be the carets `^4.11.3` and `^1.59.1` — do NOT pin exact, do NOT bump.

    Step B — install Chromium binary (one command, ~150MB one-time):
    `npx playwright install chromium`
    This populates `~/Library/Caches/ms-playwright/` (or platform equivalent). Required for `tests/contrast.spec.ts` execution in Wave 4. Skipped automatically on subsequent runs.

    Step C — fetch JetBrains Mono TTFs and commit to `assets/`:
    Create the `assets/` directory at repo root (NOT `public/` — committing to public/ would expose the font binaries on the public route surface; per the security threat model, OG font assets must NOT be reachable via `/JetBrainsMono-Bold.ttf` URL). Download the v2.304 release TTFs from the official GitHub release:
    `mkdir -p assets`
    `curl -sL https://github.com/JetBrains/JetBrainsMono/raw/v2.304/fonts/ttf/JetBrainsMono-Bold.ttf -o assets/JetBrainsMono-Bold.ttf`
    `curl -sL https://github.com/JetBrains/JetBrainsMono/raw/v2.304/fonts/ttf/JetBrainsMono-Medium.ttf -o assets/JetBrainsMono-Medium.ttf`
    Verify with `file assets/JetBrainsMono-Bold.ttf` (expect `TrueType Font data`) and `wc -c assets/JetBrainsMono-Bold.ttf` (expect >50000 bytes; <300000 bytes — sanity bounds; canonical Bold is ~190KB).

    Step D — add OFL-1.1 LICENSE file alongside TTFs:
    `curl -sL https://github.com/JetBrains/JetBrainsMono/raw/v2.304/OFL.txt -o assets/JETBRAINS-MONO-LICENSE.txt`
    Verify with `grep -q "SIL OPEN FONT LICENSE" assets/JETBRAINS-MONO-LICENSE.txt` (must return 0). This satisfies the OFL-1.1 redistribution clause.

    Step E — add test:contrast script to package.json `scripts`:
    Insert after `"check:mobile": "..."` line: `"test:contrast": "playwright test"`. Resulting scripts block has 9 entries.

    Step F — verify install:
    `npm ls @axe-core/playwright playwright` MUST resolve both packages without `UNMET PEER DEPENDENCY` warnings.

    No code changes elsewhere in this task.
  </action>
  <verify>
    <automated>
    test -f assets/JetBrainsMono-Bold.ttf &amp;&amp; test -f assets/JetBrainsMono-Medium.ttf &amp;&amp; test $(wc -c &lt; assets/JetBrainsMono-Bold.ttf) -gt 50000 &amp;&amp; test $(wc -c &lt; assets/JetBrainsMono-Medium.ttf) -gt 50000 &amp;&amp; grep -q "SIL OPEN FONT LICENSE" assets/JETBRAINS-MONO-LICENSE.txt &amp;&amp; node -e "const p=require('./package.json'); if(!p.devDependencies['@axe-core/playwright']) process.exit(1); if(!p.devDependencies.playwright) process.exit(1); if(p.scripts['test:contrast'] !== 'playwright test') process.exit(1); console.log('ok');"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f assets/JetBrainsMono-Bold.ttf` succeeds
    - `test -f assets/JetBrainsMono-Medium.ttf` succeeds
    - `wc -c < assets/JetBrainsMono-Bold.ttf` returns > 50000
    - `grep -q "SIL OPEN FONT LICENSE" assets/JETBRAINS-MONO-LICENSE.txt` returns 0
    - `node -e "console.log(require('./package.json').devDependencies['@axe-core/playwright'])"` outputs `^4.11.3`
    - `node -e "console.log(require('./package.json').devDependencies.playwright)"` outputs `^1.59.1`
    - `node -e "console.log(require('./package.json').scripts['test:contrast'])"` outputs `playwright test`
    - `assets/` directory exists and is NOT inside `public/` (per security gate — font assets must not be on public route surface)
  </acceptance_criteria>
  <done>devdeps installed, Chromium fetched, font binaries + license committed to assets/, test:contrast script wired in package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Create playwright.config.ts + tests/contrast.spec.ts scaffold</name>
  <read_first>
    - vitest.config.ts (existing config-file shape — defineConfig default export; same shape applies to playwright.config.ts)
    - .planning/phases/05-seo-accessibility-polish/05-RESEARCH.md §Architecture Patterns Pattern 7 (full playwright.config.ts + contrast.spec.ts examples)
    - .planning/phases/05-seo-accessibility-polish/05-PATTERNS.md §"tests/contrast.spec.ts + playwright.config.ts (NEW — first Playwright in repo)"
    - lib/routes.ts (ROUTES export — single source of truth iterated by the spec)
  </read_first>
  <files>playwright.config.ts, tests/contrast.spec.ts</files>
  <action>
    Create `playwright.config.ts` at repo root verbatim from RESEARCH Pattern 7:

    ```ts
    import { defineConfig } from "@playwright/test";

    export default defineConfig({
      testDir: "./tests",
      fullyParallel: false,
      forbidOnly: !!process.env.CI,
      retries: process.env.CI ? 1 : 0,
      workers: process.env.CI ? 2 : 1,
      use: {
        baseURL: "http://localhost:3000",
      },
      webServer: {
        command: "npm run dev",
        url: "http://localhost:3000",
        reuseExistingServer: !process.env.CI,
        timeout: 60_000,
      },
    });
    ```

    Create `tests/contrast.spec.ts` as a SCAFFOLD — one passing test that proves the runtime wiring works without yet executing the matrix:

    ```ts
    // tests/contrast.spec.ts
    // SCAFFOLD: Wave 0 stub. Real 4-hue x 2-theme x 7-route matrix lands in Plan 05-07
    // (per RESEARCH Pattern 7). This stub keeps `npm run test:contrast` green so
    // package.json wiring is verified end-to-end before Wave 1 implementation begins.

    import { test, expect } from "@playwright/test";

    test("scaffold: dev server boots and serves homepage HTTP 200", async ({ page }) => {
      const response = await page.goto("/");
      expect(response?.status()).toBe(200);
    });
    ```

    The scaffold MUST succeed against `npm run dev`; `playwright.config.ts` `webServer.command` boots dev. Do NOT add real AxeBuilder iteration here — that's Plan 05-07's job (after all visual changes have shipped, per CONTEXT.md D-35 wave ordering).

    Add to `.gitignore` (append, do NOT replace existing entries) at the end:
    ```
    # Playwright
    /test-results/
    /playwright-report/
    /playwright/.cache/
    ```

    Verify the scaffold runs end-to-end:
    `npm run test:contrast` should exit 0 (one passing test). If it errors with "Browser not found", `npx playwright install chromium` was skipped in Task 1 — re-run it.
  </action>
  <verify>
    <automated>
    test -f playwright.config.ts &amp;&amp; test -f tests/contrast.spec.ts &amp;&amp; grep -q 'webServer' playwright.config.ts &amp;&amp; grep -q 'npm run dev' playwright.config.ts &amp;&amp; grep -q 'baseURL.*localhost:3000' playwright.config.ts &amp;&amp; grep -q "@playwright/test" tests/contrast.spec.ts &amp;&amp; grep -q "scaffold" tests/contrast.spec.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f playwright.config.ts` succeeds
    - `test -f tests/contrast.spec.ts` succeeds
    - `grep -q 'webServer' playwright.config.ts` succeeds
    - `grep -q 'reuseExistingServer' playwright.config.ts` succeeds
    - `grep -q '@playwright/test' tests/contrast.spec.ts` succeeds
    - `npm run test:contrast` exits 0 (one scaffold test passes against `npm run dev`)
    - `.gitignore` contains `/test-results/`
  </acceptance_criteria>
  <done>Playwright runtime config + contrast spec scaffold present and green; Wave 4 can extend tests/contrast.spec.ts in place.</done>
</task>

<task type="auto">
  <name>Task 3: Create 5 Vitest test scaffolds + 4 smoke-script scaffolds</name>
  <read_first>
    - app/components/shell/accent-bootstrap-script.test.tsx (analog for json-ld-person.test.tsx — script-emitting RSC test pattern)
    - app/components/shell/live-clock.test.tsx (analog for console-signature.test.tsx — client island test pattern)
    - app/sitemap.test.tsx (analog for lib/json-ld.test.ts — pure-function test of RSC default export)
    - scripts/check-print-rules.mjs (analog for all 4 new smoke scripts — readFileSync + grep + exit 0/1)
    - scripts/check-placeholders.mjs (analog for the build-output-scan style; check-headers.mjs uses this style)
    - vitest.config.ts (existing test runner config; jsdom)
  </read_first>
  <files>
    app/components/shell/json-ld-person.test.tsx,
    app/components/shell/console-signature.test.tsx,
    lib/json-ld.test.ts,
    app/layout.test.tsx,
    scripts/check-og-files.mjs,
    scripts/check-reduced-motion.mjs,
    scripts/check-head-comment.mjs,
    scripts/check-headers.mjs
  </files>
  <action>
    Create 9 scaffold files — each a single passing assertion that proves the file is wired into Vitest / npm scripts WITHOUT depending on Wave 1+ source code that does not yet exist. Later plans extend in place.

    A) `app/components/shell/json-ld-person.test.tsx`:
    ```tsx
    import { describe, test, expect } from "vitest";
    // SCAFFOLD: Wave 0 stub. Real assertions land in Plan 05-05 (depends on
    // app/components/shell/json-ld-person.tsx). For now, prove the file is wired into
    // vitest discovery so Plan 05-05 can extend in place.
    describe("JsonLdPerson (scaffold)", () => {
      test("scaffold sentinel — real tests added in Plan 05-05", () => {
        expect(true).toBe(true);
      });
    });
    ```

    B) `app/components/shell/console-signature.test.tsx`:
    ```tsx
    import { describe, test, expect } from "vitest";
    // SCAFFOLD: Wave 0 stub. Real assertions land in Plan 05-06.
    describe("ConsoleSignature (scaffold)", () => {
      test("scaffold sentinel — real tests added in Plan 05-06", () => {
        expect(true).toBe(true);
      });
    });
    ```

    C) `lib/json-ld.test.ts`:
    ```ts
    import { describe, test, expect } from "vitest";
    // SCAFFOLD: Wave 0 stub. Real assertions land in Plan 05-05 (depends on lib/json-ld.ts).
    describe("lib/json-ld (scaffold)", () => {
      test("scaffold sentinel — real tests added in Plan 05-05", () => {
        expect(true).toBe(true);
      });
    });
    ```

    D) `app/layout.test.tsx`:
    ```tsx
    import { describe, test, expect } from "vitest";
    import { metadata } from "./layout";
    // SCAFFOLD: Wave 0 stub. Real twitter / viewport.themeColor assertions land in Plan 05-03.
    // The metadata import doubles as a smoke check that app/layout.tsx still parses.
    describe("RootLayout metadata (scaffold)", () => {
      test("scaffold sentinel — real tests added in Plan 05-03", () => {
        expect(metadata).toBeDefined();
      });
    });
    ```

    E) `scripts/check-og-files.mjs`:
    ```mjs
    #!/usr/bin/env node
    // scripts/check-og-files.mjs
    // SEO-03 smoke: 8 OG files exist (root + 7 routes). Real check ships in this scaffold —
    // Plan 05-02 creates the OG files; running this script before then SHOULD fail (as designed).
    import { existsSync } from "node:fs";
    const REQUIRED = [
      "app/opengraph-image.tsx",
      "app/(terminal)/opengraph-image.tsx",
      "app/(terminal)/projects/opengraph-image.tsx",
      "app/(terminal)/stack/opengraph-image.tsx",
      "app/(terminal)/experience/opengraph-image.tsx",
      "app/(terminal)/writing/opengraph-image.tsx",
      "app/(terminal)/contact/opengraph-image.tsx",
      "app/(terminal)/shipped/opengraph-image.tsx"
    ];
    const missing = REQUIRED.filter((p) => !existsSync(p));
    if (missing.length > 0) {
      console.error("✗ SEO-03: missing OG files:");
      for (const p of missing) console.error(`  FAIL: ${p}`);
      process.exit(1);
    }
    console.log(`✓ SEO-03: all ${REQUIRED.length} OG files present`);
    process.exit(0);
    ```

    F) `scripts/check-reduced-motion.mjs`:
    ```mjs
    #!/usr/bin/env node
    // scripts/check-reduced-motion.mjs
    // A11Y-03 smoke: app/globals.css has the universal-selector reduced-motion reset
    // inside the existing @media (prefers-reduced-motion: reduce) block.
    // Plan 05-03 ships the reset; running this script before then SHOULD fail (as designed).
    import { readFileSync } from "node:fs";
    const CSS_FILE = "app/globals.css";
    const contents = readFileSync(CSS_FILE, "utf8");
    const blockMatch = contents.match(/@media\s*\(prefers-reduced-motion:\s*reduce\)\s*\{[\s\S]*?\n\}/);
    const block = blockMatch ? blockMatch[0] : "";
    const checks = [
      { pattern: /@media\s*\(prefers-reduced-motion:\s*reduce\)/, label: "@media (prefers-reduced-motion: reduce) block present", scope: "file" },
      { pattern: /\*,\s*\*::before,\s*\*::after/, label: "universal selector *, *::before, *::after present", scope: "block" },
      { pattern: /animation-duration:\s*0\.01ms\s*!important/, label: "animation-duration: 0.01ms !important present (NOT 0ms — Pitfall 7)", scope: "block" },
      { pattern: /animation-iteration-count:\s*1\s*!important/, label: "animation-iteration-count: 1 !important present", scope: "block" },
      { pattern: /transition-duration:\s*0\.01ms\s*!important/, label: "transition-duration: 0.01ms !important present", scope: "block" }
    ];
    const failures = checks.filter(({ pattern, scope }) => {
      const target = scope === "block" ? block : contents;
      return !pattern.test(target);
    });
    if (failures.length > 0) {
      console.error("✗ A11Y-03: reduced-motion audit failed:");
      for (const { label } of failures) console.error(`  FAIL: ${label}`);
      process.exit(1);
    }
    console.log("✓ A11Y-03: reduced-motion universal-selector reset present");
    process.exit(0);
    ```

    G) `scripts/check-head-comment.mjs`:
    ```mjs
    #!/usr/bin/env node
    // scripts/check-head-comment.mjs
    // DEV-02 smoke: 6-line lowercase HTML comment is emitted by the head-comment RSC.
    // We grep the source TSX for the canonical opening line because the rendered output
    // depends on a running dev server. Plan 05-05 ships the comment; running this
    // script before then SHOULD fail (as designed).
    import { existsSync, readFileSync } from "node:fs";
    const candidates = [
      "app/components/shell/head-comment.tsx",
      "app/layout.tsx"
    ];
    const present = candidates.find((p) => existsSync(p));
    if (!present) {
      console.error("✗ DEV-02: head-comment source file not found");
      process.exit(1);
    }
    const contents = readFileSync(present, "utf8");
    const checks = [
      { pattern: /hello, you found the source/, label: "line 1: 'hello, you found the source'" },
      { pattern: /reach: beckprograms@gmail\.com/, label: "line 4: 'reach: beckprograms@gmail.com'" },
      { pattern: /github: beckinfonet/, label: "line 5: 'github: beckinfonet'" },
      { pattern: /thanks for looking\. — bakytbek/, label: "line 6: 'thanks for looking. — bakytbek'" }
    ];
    const failures = checks.filter(({ pattern }) => !pattern.test(contents));
    if (failures.length > 0) {
      console.error("✗ DEV-02: head-comment audit failed:");
      for (const { label } of failures) console.error(`  FAIL: ${label}`);
      process.exit(1);
    }
    console.log("✓ DEV-02: head-comment 6-line greeting present");
    process.exit(0);
    ```

    H) `scripts/check-headers.mjs`:
    ```mjs
    #!/usr/bin/env node
    // scripts/check-headers.mjs
    // DEV-03 smoke: next.config.ts engineerHeaders array contains x-built-with: nextjs-15-react-19.
    // No running server required — we parse the source. Already shipped in Phase 1; this
    // script verifies the slot still exists on every Phase 5 build.
    import { readFileSync } from "node:fs";
    const CONFIG = readFileSync("next.config.ts", "utf8");
    const checks = [
      { pattern: /engineerHeaders/, label: "engineerHeaders array declared" },
      { pattern: /x-built-with/, label: "x-built-with header key present" },
      { pattern: /nextjs-15-react-19/, label: "x-built-with value 'nextjs-15-react-19' present" }
    ];
    const failures = checks.filter(({ pattern }) => !pattern.test(CONFIG));
    if (failures.length > 0) {
      console.error("✗ DEV-03: header audit failed:");
      for (const { label } of failures) console.error(`  FAIL: ${label}`);
      process.exit(1);
    }
    console.log("✓ DEV-03: x-built-with header slot intact in next.config.ts");
    process.exit(0);
    ```

    Make all 4 scripts executable: `chmod +x scripts/check-og-files.mjs scripts/check-reduced-motion.mjs scripts/check-head-comment.mjs scripts/check-headers.mjs`.

    The headers smoke (H) is the only one that passes today — `next.config.ts` already has `x-built-with`. The other three intentionally fail-loud until their Wave 1+ source ships.

    Run `npm test` to confirm scaffolds are discovered: 4 new test files, all green. Run `node scripts/check-headers.mjs` — exits 0. Other smoke scripts MUST exit 1 with a clear FAIL message (the script harness is correct; the missing source is by design at Wave 0).

    No `TODO` (uppercase) string anywhere in any new file (RESEARCH Pitfall 9 — would fail INFRA-05 postbuild grep when applied; build won't run yet but the discipline ships now). Use lowercase `todo` in prose if needed.
  </action>
  <verify>
    <automated>
    test -f app/components/shell/json-ld-person.test.tsx &amp;&amp; test -f app/components/shell/console-signature.test.tsx &amp;&amp; test -f lib/json-ld.test.ts &amp;&amp; test -f app/layout.test.tsx &amp;&amp; test -x scripts/check-og-files.mjs &amp;&amp; test -x scripts/check-reduced-motion.mjs &amp;&amp; test -x scripts/check-head-comment.mjs &amp;&amp; test -x scripts/check-headers.mjs &amp;&amp; node scripts/check-headers.mjs &amp;&amp; ! grep -RE "\\bTODO\\b" scripts/check-og-files.mjs scripts/check-reduced-motion.mjs scripts/check-head-comment.mjs scripts/check-headers.mjs app/components/shell/json-ld-person.test.tsx app/components/shell/console-signature.test.tsx lib/json-ld.test.ts app/layout.test.tsx tests/contrast.spec.ts &amp;&amp; npm test --silent
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 .test.tsx/.test.ts scaffold files exist
    - All 4 .mjs smoke scripts exist and are executable (`test -x`)
    - `node scripts/check-headers.mjs` exits 0 (Phase 1 already shipped the header)
    - `node scripts/check-og-files.mjs` exits 1 with `FAIL: app/opengraph-image.tsx` (by design — Wave 1 will ship)
    - `node scripts/check-reduced-motion.mjs` exits 1 with FAIL on universal selector (by design — Wave 1 will ship)
    - `node scripts/check-head-comment.mjs` exits 1 with FAIL (by design — Wave 2 will ship)
    - `npm test` exits 0; test-file count includes the 4 new scaffolds
    - `! grep -RE "\bTODO\b" scripts/check-*.mjs app/components/shell/*.test.tsx lib/json-ld.test.ts app/layout.test.tsx tests/contrast.spec.ts` returns 0 (no uppercase TODO anywhere)
  </acceptance_criteria>
  <done>9 scaffold files in place; npm test green; smoke scripts fail-loud on missing sources by design (one passes today: check-headers.mjs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public route surface vs. repo asset directory | Files committed to `public/` are reachable at `https://example.com/<filename>`. Files committed to `assets/` are read by build-time RSC code (`opengraph-image.tsx` `readFile(join(process.cwd(), "assets/..."))`) but NOT served. Confused-deputy risk if planner accidentally writes TTFs into `public/`. |
| Devdep install (npm registry) | Untrusted npm publishes for `@axe-core/playwright` and `playwright` — but both are widely used, MIT-licensed, downloaded billions of times. Acceptable risk; mitigated by exact version pinning + `npm audit` gate (Phase 7 DEPLOY-05). |
| Chromium binary fetch (Playwright CDN) | One-time ~150MB binary fetch from Microsoft-hosted Playwright CDN. Acceptable; Playwright signs binaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | JetBrains Mono TTFs | mitigate | Commit to `assets/` not `public/`; `<files>` field forbids `public/JetBrainsMono*`; `<acceptance_criteria>` validates `assets/` location. RSC `readFile(join(process.cwd(), "assets/..."))` reads at build time only — binary never appears in `.next/static/` route surface. |
| T-05-02 | Tampering | npm devdep supply chain | accept | `^4.11.3` and `^1.59.1` carets allow patch bumps; `package-lock.json` records resolved hashes; `npm audit` runs in CI per INFRA-03 + DEPLOY-05. Out-of-scope: SBOM / sigstore — Phase 7+ candidate. |
| T-05-03 | Tampering | Inline-script CSP class for downstream JSON-LD | accept | Per CONTEXT.md D-15 deferred; Phase 1 deferral remains in effect. Wave 0 introduces zero inline scripts; this entry documents continuity for Plan 05-05's JSON-LD work. |
| T-05-04 | Repudiation | Postbuild placeholder grep coverage | mitigate | Acceptance criteria explicitly checks `! grep -RE "\\bTODO\\b"` across all new files. Lowercase `todo` in prose is permitted by `scripts/check-placeholders.mjs:25` (case-sensitive `/TODO/`). |
</threat_model>

<verification>
- `npm test` exits 0 (all existing + 4 new scaffold tests pass)
- `npm run test:contrast` exits 0 (one scaffold test passes against `npm run dev`)
- `node scripts/check-headers.mjs` exits 0 (Phase 1 header still in place)
- `npm ls @axe-core/playwright playwright` resolves both, no peer-dep warnings under React 19 / Next 15.5
- `wc -c < assets/JetBrainsMono-Bold.ttf` returns > 50000 (font binary committed)
- `grep -q "SIL OPEN FONT LICENSE" assets/JETBRAINS-MONO-LICENSE.txt` returns 0 (OFL-1.1 redistribution clause satisfied)
- `! grep -RE "\bTODO\b" scripts/check-*.mjs app/components/shell/json-ld-person.test.tsx app/components/shell/console-signature.test.tsx lib/json-ld.test.ts app/layout.test.tsx tests/contrast.spec.ts` returns 0
</verification>

<success_criteria>
Wave 0 is complete when:
1. Two devdeps `@axe-core/playwright@^4.11.3` and `playwright@^1.59.1` resolve in `package.json` and `npm ls` shows them with no peer warnings
2. JetBrains Mono Bold + Medium TTFs (~150KB total) committed to `assets/` (not `public/`); `assets/JETBRAINS-MONO-LICENSE.txt` carries the OFL-1.1 text
3. `playwright.config.ts` boots `npm run dev` on `http://localhost:3000` with `reuseExistingServer` non-CI
4. `tests/contrast.spec.ts` exists with one passing scaffold test; `npm run test:contrast` exits 0
5. 4 Vitest scaffold test files exist and are discovered by `npm test`; `npm test` exits 0
6. 4 smoke scripts (`check-og-files.mjs`, `check-reduced-motion.mjs`, `check-head-comment.mjs`, `check-headers.mjs`) exist and are executable; `check-headers.mjs` exits 0; the other three exit 1 with their fail-loud message (by design)
7. No literal `TODO` (uppercase) in any new file
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-accessibility-polish/05-01-SUMMARY.md`
</output>
