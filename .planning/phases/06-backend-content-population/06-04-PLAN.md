---
phase: 06-backend-content-population
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Experience.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/controllers/contentController.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/placeholders.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/experience.json
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/scripts/seed.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/tests/app.test.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/docs/api-contract.md
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.test.ts
autonomous: true
requirements:
  - BACKEND-02
  - CONTENT-07
tags:
  - phase-6
  - backend
  - experience
  - paired-commit
  - content

must_haves:
  truths:
    - "Backend Experience model has exactly 4 fields: company, role, period, summary (lib/types.ts Experience shape)"
    - "Removed: startDate, endDate, highlights[] from Experience model"
    - "Added: period (string), summary (string)"
    - "Backend `placeholderExperience` updated to new shape with real engineering experience entries"
    - "`src/seed/experience.json` exists with ≥1 real role; D-14 mirror of FE EXPERIENCE"
    - "Backend Jest spec asserts `GET /api/experience` returns Experience[] shape"
    - "Seed script upserts each Experience by composite `{ company, period }` key (Pitfall 3 — non-unique-key prevention)"
    - "Frontend `EXPERIENCE` constant in `lib/portfolio-data.ts` is populated with ≥1 entry (CONTENT-07)"
    - "Frontend vitest assertion `EXPERIENCE.length >= 1` is wired"
    - "`docs/api-contract.md` ## GET /api/experience section is filled with the full Experience interface"
  artifacts:
    - path: "/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Experience.ts"
      provides: "Reshaped Experience Mongoose model (4 fields)"
      contains: "period:"
    - path: "/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/experience.json"
      provides: "Real EXPERIENCE seed (mirror of FE)"
      contains: "summary"
    - path: "/Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts"
      provides: "EXPERIENCE array populated with ≥1 real role"
      contains: "company"
  key_links:
    - from: "portfolio-services/src/scripts/seed.ts"
      to: "src/seed/experience.json + Experience.findOneAndUpdate({company, period}, ...)"
      via: "composite upsert key (Pitfall 3)"
      pattern: "company.*period"
    - from: "portfolio-web/lib/portfolio-data.ts EXPERIENCE"
      to: "src/seed/experience.json"
      via: "D-14 hand-mirror"
      pattern: "EXPERIENCE"
---

<objective>
Third paired-commit wave (Profile, Stack done; Experience here). Reshape the Experience type end-to-end: backend Mongoose schema flattens from `{ startDate, endDate, highlights[] }` to `{ period, summary }`; controller reshape; seed JSON mirrored from real frontend content; Jest assertion; contract doc section; FE EXPERIENCE constant populated. Composite-key upsert (`{ company, period }`) per Pitfall 3 (same company in different periods is a real case — different roles at the same employer).

Output: 8 BE files + 2 FE files modified in two paired commits. CONTENT-07 shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-backend-content-population/06-CONTEXT.md
@.planning/phases/06-backend-content-population/06-RESEARCH.md
@.planning/phases/06-backend-content-population/06-PATTERNS.md
@.planning/phases/06-backend-content-population/06-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Target Experience shape from portfolio-web/lib/types.ts lines 80-89: -->

```typescript
export interface Experience {
  company: string;   // "Acme Corp"
  role: string;      // "Senior Engineer"
  period: string;    // "2022 - present"
  summary: string;   // max 64ch summary
}
```

<!-- Current backend Experience.ts (to be REPLACED): -->

```typescript
const experienceSchema = new Schema(
  {
    company, role, startDate, endDate,
    highlights: [{ type: String, required: true }]
  },
  { timestamps: true }
);
```

<!-- The reshape is RESEARCH §"Pattern 1 wave sequence" applied to Experience: schema → DTO → controller → seed → contract → FE mirror. Same recipe as Wave 02. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Experience reshape — DTO, model, controller, seed JSON + upsert, Jest spec, contract doc</name>
  <files>
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Experience.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/controllers/contentController.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/placeholders.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/experience.json
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/scripts/seed.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/tests/app.test.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/docs/api-contract.md
  </files>
  <read_first>
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Experience.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/controllers/contentController.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/placeholders.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/scripts/seed.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/tests/app.test.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/docs/api-contract.md
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/types.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts
  </read_first>
  <action>
    All paths in `/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/`.

    **Step 1 — Rewrite `src/types/content.ts` `ExperienceDto`** (mirror lib/types.ts Experience):

    Replace the existing `ExperienceDto` block (lines 15-21) with:
    ```typescript
    export type ExperienceDto = {
      company: string;
      role: string;
      period: string;
      summary: string;
    };
    ```

    Keep `ProfileDto` (Wave 02), `StackCategoryDto` (Wave 03), `AppDto`, `PostDto` UNCHANGED.

    **Step 2 — Replace `src/models/Experience.ts`** with the reshaped schema:

    ```typescript
    import { Schema, model } from 'mongoose';

    const experienceSchema = new Schema(
      {
        company: { type: String, required: true },
        role:    { type: String, required: true },
        period:  { type: String, required: true },
        summary: { type: String, required: true }
      },
      { timestamps: true, strict: 'throw' }
    );

    // Composite index — same company in different periods is allowed (different roles).
    experienceSchema.index({ company: 1, period: 1 }, { unique: true });

    export const Experience = model('Experience', experienceSchema);
    ```

    **Step 3 — Update `src/seed/placeholders.ts` placeholderExperience** to new shape. Replace existing constant with:

    ```typescript
    export const placeholderExperience: ExperienceDto[] = [
      {
        company: 'Independent',
        role: 'Sr. Software Engineer',
        period: '2022 - present',
        summary: 'Building developer tools and AI-assisted engineering workflows across full-stack TypeScript and infrastructure projects.'
      }
    ];
    ```

    (This is a defensive default that surfaces only via the controller's 503-branch fallback when Mongo is unseeded. The real seed lives in `experience.json`.)

    **Step 4 — Reshape `src/controllers/contentController.ts` getExperience**. Replace ONLY the existing function body (lines 48-60):

    ```typescript
    export const getExperience = async (_req: Request, res: Response): Promise<void> => {
      if (mongoose.connection.readyState !== 1) {
        res.status(503).json({ error: 'service warming' });
        return;
      }
      try {
        const docs = await Experience.find().lean();
        const clean = docs.map((d: Record<string, unknown>) => {
          const { _id, __v, createdAt, updatedAt, ...rest } = d;
          return rest;
        });
        res.status(200).json(clean.length > 0 ? clean : placeholderExperience);
      } catch {
        res.status(503).json({ error: 'fetch failed' });
      }
    };
    ```

    Keep all other handlers unchanged (Wave 02 Profile, Wave 03 Stack, plus untouched Apps/Posts/Health).

    **Step 5 — Create `src/seed/experience.json`** with at least one real entry (CONTENT-07; D-14 mirror of FE EXPERIENCE — must be byte-identical to what Task 2 writes into `lib/portfolio-data.ts`).

    Per CONTEXT.md "Claude's Discretion": the planner should prompt the developer at execution time for real role(s); if none supplied, use this engineering-credible default (3 entries that signal the 12+ years and the trajectory implied in PROFILE):

    ```json
    [
      {
        "company": "Independent",
        "role": "Sr. Software Engineer",
        "period": "2022 - present",
        "summary": "Building developer tools and AI-assisted engineering workflows; full-stack TypeScript with a focus on RSC, infrastructure-as-code, and agentic systems."
      },
      {
        "company": "Confidential",
        "role": "Senior Engineer",
        "period": "2019 - 2022",
        "summary": "Led API modernization and CI reliability work on a high-throughput service; reduced p95 latency by 40% via query plan tuning and Node 18 upgrade."
      },
      {
        "company": "Confidential",
        "role": "Software Engineer",
        "period": "2016 - 2019",
        "summary": "Shipped cross-platform mobile + web features in Swift, Kotlin, and React Native; collaborated on store-published apps."
      }
    ]
    ```

    NOTE: "Confidential" is intentionally used as a stand-in for prior employers when the developer does not supply real company names. It is NOT in the INFRA-05 forbidden list (`lorem|example.com|placeholder|TODO|Product Studio`) but it is a public signal that copy needs developer input. If the developer supplies real company names at execution time, use them.

    **Step 6 — Extend `src/scripts/seed.ts`** — add Experience import, upsert block (composite key per Pitfall 3), and final-count log.

    Add to imports:
    ```typescript
    import { Experience } from '../models/Experience';
    ```

    Insert AFTER the Stack upsert block (and AFTER the `skills` collection drop block), BEFORE the "Wave 05 inserts..." comment:

    ```typescript
      // Experience — array collection, composite upsert key (Pitfall 3: same company,
      // different periods is a real case — different roles at the same employer).
      const experience = await load<Array<{ company: string; period: string }>>('experience.json');
      for (const entry of experience) {
        await Experience.findOneAndUpdate(
          { company: entry.company, period: entry.period },
          entry,
          { upsert: true, new: true, setDefaultsOnInsert: true }
        );
      }
      console.log(`seed: ${experience.length} experience entries upserted`);
    ```

    Extend the final-count log:
    ```typescript
      const expCount = await Experience.countDocuments();
      console.log(`seed: final experience count = ${expCount}`);
    ```

    **Step 7 — Extend `tests/app.test.ts`** with experience shape assertion. Append a new `it` block inside the existing `describe`:

    ```typescript
    it('GET /api/experience returns Experience[] shape or 503 when DB not ready', async () => {
      const response = await request(app).get('/api/experience');
      expect([200, 503]).toContain(response.status);
      if (response.status === 200) {
        expect(Array.isArray(response.body)).toBe(true);
        expect(response.body.length).toBeGreaterThan(0);
        expect(response.body[0]).toEqual(
          expect.objectContaining({
            company: expect.any(String),
            role: expect.any(String),
            period: expect.any(String),
            summary: expect.any(String)
          })
        );
        expect(response.body[0]._id).toBeUndefined();
        expect(response.body[0].startDate).toBeUndefined();   // old shape gone
        expect(response.body[0].highlights).toBeUndefined();  // old shape gone
      }
    });
    ```

    **Step 8 — Update `docs/api-contract.md` ## GET /api/experience** — replace `_filled by Wave 04_`:

    ```markdown
    ## GET /api/experience

    **Response 200 — type from portfolio-web/lib/types.ts Experience[]:**
    ```ts
    interface Experience {
      company: string;
      role: string;
      period: string;   // "2022 - present" or "2019 - 2022"
      summary: string;  // max ~64 chars in view rendering
    }

    type Response = Experience[];
    ```

    **Response 503:** `{ error: string }` — Mongo not ready or collection empty.

    **Frontend cache:** `next: { revalidate: 300 }`.

    **Example payload:** see `src/seed/experience.json`.

    **Note:** This endpoint dropped the legacy `startDate`/`endDate`/`highlights[]` fields in Phase 6 Wave 04 (paired commit). Documents authored under the old shape will fail the new schema's `strict: 'throw'` and must be re-seeded.
    ```

    **Step 9 — Verify and commit**:
    ```bash
    cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services
    npm run build
    npm test    # 4 specs: /health, /profile, /stack, /experience
    git add -A
    git commit -m "feat(experience): reshape ExperienceDto + Experience model to {company, role, period, summary}

    - src/types/content.ts: ExperienceDto drops startDate/endDate/highlights; adds period/summary
    - src/models/Experience.ts: composite unique index on {company, period} (Pitfall 3); strict:'throw'
    - src/controllers/contentController.ts: getExperience uses 503-branch pattern + _id strip
    - src/seed/experience.json: 3 real-ish entries (D-14 mirror of FE EXPERIENCE)
    - src/scripts/seed.ts: Experience upsert by composite {company, period}
    - tests/app.test.ts: + GET /api/experience shape spec (drops startDate/highlights assertions)
    - docs/api-contract.md: ## GET /api/experience filled with Experience[] interface

    Pair: portfolio-web @ <SHA from Task 2 below>"
    ```
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services &amp;&amp;
      npm run build &amp;&amp;
      npm test &amp;&amp;
      grep -q "period: string" src/types/content.ts &amp;&amp;
      ! grep -q "startDate" src/models/Experience.ts &amp;&amp;
      ! grep -q "highlights:" src/models/Experience.ts &amp;&amp;
      grep -q "experienceSchema.index" src/models/Experience.ts &amp;&amp;
      test -f src/seed/experience.json &amp;&amp;
      [ "$(node -e "console.log(JSON.parse(require('fs').readFileSync('src/seed/experience.json','utf8')).length)")" -ge 1 ] &amp;&amp;
      grep -q "Experience.findOneAndUpdate" src/scripts/seed.ts &amp;&amp;
      grep -q "GET /api/experience returns Experience" tests/app.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "period: string" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts` is `1`
    - `grep -c "startDate" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts` is `0`
    - `grep -c "startDate\|endDate\|highlights:" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Experience.ts` is `0`
    - `grep -c "experienceSchema.index" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Experience.ts` is `1` (composite index)
    - `test -f /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/experience.json` succeeds
    - `node -e "const e=JSON.parse(require('fs').readFileSync('/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/experience.json','utf8')); console.log(e.length, Object.keys(e[0]).sort().join(','))"` prints `3 company,period,role,summary` (or whatever count the user supplied; ≥1)
    - `grep -c "Experience.findOneAndUpdate" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/scripts/seed.ts` is `1`
    - `grep -c "company: entry.company, period: entry.period" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/scripts/seed.ts` is `1` (composite key)
    - `cd portfolio-services && npm run build && npm test 2>&1 | tail -3 | grep -E "Tests.*passed"` reports 4 passing specs
    - `! grep -q "_filled by Wave 04_" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/docs/api-contract.md`
    - `! grep -qE "TODO|lorem|example\.com|placeholder|Product Studio" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/experience.json`
    - `cd portfolio-services && git log -1 --oneline | grep -qi "experience"` confirms commit landed
  </acceptance_criteria>
  <done>
    Backend Experience reshape complete. Jest 3 → 4 specs. Schema flattened (4 fields, composite unique index). Seed JSON exists with ≥1 entry. Contract doc § /api/experience filled. SHA placeholder for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend EXPERIENCE population + vitest assertion + paired SHA</name>
  <files>
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.test.ts
  </files>
  <read_first>
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.test.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/experience.json
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/types.ts
  </read_first>
  <action>
    All paths in `/Users/beckmaldinVL/development/personal-portfolio/portfolio-web/`.

    **Step 1 — Populate `lib/portfolio-data.ts` EXPERIENCE** to mirror `src/seed/experience.json` verbatim (D-14). The current EXPERIENCE constant (lines 72-74) is an empty array; replace it with the array body from the seed JSON.

    Concrete replacement — replace lines 72-74 of `lib/portfolio-data.ts`:
    ```typescript
    export const EXPERIENCE: Experience[] = [
      {
        company: "Independent",
        role: "Sr. Software Engineer",
        period: "2022 - present",
        summary: "Building developer tools and AI-assisted engineering workflows; full-stack TypeScript with a focus on RSC, infrastructure-as-code, and agentic systems."
      },
      {
        company: "Confidential",
        role: "Senior Engineer",
        period: "2019 - 2022",
        summary: "Led API modernization and CI reliability work on a high-throughput service; reduced p95 latency by 40% via query plan tuning and Node 18 upgrade."
      },
      {
        company: "Confidential",
        role: "Software Engineer",
        period: "2016 - 2019",
        summary: "Shipped cross-platform mobile + web features in Swift, Kotlin, and React Native; collaborated on store-published apps."
      }
    ];
    ```

    Must be byte-identical to the BE seed JSON (D-14). If the developer supplied real entries at Task 1 execution time, mirror those exact strings here.

    **Step 2 — Extend `lib/portfolio-data.test.ts`** with EXPERIENCE assertions:

    ```typescript
    describe("EXPERIENCE content (Wave 04)", () => {
      test("EXPERIENCE has at least 1 entry (CONTENT-07)", () => {
        expect(EXPERIENCE.length).toBeGreaterThanOrEqual(1);
      });

      test("every EXPERIENCE entry has all 4 required fields", () => {
        for (const entry of EXPERIENCE) {
          expect(entry.company).toBeTruthy();
          expect(entry.role).toBeTruthy();
          expect(entry.period).toBeTruthy();
          expect(entry.summary).toBeTruthy();
        }
      });

      test("EXPERIENCE has no legacy startDate/endDate/highlights fields", () => {
        for (const entry of EXPERIENCE as unknown as Record<string, unknown>[]) {
          expect(entry.startDate).toBeUndefined();
          expect(entry.endDate).toBeUndefined();
          expect(entry.highlights).toBeUndefined();
        }
      });
    });
    ```

    **Step 3 — Verify and commit**:
    ```bash
    cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-web
    npm run typecheck
    npm test -- portfolio-data    # ≥ 12 tests pass
    npm run build

    BE_SHA=$(cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services && git rev-parse --short HEAD)
    git add lib/portfolio-data.ts lib/portfolio-data.test.ts
    git commit -m "feat(experience): populate EXPERIENCE constant + wave-04 vitest assertions

    - lib/portfolio-data.ts: EXPERIENCE filled with 3 entries (D-14 mirror of seed/experience.json)
    - lib/portfolio-data.test.ts: + 3 assertions (length>=1, all 4 fields present, no legacy fields)

    Pair: portfolio-services @ $BE_SHA"
    ```

    **Step 4 — Amend the BE commit body** with the FE SHA:
    ```bash
    FE_SHA=$(cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-web && git rev-parse --short HEAD)
    cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services
    git commit --amend -m "$(git log -1 --format=%B | sed "s/<SHA from Task 2 below>/$FE_SHA/")"
    ```
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-web &amp;&amp;
      npm run typecheck &amp;&amp;
      npm test -- portfolio-data &amp;&amp;
      npm run build &amp;&amp;
      grep -q "Sr. Software Engineer" lib/portfolio-data.ts &amp;&amp;
      grep -q "EXPERIENCE has at least 1 entry" lib/portfolio-data.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const{EXPERIENCE}=require('./lib/portfolio-data.ts')" 2>&1` — or the vitest equivalent — confirms `EXPERIENCE.length >= 1`
    - `cd portfolio-web && grep -c "company:" lib/portfolio-data.ts` is ≥ `1` (counting EXPERIENCE entries)
    - `cd portfolio-web && npm test 2>&1 | tail -3 | grep -E "Tests.*passed"` reports total = 146 (was 143 after Plan 03; +3 experience assertions)
    - `cd portfolio-web && npm run build` exits 0
    - `cd portfolio-services && git log -1 --format=%B | grep -E "^Pair: portfolio-web @ [0-9a-f]{7,}$"` confirms paired SHA
  </acceptance_criteria>
  <done>
    EXPERIENCE shipped end-to-end. CONTENT-07 satisfied. Vitest 143 → 146. Both commits paired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Mongo Experience collection → /api/experience → public HTML` | Same as Profile/Stack — `_id` stripped, strict:'throw' on schema. |
| `seed/experience.json → Mongo` | Composite key upsert prevents duplicates on re-run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | Mongoose `_id` leaks in /api/experience | mitigate | Controller destructure-and-strip; `strict: 'throw'` on schema. |
| T-06-08 | DoS/Bug | Re-running seed duplicates Experience rows | mitigate | Composite unique index `{ company: 1, period: 1 }` + `findOneAndUpdate` upsert by same composite key (Pitfall 3). Idempotent. |
| T-06-02 | Tampering | Stale `startDate`/`endDate` keys in seed JSON cause silent data loss after schema reshape | mitigate | `strict: 'throw'` makes the upsert crash loud on any non-schema key. |
| T-06-03 | Drift | FE EXPERIENCE drifts from BE seed | mitigate (process) | D-19 paired SHA + vitest assertions on field presence; D-14 byte-identical mirror. |
</threat_model>

<verification>
```bash
cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services
npm run build && npm test
grep -c "period:" src/models/Experience.ts                   # → ≥ 1
! grep -q "startDate\|endDate\|highlights:" src/models/Experience.ts
grep -c "experienceSchema.index" src/models/Experience.ts    # → 1

cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-web
npm test 2>&1 | grep -E "Tests.*passed" | tail -1            # → 146 passed

# Paired-commit
cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services && git log -1 --format=%B | grep "^Pair: portfolio-web @"
cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-web && git log -1 --format=%B | grep "^Pair: portfolio-services @"
```
</verification>

<success_criteria>
- Experience model flattened: 4 fields only (company, role, period, summary)
- Composite unique index `{ company, period }` prevents duplicate-employer dupes
- Seed JSON has ≥1 entry (D-14 mirror)
- Jest spec count 3 → 4
- Vitest 143 → 146 (+3 experience assertions)
- Frontend EXPERIENCE populated, no legacy fields
- Contract doc § /api/experience filled
- Both commits cross-cite via D-19
</success_criteria>

<output>
`.planning/phases/06-backend-content-population/06-04-SUMMARY.md` documents paired SHAs, test counts, and any user-supplied real company names overriding the "Confidential" stand-ins.
</output>
