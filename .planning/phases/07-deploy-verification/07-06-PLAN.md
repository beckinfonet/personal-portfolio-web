---
phase: 07-deploy-verification
plan: 06
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - .planning/phases/07-deploy-verification/07-VERIFICATION.md
  - .planning/phases/07-deploy-verification/audit-fe.txt
  - .planning/phases/07-deploy-verification/audit-be.txt
  - .planning/phases/07-deploy-verification/knip-fe.txt
autonomous: true
requirements:
  - DEPLOY-05
tags:
  - npm-audit
  - knip
  - dead-code
  - security
  - pre-close-out-gate
must_haves:
  truths:
    - "npm audit --omit=dev --audit-level=high exits 0 against the FE repo (post Plan 03 @vercel/analytics install)"
    - "npm audit --omit=dev --audit-level=high exits 0 against the BE sibling repo at ../portfolio-services/"
    - "npx knip exits 0 against the FE repo (zero unused files / exports / deps)"
    - "Raw audit + knip outputs captured as text files under .planning/phases/07-deploy-verification/ for evidence"
    - "07-VERIFICATION.md DEPLOY-05 section records command + exit code + finding count per gate"
    - "Any high/critical CVE surfaced is fixed in-place before recording PASS (no DEFERRED escape hatch)"
  artifacts:
    - path: ".planning/phases/07-deploy-verification/07-VERIFICATION.md"
      provides: "DEPLOY-05 section with FE audit + BE audit + FE knip results"
      contains: "DEPLOY-05"
    - path: ".planning/phases/07-deploy-verification/audit-fe.txt"
      provides: "Full npm audit output for FE (production deps only)"
    - path: ".planning/phases/07-deploy-verification/audit-be.txt"
      provides: "Full npm audit output for BE sibling repo (production deps only)"
    - path: ".planning/phases/07-deploy-verification/knip-fe.txt"
      provides: "Full knip output for FE"
  key_links:
    - from: "package.json + package-lock.json (FE, post Plan 03 @vercel/analytics install)"
      to: "npm audit gate"
      via: "npm audit reads installed deps from lockfile"
      pattern: "npm audit"
    - from: "Phase 6 BE deploy (sibling repo unchanged in Phase 7 per D-23)"
      to: "BE npm audit gate"
      via: "ssh-less local check via cd ../portfolio-services"
      pattern: "../portfolio-services"
---

<objective>
Run the two pre-close-out automated gates per D-21: `npm audit --omit=dev --audit-level=high` against both the FE repo and the BE sibling repo `../portfolio-services/`, and `npx knip` against the FE repo. Both gates must exit 0 (no high/critical vulnerabilities; zero unused files/exports/deps). Re-verify that Phase 6 commit 1d9a295 (next 15.5.15 → 15.5.18 Vercel CVE bump) held, and that Plan 03's `@vercel/analytics` install did not introduce new advisories. Capture raw command outputs as text files for evidence, and record the gate results in 07-VERIFICATION.md DEPLOY-05 section.

Purpose: DEPLOY-05 — `npm audit` zero high/critical and `npx knip` zero unused. Pre-close-out gates that run BEFORE recording final scores per D-21. Catches CVE drift since Phase 6 close-out and dead code introduced by Phase 7 deltas (`@vercel/analytics` import, `<Analytics />` mount).

Output: Three evidence text files (audit-fe.txt, audit-be.txt, knip-fe.txt) + 07-VERIFICATION.md DEPLOY-05 section with exit codes + finding counts per gate + any fix commits referenced inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-deploy-verification/07-CONTEXT.md
@.planning/phases/07-deploy-verification/07-RESEARCH.md
@.planning/phases/07-deploy-verification/07-VALIDATION.md

<canonical_refs>
- RESEARCH.md §Pattern 6 (lines 486-509) — npm audit recommended command + flags + Phase 6 precedent
- RESEARCH.md §Pattern 7 (lines 511-527) — knip behavior for Next.js App Router (auto-detects layout/page/route/sitemap/robots/manifest/icon/opengraph-image as entry points)
- RESEARCH.md §Pitfall 7 (lines 616-621) — default npm audit exit code blocks on noise; --audit-level=high filter is the gate
- CONTEXT.md D-21 — both gates exit 0 for FE; BE audit applies; knip is FE-only
</canonical_refs>

<interfaces>
<!-- The exact commands and their expected exit semantics. -->

FE npm audit (production deps only, gate at high+):
```bash
npm audit --omit=dev --audit-level=high
# Exit 0 = zero high/critical; pass
# Exit 1 = at least one high/critical found; FAIL — must fix in-place
```

BE npm audit (same flags):
```bash
(cd ../portfolio-services && npm audit --omit=dev --audit-level=high)
# Same exit semantics
```

FE knip:
```bash
npx knip
# Exit 0 = zero unused files / exports / deps
# Exit 1 = at least one issue found; FAIL — must fix in-place
```

Phase 6 baseline (from 1d9a295): next 15.5.15 → 15.5.18 cleared the Vercel May 2026 CVE. Plan 03 added @vercel/analytics@^2.0.1 (verified 2026-04-17 release per RESEARCH.md Standard Stack — no known advisories at time of research).

Knip auto-recognizes (no manual config needed):
- app/layout.tsx, app/(terminal)/layout.tsx, app/not-found.tsx
- 7 page.tsx files under app/(terminal)/
- 8 opengraph-image.tsx files (Phase 5)
- app/sitemap.ts, app/robots.ts, app/manifest.ts, app/icon.tsx, app/apple-icon.tsx
- *.test.tsx, *.test.ts (Vitest plugin)
- tests/contrast.spec.ts (Playwright plugin)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm audit gate on FE repo (--omit=dev --audit-level=high)</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Pattern 6 + §Pitfall 7
    - package.json + package-lock.json (post Plan 03 install — @vercel/analytics added)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-21 (FE both audit + knip; BE audit only)
  </read_first>
  <action>
    Run the gate command from the FE repo root (current working directory):

    `npm audit --omit=dev --audit-level=high > .planning/phases/07-deploy-verification/audit-fe.txt 2>&1; echo "EXIT_CODE=$?" >> .planning/phases/07-deploy-verification/audit-fe.txt`

    The append of `EXIT_CODE=<n>` makes the exit semantics auditable from the text file alone (in case the file is inspected later without re-running the command).

    Expected outcomes:
    - Exit 0: zero high/critical vulnerabilities in prod deps. Output likely says "found 0 vulnerabilities" or "found N moderate-severity vulnerabilities" (moderate doesn't fail at `--audit-level=high`).
    - Exit 1: at least one high or critical in prod deps. Output lists them.

    If exit 1: do NOT record PASS. Identify the problematic package(s) from the output. The fix is one of:
    1. `npm audit fix` (auto-patches if the dep has a patched semver-compatible version).
    2. Upgrade the offending dep manually (e.g., `npm install <package>@<patched-version>`).
    3. If the fix is in a transitive dep with no patched parent version, document and surface as a checkpoint:decision to the user (Phase 6's 1d9a295 precedent was a manual `next` bump).

    Re-run the gate after any fix and update audit-fe.txt with the new output. Commit the fix in a focused commit (`fix(07): bump <pkg> to clear high-CVE advisory (DEPLOY-05 remediation)`) and push to trigger redeploy.

    Do NOT proceed to Task 2 with a non-zero exit until the FE audit passes.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-deploy-verification/audit-fe.txt && grep -q '^EXIT_CODE=0$' .planning/phases/07-deploy-verification/audit-fe.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-deploy-verification/audit-fe.txt` exists.
    - File ends with line `EXIT_CODE=0` (gate passed).
    - File body contains the npm audit output (either "found 0 vulnerabilities" or "found N moderate-severity vulnerabilities" with no high/critical at the gate level).
    - If remediation was required: a fix commit exists in git log with subject `fix(07): bump ... DEPLOY-05 remediation`.
  </acceptance_criteria>
  <done>
    FE npm audit gate green. Output captured for evidence trail.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run npm audit gate on BE sibling repo</name>
  <read_first>
    - ../portfolio-services/package.json (BE deps — different from FE)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-21 (BE audit applies; D-23 confirms BE has no Phase 7 code touches)
  </read_first>
  <action>
    Run from the FE repo root (uses subshell to change directory):

    `(cd ../portfolio-services && npm audit --omit=dev --audit-level=high) > .planning/phases/07-deploy-verification/audit-be.txt 2>&1; echo "EXIT_CODE=$?" >> .planning/phases/07-deploy-verification/audit-be.txt`

    Same exit semantics as Task 1.

    If exit 1: the fix is applied in the BE repo (not the FE repo). Apply the fix there, commit there (`fix(07-paired): bump <pkg> in BE to clear advisory (DEPLOY-05 remediation)`), push. Per Phase 6 D-19 paired-commit rule, if the BE fix is a true vulnerability remediation it does NOT need a paired FE commit — only architectural changes that affect FE need pairing. Document the BE commit SHA in 07-VERIFICATION.md (and reference it in the SUMMARY).

    Re-run the gate after any BE fix and update audit-be.txt.

    Per D-23, no BE code changes are expected in Phase 7; if this gate surfaces a real CVE, the fix is a dep bump (not a feature change) and shouldn't disrupt Phase 7's BE-quiet posture.

    Do NOT proceed to Task 3 with a non-zero exit until the BE audit passes.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-deploy-verification/audit-be.txt && grep -q '^EXIT_CODE=0$' .planning/phases/07-deploy-verification/audit-be.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-deploy-verification/audit-be.txt` exists.
    - File ends with line `EXIT_CODE=0`.
    - File body contains the BE npm audit output.
    - Any remediation commit SHA (in the BE repo) is recorded in the scratch buffer for Task 5.
  </acceptance_criteria>
  <done>
    BE npm audit gate green. Output captured. D-22 backend smoke gate is re-run separately in Plan 09 close-out; this task only validates dependency posture.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run npx knip gate on FE repo</name>
  <read_first>
    - knip.json (current Phase 1 configuration — confirm no false-positive workarounds needed)
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Pattern 7 (knip Next.js App Router auto-detection)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-21 (FE only)
  </read_first>
  <action>
    Run from the FE repo root:

    `npx knip > .planning/phases/07-deploy-verification/knip-fe.txt 2>&1; echo "EXIT_CODE=$?" >> .planning/phases/07-deploy-verification/knip-fe.txt`

    Expected outcome (D-21 requires exit 0):
    - "Unused files (0)", "Unused dependencies (0)", "Unused devDependencies (0)", "Unused exports (0)", "Unused types (0)" → exit 0.

    If exit 1: read the knip output. Common Phase 7 candidates for true unused:
    - Stale imports (unlikely — Tasks 3+4 in Plan 03 added exactly two imports, each with a usage in the same file).
    - Orphan files from Phase 5/6 (Plan 04 of those phases may have left a scaffold; if so, deletion + recheck).
    - False positive on the new `<Analytics />` mount in app/layout.tsx — should NOT happen since Knip's Next.js plugin recognizes `<Analytics />` usage at the JSX level. If it surfaces, investigate before applying a `// knip-ignore` directive — usually it's a real leak.

    Fix strategy:
    - For each unused file/export, decide: delete it OR add an entry point declaration in knip.json (only if it's a true entry that Knip missed).
    - Commit the fix: `chore(07): remove unused <thing> per knip gate (DEPLOY-05 remediation)`.
    - Re-run knip; update knip-fe.txt.

    Do NOT add knip-ignore directives as a workaround for real dead code. Only use them when knip genuinely misclassifies an entry point.

    Do NOT proceed to Task 4 with a non-zero exit until knip passes.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-deploy-verification/knip-fe.txt && grep -q '^EXIT_CODE=0$' .planning/phases/07-deploy-verification/knip-fe.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-deploy-verification/knip-fe.txt` exists.
    - File ends with line `EXIT_CODE=0`.
    - File body contains the knip output (all "Unused X (0)" lines or equivalent success summary).
    - If remediation was required: fix commit(s) exist in git log.
  </acceptance_criteria>
  <done>
    FE knip gate green. Output captured.
  </done>
</task>

<task type="auto">
  <name>Task 4: Append DEPLOY-05 section to 07-VERIFICATION.md + commit evidence</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-VERIFICATION.md (existing; append DEPLOY-05 section)
    - Task 1/2/3 output files for exact finding counts and command outputs
  </read_first>
  <action>
    Append a `## DEPLOY-05 — npm audit + knip (pre-close-out gates)` section to 07-VERIFICATION.md.

    Section content:

    ```markdown
    ## DEPLOY-05 — npm audit + knip (pre-close-out gates)

    **Methodology:** D-21 — `npm audit --omit=dev --audit-level=high` (FE + BE) + `npx knip` (FE only). All three must exit 0 before recording final scores in subsequent sections.

    ### FE npm audit

    - Command: `npm audit --omit=dev --audit-level=high`
    - Date: YYYY-MM-DD
    - Exit code: 0
    - Result: <0 high/critical | N moderate-severity (below gate threshold)>
    - Evidence: `audit-fe.txt`
    - Remediation: <none | bumped <pkg> via commit <sha>>

    ### BE npm audit (../portfolio-services/)

    - Command: `(cd ../portfolio-services && npm audit --omit=dev --audit-level=high)`
    - Date: YYYY-MM-DD
    - Exit code: 0
    - Result: <0 high/critical | N moderate-severity (below gate threshold)>
    - Evidence: `audit-be.txt`
    - Remediation: <none | bumped <pkg> via BE commit <sha>>

    ### FE knip

    - Command: `npx knip`
    - Date: YYYY-MM-DD
    - Exit code: 0
    - Result: zero unused files / exports / deps
    - Evidence: `knip-fe.txt`
    - Remediation: <none | removed <thing> via commit <sha>>

    **DEPLOY-05 verdict: PASS** (3/3 gates exit 0; Phase 6 Vercel CVE bump 1d9a295 re-verified; Plan 03 `@vercel/analytics` install introduced no new advisories.)
    ```

    Fill placeholders with actual exit codes (all `0` since prior tasks gated on PASS), finding counts (extract from the text files via `grep -E 'vulnerabilities|Unused' .planning/phases/07-deploy-verification/audit-fe.txt | tail -5`), and any remediation commit SHAs.

    Commit:
    `git add .planning/phases/07-deploy-verification/07-VERIFICATION.md .planning/phases/07-deploy-verification/audit-fe.txt .planning/phases/07-deploy-verification/audit-be.txt .planning/phases/07-deploy-verification/knip-fe.txt`
    `git commit -m "docs(07): record DEPLOY-05 audit + knip gates — PASS"`
    `git push origin main`
  </action>
  <verify>
    <automated>grep -q '^## DEPLOY-05' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'FE npm audit' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'BE npm audit' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'FE knip' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'DEPLOY-05 verdict: PASS' .planning/phases/07-deploy-verification/07-VERIFICATION.md && git log -1 --pretty=%s | grep -q 'DEPLOY-05'</automated>
  </verify>
  <acceptance_criteria>
    - 07-VERIFICATION.md contains a populated DEPLOY-05 section with three sub-sections (FE audit, BE audit, FE knip).
    - Each sub-section records command + exit code + finding count + evidence file pointer.
    - Verdict line: `**DEPLOY-05 verdict: PASS**` with the noted re-verifications (Phase 6 next bump + Plan 03 dep).
    - Single commit lands 07-VERIFICATION.md + 3 evidence .txt files; pushed.
  </acceptance_criteria>
  <done>
    DEPLOY-05 evidence captured + committed. Two of Wave 3's three plans (04 and 06) verdict PASS; Plan 05 (GSC) closes when its manual checkpoints complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm audit external advisory DB | npm Inc. publishes the advisory database; `npm audit` queries it locally against installed deps. No PII; well-established gate. |
| knip configuration vs source tree | Knip enforces dead-code policy from `knip.json`. Misconfiguration → false positives/negatives. |
| Evidence text files | Plain-text command outputs; non-executable; no injection vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-22 | Information Disclosure | Vulnerability findings in audit-fe.txt / audit-be.txt | accept | The output lists CVE IDs + package names. Repo is private; no public exposure. If repo goes public, audit text files could be redacted but the same info is in package-lock.json already. |
| T-07-23 | Tampering | Skipping a gate to record false PASS | mitigate | Per-task verify command checks `EXIT_CODE=0` in the evidence file; a false PASS would require manually editing the .txt file AND the section verdict — caught in review. |
| T-07-24 | Repudiation | Gate result captured at one point in time | accept | npm advisory DB updates daily; a future scan may surface advisories that didn't exist at Phase 7 close-out. Document the run date in the section. v1.1 / future phases re-run on their own schedule. |
| T-07-25 | Elevation of Privilege | knip-ignore directive abuse | mitigate | Task 3 explicitly forbids knip-ignore as a workaround for real dead code. Only legitimate entry-point misclassification justifies a directive. |

ASVS L1: PASS. Static analysis gates only; no auth surface; no secrets; no PII.
</threat_model>

<verification>
- Task 1: audit-fe.txt exists with EXIT_CODE=0.
- Task 2: audit-be.txt exists with EXIT_CODE=0.
- Task 3: knip-fe.txt exists with EXIT_CODE=0.
- Task 4: 07-VERIFICATION.md DEPLOY-05 section populated; 4-file commit (3 evidence + 1 verification) lands on origin/main.
- Any remediation commits referenced inline by SHA.
</verification>

<success_criteria>
This plan succeeds when:
1. `npm audit --omit=dev --audit-level=high` exits 0 against FE repo.
2. Same command exits 0 against BE sibling repo `../portfolio-services/`.
3. `npx knip` exits 0 against FE repo.
4. Three evidence text files committed with EXIT_CODE=0 stamps.
5. 07-VERIFICATION.md DEPLOY-05 section reads "verdict: PASS".
</success_criteria>

<output>
After completion, create `.planning/phases/07-deploy-verification/07-06-SUMMARY.md` capturing:
- Three gate commands + exit codes + finding counts
- Any remediation commits (with SHAs and the dep bumped)
- Evidence file paths
- Commit SHA for the docs commit
- Re-verification notes: Phase 6 next 15.5.18 bump held; Plan 03 @vercel/analytics introduced no new advisories
</output>
