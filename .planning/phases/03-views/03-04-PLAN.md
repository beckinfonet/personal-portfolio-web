---
phase: 03-views
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - app/globals.css
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, VIEW-04, VIEW-05, VIEW-06, VIEW-07, VIEW-08]
tags: [css, view-styles, brownfield-cleanup, phase-3]

must_haves:
  truths:
    - "All Phase 3 view-body and primitive CSS classes exist in app/globals.css with the values specified in 03-UI-SPEC.md."
    - "Selectors are appended to the bottom of the file using the existing /* ──── … ──── */ section-divider pattern."
    - "The orphan .stub-body class is removed (brownfield cleanup — Wave 3 will remove its last consumer in the next plan)."
    - "No new CSS custom properties (tokens) are introduced — Phase 3 reuses Phase 2 tokens verbatim."
  artifacts:
    - path: "app/globals.css"
      provides: "All view + primitive CSS for Phase 3"
      contains: ".tech-chip|.kbd|.copy-button|.store-badge-link|.about-cards|.projects-row|.stack-pre|.experience-row|.writing-post|.contact-card|.shipped-row|.empty-state|.btn|.btn-ghost"
  key_links:
    - from: "app/globals.css new sections"
      to: "Phase 2 tokens (--bg, --panel, --border, --accent, --warn, --muted, …)"
      via: "every rule body uses var(--token)"
      pattern: "var\\(--"
---

<objective>
Append all Phase 3 view-body and primitive CSS to `app/globals.css` in one sequential plan so that Wave 3 (about-view) and Wave 4 (six parallel views) can render against pre-existing classes without any of those plans needing to mutate `app/globals.css`. Also delete the orphan `.stub-body` class (Wave 3+ removes its last consumers).

Purpose: Avoid same-wave file conflict on `app/globals.css`. By centralizing the CSS append here in Wave 2, the six parallel Wave 4 plans can each own their TSX/test files exclusively while consuming the already-shipped class names. This is the ONE plan that touches `app/globals.css`.

Output: `app/globals.css` extended with ~80 selectors organized into 9 section-divider blocks (5 primitive sections + 7 per-view sections + 1 shared empty-state section + 1 shared button section + brownfield deletion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-views/03-CONTEXT.md
@.planning/phases/03-views/03-PATTERNS.md
@.planning/phases/03-views/03-UI-SPEC.md

<interfaces>
<!-- Existing CSS section-divider style (verbatim from app/globals.css line 247) -->
```css
/* ─────────────────────────────────────────
   <Section name> (<requirement-id> — <plan-or-phase ref>)
   ───────────────────────────────────────── */
```

<!-- Existing tokens (Phase 2 — DO NOT add new ones) -->
Surfaces: --bg, --bg-raised, --panel, --panel-hi
Borders: --border, --border-hi
Text:    --text, --text-hi, --muted, --muted-hi
Accents: --accent, --accent-dim, --accent-bg
Semantic: --warn (used Phase 3); --red, --blue NOT used Phase 3

<!-- Existing classes Phase 3 reuses -->
.sr-only      (line 202 — already exists; CopyButton's live region uses it)
.content-block (line 230 — slideIn animation wrapper)
.terminal-main (line 224 — main content padding/max-width)

<!-- Orphan to delete -->
.stub-body (line 669–674) — Phase 2 placeholder; Phase 3 page rewrites in Wave 3 + Wave 4 remove its consumers.

<!-- Append site -->
End of file (currently line 718, after `.not-found-route-desc`). Append all new sections after the existing `.not-found-route-desc` block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append all Phase 3 view + primitive CSS sections to globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (full file — verify section-divider format, current EOF is line 718, identify the .stub-body lines 669–674 and the not-found section 676–718)
    - .planning/phases/03-views/03-UI-SPEC.md §"New Primitives" lines 196–351 (CSS contracts for tech-chip, kbd, copy-button, store-badge — verbatim values)
    - .planning/phases/03-views/03-UI-SPEC.md §"Per-View Visual Contracts" lines 354–696 (per-view class names + spacing + colors per view)
    - .planning/phases/03-views/03-UI-SPEC.md §"Empty-State Contract" lines 699–721 (.empty-state shared class)
    - .planning/phases/03-views/03-UI-SPEC.md §"Color" lines 113–193 (token-to-element map)
    - .planning/phases/03-views/03-PATTERNS.md §"app/globals.css extension" lines 666–724 (append-only convention + divider format)
  </read_first>
  <action>
    Edit `app/globals.css`. Two parts:

    **Part A — Delete the orphan `.stub-body` rule.**
    Locate the section starting at the comment block `/* ─── Route stub body placeholder (Phase 3 replaces these) ─── */` (around line 665) and delete BOTH the comment block AND the `.stub-body { ... }` rule (lines ~665–674). The Wave 3 about-view plan and Wave 4 view plans replace `<p className="stub-body">` with real view bodies in the same commits that consume them, so the rule has no surviving callers. Do NOT delete any other class.

    **Part B — Append ALL Phase 3 sections to the bottom of the file (after `.not-found-route-desc`).**
    Use the section-divider pattern verbatim. Append the following sections IN THIS ORDER. Use the values as written — sizes/colors/spacings come directly from UI-SPEC and are LOCKED.

    ```css

    /* ─────────────────────────────────────────
       Phase 3 — View primitives (VIEW-08, SEO-05)
       ───────────────────────────────────────── */

    .tech-chip {
      display: inline-flex;
      align-items: center;
      padding: 3px 8px;
      border: 1px solid var(--border);
      border-radius: 3px;
      font-size: 11px;
      color: var(--muted-hi);
      margin-right: 4px;
      margin-top: 4px;
      background: var(--bg-raised);
    }

    .kbd {
      display: inline-block;
      padding: 1px 5px;
      font-family: inherit;
      font-size: 10px;
      border: 1px solid var(--border-hi);
      border-radius: 3px;
      color: var(--muted-hi);
      background: var(--panel);
    }

    .copy-button {
      background: transparent;
      color: var(--muted);
      border: 1px solid var(--border);
      padding: 4px 10px;
      border-radius: 4px;
      font-family: inherit;
      font-size: 11px;
      cursor: pointer;
      flex-shrink: 0;
      white-space: nowrap;
      display: inline-flex;
      align-items: center;
      gap: 4px;
    }

    .copy-button:hover {
      border-color: var(--border-hi);
      color: var(--text);
    }

    .copy-button--confirmed {
      color: var(--accent);
    }

    .copy-button--icon {
      padding: 4px 6px;
    }

    .store-badge-link {
      display: inline-flex;
      align-items: center;
    }

    .store-badge-link img {
      display: block;
      /* DO NOT add filter/opacity rules — modifying official Apple/Google badge artwork
         violates Marketing Identity Guidelines / Brand Guidelines (Phase 3 / D-13). */
    }

    /* ─────────────────────────────────────────
       Phase 3 — Shared CTA buttons (.btn / .btn-ghost — VIEW-01, VIEW-06)
       ───────────────────────────────────────── */

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      background: var(--accent);
      color: var(--bg);
      border: 1px solid var(--accent);
      border-radius: 4px;
      padding: 8px 14px;
      font-family: inherit;
      font-size: 13px;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      transition: opacity 0.15s ease;
    }

    .btn:hover {
      opacity: 0.9;
      text-decoration: none;
    }

    .btn-ghost {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      background: transparent;
      color: var(--text);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 8px 14px;
      font-family: inherit;
      font-size: 13px;
      text-decoration: none;
    }

    .btn-ghost:hover {
      border-color: var(--border-hi);
      color: var(--text-hi);
      text-decoration: none;
    }

    /* ─────────────────────────────────────────
       Phase 3 — Shared empty-state (D-03 — VIEW-02, VIEW-04, VIEW-05, VIEW-07)
       ───────────────────────────────────────── */

    .empty-state {
      font-size: 13px;
      color: var(--muted);
      margin-top: 14px;
    }

    /* ─────────────────────────────────────────
       Phase 3 — about-view (VIEW-01)
       ───────────────────────────────────────── */

    .terminal-main h1 {
      font-size: 26px;
      font-weight: 700;
      letter-spacing: -0.01em;
      color: var(--text-hi);
      margin: 0 0 4px;
    }

    .about-role {
      font-size: 14px;
      color: var(--accent);
      margin-bottom: 4px;
    }

    .about-meta {
      font-size: 12px;
      color: var(--muted);
      margin-bottom: 18px;
    }

    .about-para {
      font-size: 14px;
      color: var(--text);
      line-height: 1.6;
      max-width: 68ch;
      margin: 12px 0;
    }

    .about-cards {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
      max-width: 480px;
      margin: 24px 0;
    }

    .about-card {
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 14px;
    }

    .about-card-value {
      font-size: 22px;
      font-weight: 700;
      color: var(--accent);
      line-height: 1.2;
    }

    .about-card-label {
      font-size: 11px;
      color: var(--muted);
      margin-top: 2px;
    }

    .about-cta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 24px;
    }

    /* ─────────────────────────────────────────
       Phase 3 — projects-view (VIEW-02)
       ───────────────────────────────────────── */

    .projects-subhead {
      font-size: 12px;
      color: var(--muted);
      margin-bottom: 14px;
    }

    .projects-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .projects-row {
      display: grid;
      grid-template-columns: 32px 1fr 110px;
      gap: 16px;
      padding: 16px 0;
      border-bottom: 1px solid var(--border);
      color: var(--text);
      text-decoration: none;
    }

    .projects-row:hover {
      text-decoration: none;
      background: var(--panel);
    }

    .projects-row-index {
      font-size: 12px;
      color: var(--muted);
    }

    .projects-row-name {
      font-size: 15px;
      font-weight: 600;
      color: var(--accent);
    }

    .projects-row-summary {
      font-size: 13px;
      color: var(--text);
      line-height: 1.5;
      margin-top: 4px;
    }

    .projects-row-chips {
      margin-top: 8px;
    }

    .projects-row-meta {
      text-align: right;
    }

    .projects-row-year {
      font-size: 11px;
      color: var(--muted-hi);
    }

    .projects-row-status {
      font-size: 11px;
      color: var(--warn);
      margin-top: 2px;
    }

    .projects-row-role {
      font-size: 11px;
      color: var(--muted);
      margin-top: 2px;
    }

    /* ─────────────────────────────────────────
       Phase 3 — stack-view (VIEW-03)
       ───────────────────────────────────────── */

    .stack-pre-wrap {
      position: relative;
    }

    .stack-pre {
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 24px 20px;
      font-family: inherit;
      font-size: 13px;
      line-height: 1.7;
      color: var(--text);
      margin: 0;
      overflow: auto;
    }

    .stack-copy {
      position: absolute;
      top: 12px;
      right: 12px;
    }

    .json-key {
      color: var(--warn);
    }

    .json-string {
      color: var(--accent);
    }

    .json-punc {
      color: var(--muted);
    }

    /* ─────────────────────────────────────────
       Phase 3 — experience-view (VIEW-04)
       ───────────────────────────────────────── */

    .experience-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .experience-row {
      padding: 16px 0;
      border-bottom: 1px solid var(--border);
    }

    .experience-row-header {
      display: flex;
      align-items: baseline;
      flex-wrap: wrap;
      gap: 12px;
    }

    .experience-hash {
      font-size: 13px;
      color: var(--warn);
    }

    .experience-role {
      font-size: 13px;
      font-weight: 600;
      color: var(--accent);
    }

    .experience-company {
      font-size: 13px;
      color: var(--muted);
    }

    .experience-period {
      font-size: 12px;
      color: var(--muted);
      margin-left: auto;
    }

    .experience-summary {
      font-size: 13px;
      color: var(--text);
      max-width: 64ch;
      margin-top: 6px;
    }

    /* ─────────────────────────────────────────
       Phase 3 — writing-view (VIEW-05)
       ───────────────────────────────────────── */

    .writing-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .writing-post {
      display: block;
      padding: 18px 0;
      border-bottom: 1px dashed var(--border);
      color: var(--text);
      text-decoration: none;
    }

    .writing-post:hover {
      text-decoration: none;
    }

    .writing-post-meta {
      font-size: 11px;
      color: var(--muted);
      letter-spacing: 0.05em;
    }

    .writing-post-title {
      font-size: 16px;
      font-weight: 600;
      color: var(--accent);
      margin-top: 6px;
    }

    .writing-post-excerpt {
      font-size: 13px;
      color: var(--text);
      max-width: 64ch;
      margin-top: 8px;
      line-height: 1.55;
    }

    /* ─────────────────────────────────────────
       Phase 3 — contact-view (VIEW-06)
       ───────────────────────────────────────── */

    .contact-lead {
      font-size: 14px;
      color: var(--text);
      max-width: 68ch;
      line-height: 1.6;
    }

    .contact-card {
      max-width: 480px;
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 20px;
      margin: 20px 0;
    }

    .contact-row {
      display: grid;
      grid-template-columns: 90px 1fr auto;
      gap: 12px;
      align-items: baseline;
      padding: 8px 0;
      border-bottom: 1px solid var(--border);
    }

    .contact-row:last-child {
      border-bottom: none;
    }

    .contact-label {
      font-size: 12px;
      color: var(--muted);
    }

    .contact-cta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 24px;
    }

    /* ─────────────────────────────────────────
       Phase 3 — shipped-view (VIEW-07)
       ───────────────────────────────────────── */

    .shipped-subhead {
      font-size: 12px;
      color: var(--muted);
      margin-bottom: 14px;
    }

    .shipped-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .shipped-row {
      display: grid;
      grid-template-columns: 32px 1fr 110px;
      gap: 16px;
      padding: 16px 0;
      border-bottom: 1px solid var(--border);
    }

    .shipped-row-index {
      font-size: 12px;
      color: var(--muted);
    }

    .shipped-row-name {
      font-size: 15px;
      font-weight: 600;
      color: var(--accent);
    }

    .shipped-row-summary {
      font-size: 13px;
      color: var(--text);
      line-height: 1.5;
      margin-top: 4px;
    }

    .shipped-row-affordances {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-top: 12px;
      flex-wrap: wrap;
    }

    .shipped-row-meta {
      text-align: right;
    }

    .shipped-row-year {
      font-size: 11px;
      color: var(--muted-hi);
    }

    .shipped-row-status {
      font-size: 11px;
      color: var(--warn);
      margin-top: 2px;
    }

    .shipped-row-role {
      font-size: 11px;
      color: var(--muted);
      margin-top: 2px;
    }
    ```

    Notes on the append:
    - All values come straight from UI-SPEC §"Per-View Visual Contracts" — no improvisation.
    - Every rule body uses `var(--token)` from the Phase 2 token set. Zero new tokens.
    - The `.btn` / `.btn-ghost` definitions are NEW in Phase 3 (handoff styles them as inline objects in `app.jsx`); they're shared between about-view and contact-view, so they live in their own divider block before the per-view sections.
    - `.empty-state` is shared by 4 views (projects, experience, writing, shipped) — single class block. Per-view variants like `.empty-state--writing` are NOT needed (UI-SPEC mentions but does not require — the base `.empty-state` covers all four locked strings).
    - Brownfield: `.stub-body` is deleted. The 7 page-stubs that consumed it will be rewritten in Wave 3 (about) and Wave 4 (six other views) — deletion is in this commit so all consumers are gone before any consumer-removal happens (defensive ordering: dead CSS class removed first, then consumers replaced; if Wave 3/4 plans fail mid-execution the worst case is a missing class, not a stale class with no callers).

    DO NOT introduce media queries (Phase 4 owns mobile breakpoints — UI-SPEC §"Mobile Scope Note").
    DO NOT introduce new tokens.
    DO NOT change any pre-existing class.
    DO NOT modify the section dividers above the new sections.
  </action>
  <verify>
    <automated>grep -c '\.tech-chip {' app/globals.css | grep -qx 1 &amp;&amp; grep -c '\.kbd {' app/globals.css | grep -qx 1 &amp;&amp; grep -c '\.copy-button {' app/globals.css | grep -qx 1 &amp;&amp; grep -c '\.store-badge-link' app/globals.css | grep -qE '^[12]$' &amp;&amp; grep -c '\.empty-state {' app/globals.css | grep -qx 1 &amp;&amp; grep -c '\.btn {' app/globals.css | grep -qx 1 &amp;&amp; grep -c '\.btn-ghost {' app/globals.css | grep -qx 1 &amp;&amp; grep -q '\.about-cards' app/globals.css &amp;&amp; grep -q '\.projects-row' app/globals.css &amp;&amp; grep -q '\.stack-pre' app/globals.css &amp;&amp; grep -q '\.experience-row' app/globals.css &amp;&amp; grep -q '\.writing-post' app/globals.css &amp;&amp; grep -q '\.contact-card' app/globals.css &amp;&amp; grep -q '\.shipped-row' app/globals.css &amp;&amp; grep -q '\.json-key' app/globals.css &amp;&amp; grep -q '\.json-string' app/globals.css &amp;&amp; grep -c '\.stub-body' app/globals.css | grep -qx 0 &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.tech-chip {' app/globals.css` returns `1`
    - `grep -c '\.kbd {' app/globals.css` returns `1`
    - `grep -c '\.copy-button {' app/globals.css` returns `1`
    - `grep -c '\.copy-button--confirmed' app/globals.css` returns `1`
    - `grep -c '\.copy-button--icon' app/globals.css` returns `1`
    - `grep -c '\.empty-state {' app/globals.css` returns `1`
    - `grep -c '\.btn {' app/globals.css` returns `1`
    - `grep -c '\.btn-ghost {' app/globals.css` returns `1`
    - `grep -q '\.about-cards' app/globals.css` succeeds
    - `grep -q '\.about-card {' app/globals.css` succeeds
    - `grep -q '\.about-card-value' app/globals.css` succeeds
    - `grep -q '\.projects-row' app/globals.css` succeeds
    - `grep -q '\.projects-row-name' app/globals.css` succeeds
    - `grep -q '\.stack-pre' app/globals.css` succeeds
    - `grep -q '\.stack-pre-wrap' app/globals.css` succeeds
    - `grep -q '\.json-key' app/globals.css` succeeds
    - `grep -q '\.json-string' app/globals.css` succeeds
    - `grep -q '\.json-punc' app/globals.css` succeeds
    - `grep -q '\.experience-row' app/globals.css` succeeds
    - `grep -q '\.experience-hash' app/globals.css` succeeds
    - `grep -q '\.writing-post' app/globals.css` succeeds
    - `grep -q '\.writing-post-meta' app/globals.css` succeeds
    - `grep -q 'border-bottom: 1px dashed var(--border)' app/globals.css` succeeds (handoff-specified dashed divider — VIEW-05)
    - `grep -q '\.contact-card' app/globals.css` succeeds
    - `grep -q '\.contact-row' app/globals.css` succeeds
    - `grep -q '\.shipped-row' app/globals.css` succeeds
    - `grep -q '\.shipped-row-affordances' app/globals.css` succeeds
    - `grep -c '\.stub-body' app/globals.css` returns `0` (orphan deleted)
    - No new `--` token introduced: `git diff app/globals.css | grep -E '^\+\s*--[a-z-]+:' | wc -l` returns `0`
    - `npm run lint` exits 0
    - `npm run build` exits 0 (the build still passes despite `.stub-body` deletion because the stubs in Wave 3/Wave 4 will replace the class before this plan ships — but since Wave 4 hasn't happened yet, you may temporarily see "missing class" in the runtime; CSS missing classes do NOT fail Next builds. The stubs still work at this point because their `<p className="stub-body">` content still renders, just unstyled. Wave 3/Wave 4 will replace those JSX nodes.)
  </acceptance_criteria>
  <done>
    `app/globals.css` carries every Phase 3 class with locked sizes/colors/spacings. No new tokens. The orphan `.stub-body` block is removed. Build, lint, knip pass. The unstyled stubs continue to function until Wave 3/Wave 4 replace them.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| globals.css → rendered DOM | static stylesheet; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering (CSS-driven brand-asset modification) | `.store-badge-link img` rules | mitigate | Comment-pinned in the rule block: "DO NOT add filter/opacity rules — modifying official Apple/Google badge artwork violates Marketing Identity Guidelines." Verifier-grep ensures no `filter:` or `opacity:` is set on `.store-badge-link img` in this plan or any future plan. |
| T-03-13 | Information Disclosure (token drift exposing internal state) | new selectors | accept | All values are visual styling; no token contains URLs, secrets, or internal state. |
| T-03-14 | Tampering (orphaned `.stub-body` removed before consumers replaced) | brownfield deletion ordering | accept | CSS missing classes do not fail Next builds. The temporary unstyled `<p className="stub-body">` is acceptable for the duration of Wave 3 + Wave 4 (a few executor cycles). The risk is purely visual and does not break the app. |
</threat_model>

<verification>
- `npm run lint` exits 0
- `npm run typecheck` exits 0
- `npm run build` exits 0
- `grep -c '\.stub-body' app/globals.css` returns `0`
- `git diff --stat app/globals.css` shows ~+170 / -10 line range
- The seven prior plans (01, 02, 03) plus the next plan (05 about-view) all pass when this plan lands
</verification>

<success_criteria>
- [ ] All 9 new section-divider blocks present (primitives, btn/btn-ghost, empty-state, about, projects, stack, experience, writing, contact, shipped)
- [ ] All ~80 new selectors use only Phase 2 tokens (`var(--…)`)
- [ ] `.stub-body` is gone
- [ ] No new CSS custom properties (`--newtoken`)
- [ ] Build/lint/typecheck green
</success_criteria>

<output>
After completion, create `.planning/phases/03-views/03-04-SUMMARY.md` per `summary.md` template — include the line count delta, the deleted `.stub-body` reference, and the verification commands.
</output>
