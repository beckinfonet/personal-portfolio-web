---
phase: 07-deploy-verification
plan: 09
type: execute
wave: 4
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
  - "07-05"
  - "07-06"
  - "07-07"
  - "07-08"
files_modified:
  - .planning/phases/07-deploy-verification/07-VERIFICATION.md
  - .planning/phases/07-deploy-verification/be-smoke-output.txt
  - .planning/phases/07-deploy-verification/check-prod-output.txt
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06
  - DEPLOY-07
tags:
  - close-out
  - backend-smoke-gate
  - requirements-update
  - phase-completion
must_haves:
  truths:
    - "Backend smoke gate (D-22 / Phase 6 D-11) re-run against Railway BE; exits 0 with 7/7 endpoints green"
    - "scripts/check-production-routes.mjs re-run against https://www.tatibekov.com; exits 0 with 7/7 routes green"
    - "All 7 DEPLOY-* requirements flipped to [x] in REQUIREMENTS.md"
    - "REQUIREMENTS.md traceability table updated: DEPLOY-01..07 status = Complete (07-NN reference)"
    - "PROJECT.md Decisions section captures phase outcomes (third prod dep allowlisted, x-portfolio-source dropped, real-device limitations accepted)"
    - "ROADMAP.md Phase 7 status flipped to [x] with completion date"
    - "STATE.md updated to reflect Phase 7 complete + zero remaining v1 phases"
    - "07-VERIFICATION.md final sign-off section records phase verdict PASS with all 7 DEPLOY IDs green + carry-forwards closed"
  artifacts:
    - path: ".planning/phases/07-deploy-verification/07-VERIFICATION.md"
      provides: "Final close-out section with phase verdict + cross-section summary + carry-forward audit trail"
      contains: "Phase 7 verdict"
    - path: ".planning/phases/07-deploy-verification/be-smoke-output.txt"
      provides: "Phase 7 close-out re-run of scripts/check-backend.mjs evidence (D-22)"
    - path: ".planning/phases/07-deploy-verification/check-prod-output.txt"
      provides: "Phase 7 close-out re-run of scripts/check-production-routes.mjs evidence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated v1 requirements list with DEPLOY-01..07 marked [x]"
      contains: "[x] **DEPLOY-07**"
    - path: ".planning/PROJECT.md"
      provides: "Decisions section appended with Phase 7 outcomes"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 row flipped to Complete with date"
      contains: "[x] **Phase 7"
    - path: ".planning/STATE.md"
      provides: "Current position reflects Phase 7 complete + v1 milestone done"
  key_links:
    - from: "Phase 6 D-22 backend smoke gate"
      to: "Phase 7 close-out re-run"
      via: "scripts/check-backend.mjs against $PROD_API_URL"
      pattern: "check-backend"
    - from: "Plan 02 scripts/check-production-routes.mjs"
      to: "Phase 7 close-out end-to-end gate"
      via: "npm run check:prod against $NEXT_PUBLIC_SITE_URL"
      pattern: "check-production-routes"
---

<objective>
Close out Phase 7: re-run the backend smoke gate (D-22 — Phase 6 D-11 re-run; confirms BE didn't drift during Phase 7 FE-only changes) + the new `scripts/check-production-routes.mjs` smoke gate (Plan 02 deliverable) end-to-end against production; update all status artifacts (REQUIREMENTS.md ticks, PROJECT.md decisions evolution, ROADMAP.md Phase 7 [x], STATE.md current-position); write the final 07-VERIFICATION.md sign-off section with phase verdict + cross-section summary + carry-forward audit trail. Phase 7 close = v1 milestone complete.

Purpose: Final phase gate. Confirms no last-minute drift between Phase 7 deltas and the production system; flips all status markers to the closed state; produces an auditable phase verdict that downstream readers (or v1.1 planners) can use as the canonical "v1 shipped" reference.

Output: Two close-out smoke gate text files + 4 status artifacts updated (REQUIREMENTS.md / PROJECT.md / ROADMAP.md / STATE.md) + 07-VERIFICATION.md final sign-off section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-deploy-verification/07-CONTEXT.md
@.planning/phases/07-deploy-verification/07-RESEARCH.md
@.planning/phases/07-deploy-verification/07-VALIDATION.md
@.planning/phases/06-backend-content-population/06-CONTEXT.md

<canonical_refs>
- CONTEXT.md D-22 — backend smoke gate re-run in close-out
- CONTEXT.md D-04 — REQUIREMENTS.md DEV-03 status (already updated in Plan 01 Task 3; verify here)
- Phase 6 06-CONTEXT.md §D-11 — backend smoke gate pattern (scripts/check-backend.mjs against $PROD_API_URL)
- Plan 02 — scripts/check-production-routes.mjs (npm run check:prod) deliverable for the end-to-end FE gate
- All eight prior Phase 7 plans (07-01 through 07-08) — sections of 07-VERIFICATION.md to summarize
</canonical_refs>

<interfaces>
<!-- Status-artifact update points — exact section locations. -->

REQUIREMENTS.md edits (4 locations):

1. Lines 130-136 — v1 DEPLOY-01..07 checkboxes — flip each `- [ ]` to `- [x]`:
   - `- [x] **DEPLOY-01**: Production deploy on Vercel ...`
   - `- [x] **DEPLOY-02**: Lighthouse on the production URL ...`
   - ... through DEPLOY-07

2. Lines 285-291 — traceability table — flip the "Pending" cells to "Complete (07-NN)":
   - `| DEPLOY-01 | Phase 7 | Complete (07-01) |`
   - `| DEPLOY-02 | Phase 7 | Complete (07-04) |`
   - `| DEPLOY-03 | Phase 7 | Complete (07-05) |`
   - `| DEPLOY-04 | Phase 7 | Complete (07-07) |`
   - `| DEPLOY-05 | Phase 7 | Complete (07-06) |`
   - `| DEPLOY-06 | Phase 7 | Complete (07-03) |`
   - `| DEPLOY-07 | Phase 7 | Complete (07-08) |`

3. v1 milestone-complete summary line (find the existing "Mapped to phases" / "Coverage" lines near line 294 — confirm count remains 89/89 and add a one-line v1 completion stamp if a natural spot exists).

ROADMAP.md edits (2 locations):

1. Line 21 — `- [ ] **Phase 7: Deploy + Verification** ...` → `- [x] **Phase 7: Deploy + Verification** ...`

2. Lines 150-160 (Phase 7 Phase Details section) — add a completion date and verdict marker to the Goal line / Plans count, modeling on Phase 6's existing pattern:
   `### Phase 7: Deploy + Verification`
   `**Goal**: Production cutover ... (completed YYYY-MM-DD — verdict PASS; all 7 DEPLOY requirements green; Phase 4 G7/G8 + Phase 5 G3 carry-forwards closed via D-18)`

3. Lines 172-180 (Progress table) — flip the Phase 7 row:
   `| 7. Deploy + Verification | 9/9 | Complete | YYYY-MM-DD |`

STATE.md edits — replace current "Phase 7 not started" entries with "Phase 7 complete + v1 milestone done" entries. Specific format depends on existing STATE.md structure (which had "Phase 7 context session recorded" in the most-recent commit 250a508).

PROJECT.md edits — append to the existing "Decisions" or equivalent section a small "Phase 7 evolution" note covering: third prod dep (@vercel/analytics) allowlisted; x-portfolio-source dropped (repo private); real-device carry-forwards closed via DevTools emulation; the four consciously-accepted Safari limitations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run backend smoke gate (D-22 / Phase 6 D-11)</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-22 (BE smoke gate re-runs as part of close-out)
    - .planning/phases/06-backend-content-population/06-CONTEXT.md D-11 (original BE smoke gate definition)
    - ../portfolio-services/scripts/check-backend.mjs (the actual script + PROD_API_URL env var)
  </read_first>
  <action>
    Confirm the Railway BE URL (Phase 6 close-out 06-09 set this; should be `https://personal-portfolio-services-production.up.railway.app`).

    Run the BE smoke gate, capturing output and exit code to evidence file:

    `PROD_API_URL=https://personal-portfolio-services-production.up.railway.app node ../portfolio-services/scripts/check-backend.mjs > .planning/phases/07-deploy-verification/be-smoke-output.txt 2>&1; echo "EXIT_CODE=$?" >> .planning/phases/07-deploy-verification/be-smoke-output.txt`

    Expected: exit 0 with 7/7 endpoints green (matches Phase 6 close-out outcome). The 7 BE endpoints validate against the contracts shipped during Phase 6 paired commits:
    - /api/profile
    - /api/projects
    - /api/stack
    - /api/experience
    - /api/apps
    - /api/posts
    - (one more per Phase 6 contract — verify via the script's own output)

    If exit != 0:
    - Identify the failing endpoint(s) from the script output.
    - Per D-23, Phase 7 has no expected BE touches — a failure here is BE-side drift (Railway service restart, Mongo connection issue, accidentally-redeployed BE during the Phase 7 wall-clock). Halt and surface to the user as a checkpoint:decision; the fix is BE-side and may require a paired-commit pattern (Phase 6 D-19) if it changes any API shape.
    - Do NOT proceed to Task 2 with a BE smoke gate failure.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-deploy-verification/be-smoke-output.txt && grep -q '^EXIT_CODE=0$' .planning/phases/07-deploy-verification/be-smoke-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-deploy-verification/be-smoke-output.txt` exists.
    - File ends with line `EXIT_CODE=0`.
    - File body contains the per-endpoint `✓` lines + final "all N endpoints green" summary.
  </acceptance_criteria>
  <done>
    BE smoke gate re-run green; D-22 satisfied. Backend posture unchanged since Phase 6 close-out 6436d96.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run check-production-routes.mjs end-to-end (Plan 02 deliverable)</name>
  <read_first>
    - scripts/check-production-routes.mjs (Plan 02 output)
    - package.json scripts.check:prod
    - .planning/phases/07-deploy-verification/07-VALIDATION.md DEPLOY-01 evidence-capture row
  </read_first>
  <action>
    Run the FE production-routes smoke gate against the production URL, capturing output and exit code:

    `NEXT_PUBLIC_SITE_URL=https://www.tatibekov.com npm run check:prod > .planning/phases/07-deploy-verification/check-prod-output.txt 2>&1; echo "EXIT_CODE=$?" >> .planning/phases/07-deploy-verification/check-prod-output.txt`

    Expected: exit 0 with 7/7 routes green (same as Plan 01 Task 5 inline curl verification, but now using the canonical script).

    If exit != 0:
    - Identify failing routes from the script output.
    - Likely cause: Vercel deploy regression OR env var unset. Both are show-stoppers — halt and surface.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-deploy-verification/check-prod-output.txt && grep -q '^EXIT_CODE=0$' .planning/phases/07-deploy-verification/check-prod-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-deploy-verification/check-prod-output.txt` exists.
    - File ends with line `EXIT_CODE=0`.
    - File body contains 7 `✓ <path> → <status>` lines plus final "all 7 routes green on https://www.tatibekov.com" line.
  </acceptance_criteria>
  <done>
    Production end-to-end smoke gate green. Plan 02's `scripts/check-production-routes.mjs` proven in real-world execution.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md (flip 7 DEPLOY checkboxes + traceability table rows)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 130-136 — DEPLOY-* v1 checkboxes; lines 285-291 — traceability table rows)
    - .planning/phases/07-deploy-verification/07-VERIFICATION.md (each DEPLOY-NN section's verdict for the traceability comment cell)
  </read_first>
  <action>
    Edit REQUIREMENTS.md:

    1. Lines 130-136 — flip each DEPLOY checkbox from `- [ ]` to `- [x]`. Do not change the requirement text after the bold ID. Seven edits total.

    2. Lines 285-291 — replace each `Pending` cell with `Complete (07-NN)` per the plan-to-requirement mapping:
       - `| DEPLOY-01 | Phase 7 | Complete (07-01) |`
       - `| DEPLOY-02 | Phase 7 | Complete (07-04) |`
       - `| DEPLOY-03 | Phase 7 | Complete (07-05) |`
       - `| DEPLOY-04 | Phase 7 | Complete (07-07) |`
       - `| DEPLOY-05 | Phase 7 | Complete (07-06) |`
       - `| DEPLOY-06 | Phase 7 | Complete (07-03) |`
       - `| DEPLOY-07 | Phase 7 | Complete (07-08) |`

    3. Do NOT touch the DEV-03 row — Plan 01 Task 3 already updated it to PASS-with-deviation.

    4. Do NOT change the "Coverage: v1 requirements 89 total, Mapped to phases 89/89" lines (they remain accurate).

    Do not commit yet — Task 6 stages everything together.
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*DEPLOY-0' .planning/REQUIREMENTS.md | (read n; test "$n" = "7") && grep -c '| DEPLOY-0[1-7] | Phase 7 | Complete (07-0[1-9]) |' .planning/REQUIREMENTS.md | (read n; test "$n" = "7")</automated>
  </verify>
  <acceptance_criteria>
    - All 7 DEPLOY-* v1 bullets are checked `[x]`.
    - All 7 DEPLOY-* traceability rows reflect `Complete (07-NN)` with the correct plan ID per the mapping above.
    - DEV-03 status (from Plan 01 Task 3) is preserved as `PASS-with-deviation`.
    - File parses cleanly (no broken markdown table rows).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects Phase 7 closure. All 89 v1 requirements traceability-complete.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md + STATE.md (phase status flips)</name>
  <read_first>
    - .planning/ROADMAP.md (line 21 — Phase 7 checkbox; lines 150-160 — Phase Details Goal line; lines 172-180 — Progress table row)
    - .planning/STATE.md (current state; recent commit 250a508 indicated "Phase 7 context session recorded")
  </read_first>
  <action>
    ROADMAP.md edits:

    1. Line 21 — flip checkbox + add date marker:
       FROM: `- [ ] **Phase 7: Deploy + Verification** - Production deploy, Lighthouse, Search Console, recruiter test, audits`
       TO: `- [x] **Phase 7: Deploy + Verification** - Production deploy, Lighthouse, Search Console, recruiter test, audits (YYYY-MM-DD — verdict PASS)`

    2. Phase Details section (around lines 150-160) — append a completion stamp to the existing Goal line, modeling on Phase 6's pattern (Phase 6 has `(completed 2026-05-11)` at the end of its Goal line).
       Goal line append: `(completed YYYY-MM-DD — verdict PASS; all 7 DEPLOY requirements green; Phase 4 Gates 7+8 + Phase 5 Gate 3 real-device carries closed via D-18; Phase 5 SEO-03c unfurl carry closed via Plan 07; v1 milestone shipped)`

    3. Plans count + per-plan checkbox list: update `**Plans**: TBD` to `**Plans:** 9 plans` and add the 9-plan checkbox list with brief objectives (matches Phase 6's format):
       ```
       **Plans:** 9 plans
       - [x] 07-01-PLAN.md — Wave 1: env-var flip + drop x-portfolio-source + redeploy (DEPLOY-01)
       - [x] 07-02-PLAN.md — Wave 1: Wave 0 infra — check-production-routes smoke gate + evidence dirs
       - [x] 07-03-PLAN.md — Wave 2: Vercel Analytics integration + track('resume_download') (DEPLOY-06)
       - [x] 07-04-PLAN.md — Wave 3: PageSpeed Insights × 7 routes (DEPLOY-02)
       - [x] 07-05-PLAN.md — Wave 3: Google Search Console DNS TXT + sitemap (DEPLOY-03)
       - [x] 07-06-PLAN.md — Wave 3: npm audit + knip pre-close-out gates (DEPLOY-05)
       - [x] 07-07-PLAN.md — Wave 4: 5s recruiter test + Slack/LinkedIn unfurl (DEPLOY-04)
       - [x] 07-08-PLAN.md — Wave 4: 375px DevTools shell review × 7 routes + real-device carry closure (DEPLOY-07)
       - [x] 07-09-PLAN.md — Wave 4: BE smoke gate re-run + status artifacts + 07-VERIFICATION.md sign-off
       ```

    4. Progress table (lines 172-180) — flip Phase 7 row:
       FROM: `| 7. Deploy + Verification | 0/TBD | Not started | - |`
       TO: `| 7. Deploy + Verification | 9/9 | Complete | YYYY-MM-DD |`

    STATE.md edits:

    Read current STATE.md structure first. The most recent commit (250a508) added a "Phase 7 context session recorded" entry. Update STATE.md to:
    - Reflect Phase 7 complete + v1 milestone done.
    - Append a "Phase 7 close-out" entry to the recent-decisions list.
    - Update the "current position" indicator (whatever shape STATE.md uses — frontmatter or top-section).
    - Note: v1 milestone complete; next milestone is v1.1 / v2 (no active Phase 8 currently in roadmap).

    Replace `YYYY-MM-DD` with the actual completion date in all four edits.

    Do not commit yet — Task 6 stages everything together.
  </action>
  <verify>
    <automated>grep -q '^- \[x\] \*\*Phase 7' .planning/ROADMAP.md && grep -q '7\. Deploy + Verification | 9/9 | Complete' .planning/ROADMAP.md && grep -q '07-09-PLAN.md' .planning/ROADMAP.md && grep -q 'Phase 7' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 7 checkbox flipped to `[x]` with completion-date marker.
    - ROADMAP.md Phase 7 Goal line has the completion-stamp suffix.
    - ROADMAP.md Plans block lists all 9 plans with checkmarks.
    - ROADMAP.md Progress table Phase 7 row reads `9/9 | Complete | YYYY-MM-DD`.
    - STATE.md reflects Phase 7 closure + v1 milestone done.
  </acceptance_criteria>
  <done>
    Roadmap + state artifacts reflect Phase 7 closure. v1 milestone visible as complete from the canonical roadmap.
  </done>
</task>

<task type="auto">
  <name>Task 5: Append Phase 7 evolution note to PROJECT.md Decisions section</name>
  <read_first>
    - .planning/PROJECT.md (current Decisions section structure; the most recent evolution was Phase 6 close-out per commit 048698a "docs(phase-6): evolve PROJECT.md after Phase 6 completion")
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-04, D-08, D-09, D-18 (the four decisions worth noting at project-level)
  </read_first>
  <action>
    Append a "Phase 7 evolution" entry to PROJECT.md's Decisions section (or equivalent — match the existing pattern set in commit 048698a). The entry captures the four project-level decisions made during Phase 7 that future contributors / readers need to know without digging into 07-CONTEXT.md:

    Content to add:

    ```markdown
    ### Phase 7 evolution (YYYY-MM-DD)

    - **Third prod dep allowlisted (D-08, D-09):** `@vercel/analytics@^2.0.1` added for `resume_download` event tracking (DEPLOY-06). Total v1 prod deps: 3 (next-themes, cmdk, @vercel/analytics). Same "no new prod deps" rule applies going forward. CLAUDE.md Stack constraints updated.
    - **`x-portfolio-source` HTTP header DROPPED (D-04):** The slot was Phase 5 D-30 DEFERRED-PHASE-7. Phase 7 resolved-as-dropped because the repo is private — pointing engineers at an inaccessible GitHub URL adds no signal. DEV-03 status: PASS-with-deviation. If repo goes public in v1.1, this decision flips and the header gets re-added.
    - **Real-device carry-forwards closed via DevTools emulation (D-18):** Phase 4 Gates 7+8 (iPhone Safari + Android Chrome) and Phase 5 Gate 3 (reduce-motion real-device) closed via Chrome DevTools emulation + Phase 5 56-cell axe matrix + Plan 04 PSI mobile profile. **Consciously-accepted limitations (NOT physically validated in v1):** Safari `dvh`/`svh` viewport units, soft-keyboard behavior in palette, mobile address-bar overlap at TopBar, `-webkit-overflow-scrolling: touch` momentum scrolling. A v1.1 user report along any of these dimensions triggers physical-device validation.
    - **Canonical site URL = https://www.tatibekov.com (D-01):** `NEXT_PUBLIC_SITE_URL` set in Vercel Production env; apex `tatibekov.com` 307s to www; Vercel-hostname alias `personal-portfolio-web-orcin.vercel.app` remains as Production alias and de-duplicates via canonical URLs from app/sitemap.ts (no explicit noindex).
    - **GSC Domain property at `tatibekov.com` (D-06, D-07):** DNS TXT verification (PERMANENT — do not delete); sitemap.xml submitted; ≥4/7 routes Discovered/Crawled/Indexed at Phase 7 close. Full indexing async (7-21 days normal for new domain).
    ```

    Insert at the END of the existing Decisions section (or wherever Phase 6's evolution entry lives — match its placement convention). Replace `YYYY-MM-DD` with the actual completion date.

    Do not commit yet — Task 6 stages everything together.
  </action>
  <verify>
    <automated>grep -q 'Phase 7 evolution' .planning/PROJECT.md && grep -q '@vercel/analytics' .planning/PROJECT.md && grep -q 'x-portfolio-source' .planning/PROJECT.md && grep -q 'Real-device carry-forwards' .planning/PROJECT.md && grep -q 'https://www.tatibekov.com' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md contains a `### Phase 7 evolution` heading with the completion date.
    - The 5 bullets cover: third prod dep, x-portfolio-source drop, real-device closure with limitations, canonical URL, GSC.
    - DNS TXT permanence warning is preserved in the GSC bullet.
    - Markdown renders cleanly.
  </acceptance_criteria>
  <done>
    PROJECT.md captures the project-level evolution. Future planners reading PROJECT.md see Phase 7 outcomes without spelunking phase artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 6: Write final 07-VERIFICATION.md sign-off section + commit all close-out artifacts</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-VERIFICATION.md (existing — should now have DEPLOY-02, DEPLOY-03, DEPLOY-04, DEPLOY-05, DEPLOY-07 sections from Plans 04-08)
    - Tasks 1, 2 evidence files (be-smoke-output.txt + check-prod-output.txt) — exit codes for the sign-off section
  </read_first>
  <action>
    Append a final `## Phase 7 — Sign-off` section to 07-VERIFICATION.md.

    Section content:

    ```markdown
    ## Phase 7 — Sign-off

    **Phase 7 verdict: PASS**

    **Close-out date:** YYYY-MM-DD

    ### Cross-section summary

    | Requirement | Plan | Verdict | Section |
    |-------------|------|---------|---------|
    | DEPLOY-01 | 07-01 | PASS | (Inline — production deploy verified via Plan 01 Task 5 curl loop + Plan 09 Task 2 check-production-routes re-run) |
    | DEPLOY-02 | 07-04 | PASS | ## DEPLOY-02 — Lighthouse (PageSpeed Insights, mobile profile) |
    | DEPLOY-03 | 07-05 | PASS | ## DEPLOY-03 — Search Console (sitemap + indexing) |
    | DEPLOY-04 | 07-07 | PASS | ## DEPLOY-04 — 5-second recruiter test + unfurl preview |
    | DEPLOY-05 | 07-06 | PASS | ## DEPLOY-05 — npm audit + knip (pre-close-out gates) |
    | DEPLOY-06 | 07-03 | PASS | (Inline — Plan 03 Task 7 dashboard event-row verification) |
    | DEPLOY-07 | 07-08 | PASS | ## DEPLOY-07 — 375px shell review |

    ### Close-out smoke gates re-run

    | Gate | Command | Exit | Evidence |
    |------|---------|------|----------|
    | BE smoke (D-22) | `node ../portfolio-services/scripts/check-backend.mjs` against Railway | 0 | `be-smoke-output.txt` |
    | FE production routes | `NEXT_PUBLIC_SITE_URL=https://www.tatibekov.com npm run check:prod` | 0 | `check-prod-output.txt` |

    ### Carry-forwards closed in Phase 7

    - **Phase 5 D-30 (x-portfolio-source value DEFERRED-PHASE-7):** RESOLVED-as-dropped per Plan 01 D-04. DEV-03 status: PASS-with-deviation.
    - **Phase 5 SEO-03c (Slack/LinkedIn unfurl manual test):** PASS via Plan 07 unfurl section.
    - **Phase 4 Gate 7 (iPhone Safari real-device):** closed via Plan 08 DevTools emulation + Phase 5 axe matrix.
    - **Phase 4 Gate 8 (Android Chrome real-device):** closed via Plan 08 DevTools emulation + Phase 5 axe matrix.
    - **Phase 5 Gate 3 (reduce-motion real-device):** closed via Plan 08 DevTools emulation + Phase 5 source-level reduce-motion CSS.

    ### Known Limitations (carried into v1)

    Per D-18, four Safari-specific behaviors are NOT physically validated in v1:
    1. Safari `dvh` / `svh` viewport units (iOS Safari address-bar dynamics).
    2. Soft-keyboard behavior in mobile palette / contact inputs.
    3. Mobile address-bar overlap at TopBar.
    4. `-webkit-overflow-scrolling: touch` momentum scroll.

    A v1.1 user report along any of these dimensions triggers physical-device validation post-v1.

    ### v1 milestone

    All 89 v1 requirements traceability-complete (REQUIREMENTS.md). Roadmap shows 7/7 phases complete. **v1 SHIPPED.**
    ```

    Replace `YYYY-MM-DD` with the actual close-out date.

    Stage and commit all close-out artifacts:
    `git add .planning/phases/07-deploy-verification/07-VERIFICATION.md .planning/phases/07-deploy-verification/be-smoke-output.txt .planning/phases/07-deploy-verification/check-prod-output.txt .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md`

    `git commit -m "docs(07): close-out Phase 7 — verdict PASS, v1 milestone shipped (DEPLOY-01..07)"`

    `git push origin main`
  </action>
  <verify>
    <automated>grep -q '^## Phase 7 — Sign-off' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q '\*\*Phase 7 verdict: PASS\*\*' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'Cross-section summary' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'BE smoke (D-22)' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'Carry-forwards closed' .planning/phases/07-deploy-verification/07-VERIFICATION.md && grep -q 'v1 SHIPPED' .planning/phases/07-deploy-verification/07-VERIFICATION.md && git log -1 --pretty=%s | grep -qE 'close-out Phase 7|v1 milestone'</automated>
  </verify>
  <acceptance_criteria>
    - 07-VERIFICATION.md ends with a populated `## Phase 7 — Sign-off` section containing: phase verdict PASS, close-out date, 7-row cross-section summary table, close-out smoke gates table, carry-forwards-closed list (5 items), Known Limitations list (4 items), v1 milestone stamp.
    - Single commit lands all close-out artifacts (07-VERIFICATION.md + 2 evidence .txt files + REQUIREMENTS.md + PROJECT.md + ROADMAP.md + STATE.md = 7 files).
    - Commit subject references "close-out Phase 7" and "v1 milestone".
    - Push completes; working tree clean.
  </acceptance_criteria>
  <done>
    Phase 7 closed. v1 milestone shipped. All 89 v1 requirements green. Carry-forwards from Phases 4 and 5 traceably resolved. Known Limitations documented for v1.1 triage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BE smoke gate (cross-repo invocation) | Runs a script from the sibling repo against the Railway production URL. Read-only HTTP requests; no mutation. |
| Status artifacts in `.planning/` | Markdown files; non-executable; updates are documentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-33 | Tampering | Status-artifact updates (REQUIREMENTS / ROADMAP / STATE / PROJECT) | mitigate | Each edit is small + reviewable in the commit diff; Task 6's verify command checks for presence of expected markers across all four files before commit succeeds. |
| T-07-34 | Repudiation | Phase 7 verdict claim "PASS" with no automated cross-check | mitigate | The sign-off section's Cross-section summary table back-references each DEPLOY-* sub-section in 07-VERIFICATION.md, each of which has its own evidence (screenshots, exit codes, recruiter test data). A reviewer can audit any cell back to evidence in 2 hops. |
| T-07-35 | Information Disclosure | BE smoke output may reveal endpoint shape | accept | Same as Phase 6 D-11 disposition — endpoint shapes are public anyway (GET requests); no auth surface, no PII. Output file goes in private repo. |
| T-07-36 | Denial of Service | Re-running smoke gates against production | accept | Two batch fetches (7 endpoints BE + 7 routes FE); well below any platform rate limit. Same posture as Phase 6 D-11 + Plan 01 Task 5. |

ASVS L1: PASS. Close-out is documentation-only; no new attack surface; no new auth; no new endpoints.
</threat_model>

<verification>
- Task 1: BE smoke gate re-run; be-smoke-output.txt ends EXIT_CODE=0; 7/7 endpoints green.
- Task 2: FE check-production-routes re-run; check-prod-output.txt ends EXIT_CODE=0; 7/7 routes green.
- Task 3: REQUIREMENTS.md DEPLOY-01..07 flipped to [x] in v1 list + traceability table.
- Task 4: ROADMAP.md Phase 7 row flipped to [x] with completion date + Plans block populated + Progress table row reads 9/9 Complete; STATE.md reflects Phase 7 done + v1 milestone.
- Task 5: PROJECT.md "Phase 7 evolution" entry appended with 5 bullets.
- Task 6: 07-VERIFICATION.md final Sign-off section appended; single 7-file close-out commit pushed.
</verification>

<success_criteria>
This plan succeeds when:
1. BE smoke gate exits 0 (D-22 satisfied).
2. FE check-production-routes exits 0 with 7/7 green.
3. REQUIREMENTS.md DEPLOY-01..07 all `[x]` + traceability rows all `Complete (07-NN)`.
4. ROADMAP.md Phase 7 `[x]` + Progress table `9/9 | Complete`.
5. PROJECT.md "Phase 7 evolution" entry captures the 5 project-level decisions.
6. STATE.md reflects Phase 7 complete + v1 milestone done.
7. 07-VERIFICATION.md ends with Sign-off section reading "Phase 7 verdict: PASS" and "v1 SHIPPED".
8. Single 7-file close-out commit pushed to origin/main.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deploy-verification/07-09-SUMMARY.md` capturing:
- BE smoke gate exit code + 7-endpoint status
- FE check-production-routes exit code + 7-route status
- REQUIREMENTS.md DEPLOY-* statuses confirmed flipped
- ROADMAP.md + STATE.md flips confirmed
- PROJECT.md "Phase 7 evolution" entry placement
- 07-VERIFICATION.md Sign-off section line count
- Close-out commit SHA
- v1 milestone shipped stamp
</output>
