---
phase: 06-backend-content-population
plan: 07
type: execute
wave: 7
depends_on: ["06-06"]
files_modified:
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Project.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/controllers/contentController.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/routes/apiRoutes.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/placeholders.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/projects.json
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/scripts/seed.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/tests/app.test.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/docs/api-contract.md
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts
  - /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.test.ts
autonomous: true
requirements:
  - BACKEND-01
  - BACKEND-02
  - CONTENT-02
tags:
  - phase-6
  - backend
  - projects
  - paired-commit
  - content
  - greenfield

must_haves:
  truths:
    - "NEW backend `Project` Mongoose model with 7 fields: name, year, status, summary, tech[], role, link"
    - "NEW `/api/projects` route handler `getProjects` in contentController.ts"
    - "NEW route line `apiRoutes.get('/projects', getProjects)` in apiRoutes.ts"
    - "NEW `ProjectDto` in src/types/content.ts mirroring lib/types.ts Project"
    - "NEW `placeholderProjects` in src/seed/placeholders.ts (503-branch fallback)"
    - "`src/seed/projects.json` has ≥3 real projects (CONTENT-02)"
    - "Backend Jest spec asserts `GET /api/projects` returns Project[] shape"
    - "Seed script upserts each Project by `name` (unique)"
    - "Frontend `PROJECTS` constant in `lib/portfolio-data.ts` populated with ≥3 real entries (mirror of seed)"
    - "Frontend vitest asserts PROJECTS.length >= 3"
    - "`docs/api-contract.md` ## GET /api/projects section is filled with the full Project[] interface"
    - "BACKEND-01 satisfied — the /api/projects endpoint exists end-to-end with model + controller + route + seed + test + contract"
  artifacts:
    - path: "/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Project.ts"
      provides: "NEW Mongoose model for Project; mirrors lib/types.ts Project"
      contains: "tech:"
    - path: "/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/projects.json"
      provides: "≥3 real projects (CONTENT-02)"
      contains: "summary"
  key_links:
    - from: "portfolio-services/src/routes/apiRoutes.ts"
      to: "getProjects controller"
      via: "apiRoutes.get('/projects', getProjects)"
      pattern: "'/projects'"
    - from: "portfolio-services/src/scripts/seed.ts"
      to: "Project.findOneAndUpdate({ name }, ...)"
      via: "upsert by name (unique)"
      pattern: "Project.findOneAndUpdate"
---

<objective>
Greenfield wave: introduce the new `/api/projects` endpoint from zero (BACKEND-01). No "delete" half — this is purely additive on the BE side. Ship ≥3 real projects (CONTENT-02) mirrored across both repos. Same recipe as Waves 02-06, but everything is new (no rename, no field-drop).

Output: 8 BE files modified/created + 2 FE files modified across two paired commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-backend-content-population/06-CONTEXT.md
@.planning/phases/06-backend-content-population/06-RESEARCH.md
@.planning/phases/06-backend-content-population/06-PATTERNS.md
@.planning/phases/06-backend-content-population/06-06-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Target Project shape from lib/types.ts lines 62-77: -->

```typescript
export interface Project {
  name: string;
  year: string;
  status: string;     // "shipped", "active", "archived"
  summary: string;
  tech: string[];     // ["TypeScript", "Next.js", "AWS"]
  role: string;       // "lead", "ic"
  link: string;       // https URL
}
```

<!-- RESEARCH §"Code Examples" Project schema (lines 701-722) — use VERBATIM: -->

```typescript
import { Schema, model } from 'mongoose';

const projectSchema = new Schema(
  {
    name:    { type: String, required: true, unique: true },
    year:    { type: String, required: true },
    status:  { type: String, required: true },
    summary: { type: String, required: true },
    tech:    { type: [String], required: true, default: [] },
    role:    { type: String, required: true },
    link:    { type: String, required: true }
  },
  { timestamps: true, strict: 'throw' }
);

export const Project = model('Project', projectSchema);
```

<!-- RESEARCH §"Code Examples" getProjects controller (lines 794-817) — use VERBATIM: -->

```typescript
export const getProjects = async (_req, res) => {
  if (mongoose.connection.readyState !== 1) { res.status(503).json({ error: 'service warming' }); return; }
  try {
    const docs = await Project.find().sort({ year: -1 }).lean();
    const clean = docs.map(({ _id, __v, createdAt, updatedAt, ...rest }) => rest);
    res.status(200).json(clean);
  } catch { res.status(503).json({ error: 'fetch failed' }); }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Projects greenfield — new model, controller, route, DTO, seed JSON + upsert, Jest spec, contract</name>
  <files>
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Project.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/controllers/contentController.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/routes/apiRoutes.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/placeholders.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/projects.json
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/scripts/seed.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/tests/app.test.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/docs/api-contract.md
  </files>
  <read_first>
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/controllers/contentController.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/routes/apiRoutes.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/placeholders.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/scripts/seed.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/tests/app.test.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/docs/api-contract.md
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/types.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts
  </read_first>
  <action>
    All paths in `/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/`.

    **Step 1 — Add `ProjectDto` to `src/types/content.ts`** (append after the existing DTOs):

    ```typescript
    // Mirror of portfolio-web/lib/types.ts Project (paired-commit / D-19).
    export type ProjectDto = {
      name: string;
      year: string;
      status: string;     // "shipped" | "active" | "archived"
      summary: string;
      tech: string[];
      role: string;
      link: string;       // https URL
    };
    ```

    **Step 2 — Create `src/models/Project.ts`** (RESEARCH §"Code Examples" verbatim):

    ```typescript
    import { Schema, model } from 'mongoose';

    const projectSchema = new Schema(
      {
        name:    { type: String, required: true, unique: true },
        year:    { type: String, required: true },
        status:  { type: String, required: true },
        summary: { type: String, required: true },
        tech:    { type: [String], required: true, default: [] },
        role:    { type: String, required: true },
        link:    { type: String, required: true }
      },
      { timestamps: true, strict: 'throw' }
    );

    export const Project = model('Project', projectSchema);
    ```

    **Step 3 — Add `placeholderProjects` to `src/seed/placeholders.ts`**:

    Add `ProjectDto` to the import line. Append:

    ```typescript
    export const placeholderProjects: ProjectDto[] = [
      {
        name: 'Terminal Portfolio',
        year: '2026',
        status: 'shipped',
        summary: 'Brownfield redesign of this site into a terminal/IDE shell with seven RSC views.',
        tech: ['Next.js', 'React', 'TypeScript'],
        role: 'lead',
        link: 'https://github.com/beckinfonet/portfolio-web'
      }
    ];
    ```

    **Step 4 — Add `getProjects` to `src/controllers/contentController.ts`** (RESEARCH §"Code Examples" verbatim). Add the Project import + placeholderProjects import; append the new handler at the end of the file:

    Add to imports:
    ```typescript
    import { Project } from '../models/Project';
    ```

    Add to the seed-placeholder import block:
    ```typescript
    import {
      placeholderApps,
      placeholderExperience,
      placeholderPosts,
      placeholderProfile,
      placeholderProjects,
      placeholderStack
    } from '../seed/placeholders';
    ```

    Append handler:

    ```typescript
    export const getProjects = async (_req: Request, res: Response): Promise<void> => {
      if (mongoose.connection.readyState !== 1) {
        res.status(503).json({ error: 'service warming' });
        return;
      }
      try {
        const docs = await Project.find().sort({ year: -1 }).lean();
        const clean = docs.map((d: Record<string, unknown>) => {
          const { _id, __v, createdAt, updatedAt, ...rest } = d;
          return rest;
        });
        res.status(200).json(clean.length > 0 ? clean : placeholderProjects);
      } catch {
        res.status(503).json({ error: 'fetch failed' });
      }
    };
    ```

    **Step 5 — Add route to `src/routes/apiRoutes.ts`**. Add `getProjects` to imports + add a new `apiRoutes.get(...)` line:

    ```typescript
    import { Router } from 'express';
    import { getApps, getExperience, getHealth, getPosts, getProfile, getProjects, getStack } from '../controllers/contentController';

    const apiRoutes = Router();

    apiRoutes.get('/health', getHealth);
    apiRoutes.get('/profile', getProfile);
    apiRoutes.get('/projects', getProjects);
    apiRoutes.get('/stack', getStack);
    apiRoutes.get('/experience', getExperience);
    apiRoutes.get('/apps', getApps);
    apiRoutes.get('/posts', getPosts);

    export default apiRoutes;
    ```

    **Step 6 — Create `src/seed/projects.json`** with ≥3 real projects (CONTENT-02). Per CONTEXT.md "Claude's Discretion": planner prompts developer at execution time for real projects. Default content (developer must replace anything that fails INFRA-05; "Confidential" is a safe stand-in for client work that isn't public):

    ```json
    [
      {
        "name": "Terminal Portfolio",
        "year": "2026",
        "status": "shipped",
        "summary": "Brownfield redesign of this site into a terminal/IDE shell with seven RSC views, four-hue theme system, and full mobile redistribution.",
        "tech": ["Next.js 15", "React 19", "TypeScript", "Vitest", "Playwright"],
        "role": "lead",
        "link": "https://github.com/beckinfonet/portfolio-web"
      },
      {
        "name": "Portfolio Services",
        "year": "2026",
        "status": "shipped",
        "summary": "Express 5 + Mongoose backend for the terminal portfolio; hand-mirrored type contract with the Next.js frontend.",
        "tech": ["Express 5", "Mongoose 9", "TypeScript", "Jest", "Railway"],
        "role": "lead",
        "link": "https://github.com/beckinfonet/portfolio-services"
      },
      {
        "name": "GSD Workflow",
        "year": "2025",
        "status": "active",
        "summary": "Multi-agent Claude planning workflow for incremental software delivery; ships PLAN.md files as executable prompts.",
        "tech": ["Claude", "Anthropic SDK", "Bash", "TypeScript"],
        "role": "lead",
        "link": "https://github.com/beckinfonet"
      }
    ]
    ```

    All three projects above MUST have publicly-reachable HTTPS links (Pitfall 5 generalized). If the developer's actual project list at execution time is different, use those exact names + links — the above is fallback shape-only content.

    **Step 7 — Extend `src/scripts/seed.ts`** — Project import + upsert block:

    ```typescript
    import { Project } from '../models/Project';
    ```

    After the Posts upsert block:

    ```typescript
      // Projects — array collection, upsert by name (unique).
      const projects = await load<Array<{ name: string }>>('projects.json');
      for (const entry of projects) {
        await Project.findOneAndUpdate(
          { name: entry.name },
          entry,
          { upsert: true, new: true, setDefaultsOnInsert: true }
        );
      }
      console.log(`seed: ${projects.length} projects upserted`);
    ```

    Extend final-count log:
    ```typescript
      const projectCount = await Project.countDocuments();
      console.log(`seed: final project count = ${projectCount}`);
    ```

    **Step 8 — Extend `tests/app.test.ts`** — append:

    ```typescript
    it('GET /api/projects returns Project[] shape or 503 when DB not ready', async () => {
      const response = await request(app).get('/api/projects');
      expect([200, 503]).toContain(response.status);
      if (response.status === 200) {
        expect(Array.isArray(response.body)).toBe(true);
        expect(response.body.length).toBeGreaterThan(0);
        expect(response.body[0]).toEqual(
          expect.objectContaining({
            name: expect.any(String),
            year: expect.any(String),
            status: expect.any(String),
            summary: expect.any(String),
            tech: expect.any(Array),
            role: expect.any(String),
            link: expect.any(String)
          })
        );
        expect(response.body[0]._id).toBeUndefined();
        expect(response.body[0].link).toMatch(/^https?:\/\//);
      }
    });
    ```

    **Step 9 — Fill `docs/api-contract.md` ## GET /api/projects** — replace `_filled by Wave 07_`:

    ```markdown
    ## GET /api/projects

    **Response 200 — type from portfolio-web/lib/types.ts Project[]:**
    ```ts
    interface Project {
      name: string;
      year: string;
      status: string;     // "shipped" | "active" | "archived"
      summary: string;
      tech: string[];
      role: string;
      link: string;       // https URL
    }

    type Response = Project[];   // sorted by year desc
    ```

    **Response 503:** `{ error: string }`.

    **Frontend cache:** `next: { revalidate: 300 }`.

    **Example payload:** see `src/seed/projects.json`.

    **Note:** Phase 6 Wave 07 added this endpoint (BACKEND-01). New `Project` model + `getProjects` controller + route line; mirror frontend constant in portfolio-web/lib/portfolio-data.ts.
    ```

    **Step 10 — Verify and commit**:
    ```bash
    cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services
    npm run build
    npm test     # 8 specs now
    git add -A
    git commit -m "feat(projects): add /api/projects endpoint end-to-end (BACKEND-01)

    - src/models/Project.ts: NEW — 7-field schema with strict:'throw' + unique on name
    - src/types/content.ts: + ProjectDto
    - src/controllers/contentController.ts: + getProjects (sort year desc, 503 branch, _id strip)
    - src/routes/apiRoutes.ts: + /api/projects route line
    - src/seed/placeholders.ts: + placeholderProjects (503-branch fallback)
    - src/seed/projects.json: 3 real projects (CONTENT-02)
    - src/scripts/seed.ts: Project upsert by name
    - tests/app.test.ts: + GET /api/projects shape spec
    - docs/api-contract.md: ## GET /api/projects filled

    Pair: portfolio-web @ <SHA from Task 2 below>"
    ```
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services &amp;&amp;
      npm run build &amp;&amp;
      npm test &amp;&amp;
      test -f src/models/Project.ts &amp;&amp;
      grep -q "ProjectDto" src/types/content.ts &amp;&amp;
      grep -q "getProjects" src/controllers/contentController.ts &amp;&amp;
      grep -q "apiRoutes.get('/projects'" src/routes/apiRoutes.ts &amp;&amp;
      test -f src/seed/projects.json &amp;&amp;
      [ "$(node -e "console.log(JSON.parse(require('fs').readFileSync('src/seed/projects.json','utf8')).length)")" -ge 3 ] &amp;&amp;
      grep -q "Project.findOneAndUpdate" src/scripts/seed.ts &amp;&amp;
      grep -q "GET /api/projects returns Project" tests/app.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/models/Project.ts` succeeds
    - `grep -c "ProjectDto" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/types/content.ts` is ≥ `1`
    - `grep -c "apiRoutes.get('/projects'" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/routes/apiRoutes.ts` is `1`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/projects.json','utf8')).length)"` prints a number ≥ `3`
    - `node -e "const p=JSON.parse(require('fs').readFileSync('/Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/projects.json','utf8')); console.log(p.every(x=>/^https?:/.test(x.link)))"` prints `true`
    - `cd portfolio-services && npm test 2>&1 | tail -3 | grep -E "Tests.*passed"` reports 8 passing specs
    - `! grep -q "_filled by Wave 07_" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/docs/api-contract.md`
    - `! grep -qE "TODO|lorem|example\.com|placeholder|Product Studio" /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/projects.json`
    - `cd portfolio-services && git log -1 --oneline | grep -qi "projects"` confirms commit
  </acceptance_criteria>
  <done>
    Backend /api/projects endpoint added end-to-end. Jest 7 → 8 specs. BACKEND-01 satisfied at the BE level. SHA placeholder for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend PROJECTS population + vitest assertions + paired SHA</name>
  <files>
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.test.ts
  </files>
  <read_first>
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-web/lib/portfolio-data.test.ts
    /Users/beckmaldinVL/development/personal-portfolio/portfolio-services/src/seed/projects.json
  </read_first>
  <action>
    All paths in `/Users/beckmaldinVL/development/personal-portfolio/portfolio-web/`.

    **Step 1 — Populate `lib/portfolio-data.ts` PROJECTS** — replace the empty array (lines 67-70) with the mirror of `src/seed/projects.json`. Translate each JSON object to a TypeScript object literal with the exact same key order + values (D-14 byte-identical mirror).

    Concrete target (assuming the default seed JSON from Task 1; if developer supplied real projects, mirror those):

    ```typescript
    export const PROJECTS: Project[] = [
      {
        name: "Terminal Portfolio",
        year: "2026",
        status: "shipped",
        summary: "Brownfield redesign of this site into a terminal/IDE shell with seven RSC views, four-hue theme system, and full mobile redistribution.",
        tech: ["Next.js 15", "React 19", "TypeScript", "Vitest", "Playwright"],
        role: "lead",
        link: "https://github.com/beckinfonet/portfolio-web"
      },
      {
        name: "Portfolio Services",
        year: "2026",
        status: "shipped",
        summary: "Express 5 + Mongoose backend for the terminal portfolio; hand-mirrored type contract with the Next.js frontend.",
        tech: ["Express 5", "Mongoose 9", "TypeScript", "Jest", "Railway"],
        role: "lead",
        link: "https://github.com/beckinfonet/portfolio-services"
      },
      {
        name: "GSD Workflow",
        year: "2025",
        status: "active",
        summary: "Multi-agent Claude planning workflow for incremental software delivery; ships PLAN.md files as executable prompts.",
        tech: ["Claude", "Anthropic SDK", "Bash", "TypeScript"],
        role: "lead",
        link: "https://github.com/beckinfonet"
      }
    ];
    ```

    **Step 2 — Extend `lib/portfolio-data.test.ts`**:

    ```typescript
    describe("PROJECTS content (Wave 07)", () => {
      test("PROJECTS has at least 3 entries (CONTENT-02)", () => {
        expect(PROJECTS.length).toBeGreaterThanOrEqual(3);
      });

      test("every PROJECTS entry has all 7 required fields + HTTPS link", () => {
        for (const p of PROJECTS) {
          expect(p.name).toBeTruthy();
          expect(p.year).toBeTruthy();
          expect(p.status).toBeTruthy();
          expect(p.summary).toBeTruthy();
          expect(Array.isArray(p.tech)).toBe(true);
          expect(p.tech.length).toBeGreaterThan(0);
          expect(p.role).toBeTruthy();
          expect(p.link).toMatch(/^https?:\/\//);
        }
      });

      test("PROJECTS names are unique (mirrors Mongo `unique: true` on name)", () => {
        const names = PROJECTS.map((p) => p.name);
        expect(new Set(names).size).toBe(names.length);
      });
    });
    ```

    **Step 3 — Verify and commit**:
    ```bash
    cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-web
    npm run typecheck
    npm test -- portfolio-data    # ≥ 22 tests pass
    npm run build

    BE_SHA=$(cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services && git rev-parse --short HEAD)
    git add lib/portfolio-data.ts lib/portfolio-data.test.ts
    git commit -m "feat(projects): populate PROJECTS constant + wave-07 vitest assertions

    - lib/portfolio-data.ts: PROJECTS filled with 3 entries (D-14 mirror of seed/projects.json); CONTENT-02 satisfied
    - lib/portfolio-data.test.ts: + 3 assertions (length>=3, all 7 fields + HTTPS link, unique names)

    Pair: portfolio-services @ $BE_SHA"

    FE_SHA=$(git rev-parse --short HEAD)
    cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services
    git commit --amend -m "$(git log -1 --format=%B | sed "s/<SHA from Task 2 below>/$FE_SHA/")"
    ```
  </action>
  <verify>
    <automated>
      cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-web &amp;&amp;
      npm run typecheck &amp;&amp;
      npm test -- portfolio-data &amp;&amp;
      npm run build &amp;&amp;
      grep -q "Terminal Portfolio" lib/portfolio-data.ts &amp;&amp;
      grep -q "PROJECTS has at least 3 entries" lib/portfolio-data.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `cd portfolio-web && npm run build` exits 0 (INFRA-05 grep clean)
    - `cd portfolio-web && grep -c "summary:" lib/portfolio-data.ts` is ≥ `5` (3 PROJECTS + 3 EXPERIENCE entries — could be ≥6 once Wave 04 and Wave 07 both land)
    - `cd portfolio-web && npm test 2>&1 | tail -3 | grep -E "Tests.*passed"` reports total = 156 (was 153 after Plan 06; +3 projects assertions)
    - `cd portfolio-services && git log -1 --format=%B | grep -E "^Pair: portfolio-web @ [0-9a-f]{7,}$"` confirms paired SHA
  </acceptance_criteria>
  <done>
    PROJECTS populated end-to-end. Vitest 153 → 156. CONTENT-02 + BACKEND-01 fully shipped. Both commits paired.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | `_id` leak on /api/projects | mitigate | destructure-and-strip + strict:'throw' |
| T-06-08 | DoS/Bug | Duplicate projects on re-seed | mitigate | `name: unique: true` + upsert by name |
| T-06-09 | Tampering | Dead/wrong project link ships | mitigate | `/^https?:\/\//` vitest assertion; manual link tap in Wave 09 |
| T-06-03 | Drift | FE/BE drift | mitigate | D-19 paired SHAs |
</threat_model>

<verification>
```bash
cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-services
npm run build && npm test                                # → 8 specs pass
test -f src/models/Project.ts                            # → exists
grep -c "apiRoutes.get('/projects'" src/routes/apiRoutes.ts   # → 1

cd /Users/beckmaldinVL/development/personal-portfolio/portfolio-web
npm test 2>&1 | grep -E "Tests.*passed" | tail -1        # → 156 passed
```
</verification>

<success_criteria>
- New `/api/projects` endpoint live end-to-end (BACKEND-01)
- ≥3 real projects with HTTPS links (CONTENT-02)
- Jest 7 → 8
- Vitest 153 → 156
- Both commits cross-cite SHAs (D-19)
- All 7 `## GET /api/*` sections in api-contract.md filled (no `_filled by Wave NN_` placeholders remain)
</success_criteria>

<output>
`.planning/phases/06-backend-content-population/06-07-SUMMARY.md` documents: paired SHAs, names of the 3 (or more) projects shipped, whether all 7 contract sections are now filled, and Jest/Vitest counts.
</output>
