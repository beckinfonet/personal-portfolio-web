---
phase: 11-deploy-smoke-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .env.example
  - .planning/phases/11-deploy-smoke-verification/11-PROVISIONING-GUIDE.md
  - .planning/phases/11-deploy-smoke-verification/11-SMOKE-VERIFICATION.md
autonomous: true
requirements: [DEPLOY-V11-02, DEPLOY-V11-03]
user_setup: []

must_haves:
  truths:
    - "README.md documents GITHUB_TOKEN, its required scope, and the absence consequence"
    - "README.md no longer instructs copying a deleted .env.example file"
    - ".env.example is no longer a tracked file in the repo"
    - "npm run build, lint, and test all exit 0 with lib/github.ts integrated"
    - "An owner-facing step-by-step PAT + Vercel provisioning guide exists in the phase directory"
    - "A smoke-verification document scaffold exists ready for Plan 02 to fill"
  artifacts:
    - path: "README.md"
      provides: "Rewritten ## Environment and ## Deploy sections with inline env-var list"
      contains: "GITHUB_TOKEN"
    - path: ".planning/phases/11-deploy-smoke-verification/11-PROVISIONING-GUIDE.md"
      provides: "Owner-facing GitHub fine-grained PAT creation + Vercel dashboard env-var steps"
      min_lines: 30
    - path: ".planning/phases/11-deploy-smoke-verification/11-SMOKE-VERIFICATION.md"
      provides: "Smoke-test + ISR verification document scaffold with empty result slots"
      min_lines: 25
  key_links:
    - from: "README.md ## Environment"
      to: ".env.local"
      via: "explicit inline var list replacing cp .env.example instruction"
      pattern: "GITHUB_TOKEN"
---

<objective>
Prepare Phase 11 for the production cutover: rewrite the README env/deploy
documentation to inline every variable (including the new `GITHUB_TOKEN`),
remove the now-stale tracked `.env.example` file in the same commit, run the
local quality gates green, and author the owner-facing provisioning guide plus
the smoke-verification document scaffold that Plan 02 fills in.

Purpose: This is the fully-autonomous half of Phase 11 — everything Claude can
do without Vercel/GitHub dashboard auth. It unblocks Plan 02 by handing the
owner a precise step-by-step and a ready-to-fill verification artifact.

Output: Rewritten `README.md`, deleted `.env.example`, `11-PROVISIONING-GUIDE.md`,
and `11-SMOKE-VERIFICATION.md` scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-deploy-smoke-verification/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README env/deploy docs and remove .env.example in one commit</name>
  <files>README.md, .env.example</files>
  <read_first>
    - README.md (current ## Environment and ## Deploy sections — lines 20-51)
    - .env.example (the file being removed — confirm it holds only NEXT_PUBLIC_API_BASE_URL, NEXT_PUBLIC_SITE_URL, and a Production comment)
    - .planning/phases/11-deploy-smoke-verification/11-CONTEXT.md (D-04, D-05 — the exact documentation requirements)
    - lib/github.ts (lines 1-110 — GITHUB_TOKEN conditional bearer auth, the 60/hr unauthenticated ceiling, the no-token dev warning, daily ISR revalidate)
  </read_first>
  <action>
    Rewrite the `## Environment` section of README.md so it NO LONGER contains the string `cp .env.example .env.local` and NO LONGER references `.env.example` anywhere. Replace it with an explicit inline list of the three env vars to create in a git-ignored `.env.local`: `NEXT_PUBLIC_API_BASE_URL` (purpose: base URL of the portfolio-services backend; default `http://localhost:8080` if unset; absence consequence: lib/api.ts falls back to static data), `NEXT_PUBLIC_SITE_URL` (purpose: canonical site URL for metadata/sitemap; default `http://localhost:3000` if unset; in Production set to `https://www.tatibekov.com`), and `GITHUB_TOKEN` (purpose: authenticates lib/github.ts GitHub REST calls; required scope: fine-grained Personal Access Token, read-only, public-repo metadata only — no write, no private access per D-01; absence consequence: GitHub stats degrade gracefully to unauthenticated requests at the 60/hr rate-limit ceiling, and lib/github.ts logs a one-line dev warning — the strip/panel simply omit when stats are null, no crash). Mark `GITHUB_TOKEN` as server-only (no `NEXT_PUBLIC_` prefix — it must never reach the client bundle). Rewrite the `## Deploy` section so the Vercel bullet no longer says "set environment variables from `.env.example`"; instead state that the three env vars above are configured in Vercel Project Settings -> Environment Variables -> Production scope, and `GITHUB_TOKEN` specifically is added via the dashboard per the owner provisioning guide. In the SAME task (and SAME commit), run `git rm .env.example` to delete the tracked file. Per CLAUDE.md brownfield discipline the README rewrite that removes the `cp .env.example` instruction and the `.env.example` deletion MUST land together — never delete-then-replace leaving README pointing at a missing file. Implements D-04 and D-05. Do NOT write any real token value into README.md.
  </action>
  <verify>
    <automated>grep -q 'GITHUB_TOKEN' README.md && ! grep -q 'cp .env.example' README.md && ! grep -q '.env.example' README.md && ! git ls-files --error-unmatch .env.example 2>/dev/null && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README.md does NOT contain the string `cp .env.example .env.local`
    - README.md does NOT contain the substring `.env.example` anywhere
    - README.md `## Environment` section lists `GITHUB_TOKEN`, `NEXT_PUBLIC_API_BASE_URL`, and `NEXT_PUBLIC_SITE_URL`, each with a purpose line and an absence-consequence line
    - README.md states `GITHUB_TOKEN` requires a fine-grained read-only public-repo-metadata PAT and is server-only (no `NEXT_PUBLIC_` prefix)
    - `git ls-files` does NOT list `.env.example`
    - No real token value appears anywhere in README.md
  </acceptance_criteria>
  <done>README env/deploy docs inline all three vars with GITHUB_TOKEN fully documented; .env.example removed from the repo in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Run local quality gates green and author the owner provisioning guide + verification scaffold</name>
  <files>.planning/phases/11-deploy-smoke-verification/11-PROVISIONING-GUIDE.md, .planning/phases/11-deploy-smoke-verification/11-SMOKE-VERIFICATION.md</files>
  <read_first>
    - package.json (scripts block — prebuild check-resume-pdf/check-resume-docx, build, postbuild check-placeholders, lint, test)
    - scripts/check-placeholders.mjs (the INFRA-05 postbuild grep that must exit 0)
    - lib/github.ts (lines 100-260 — the X-RateLimit-Remaining / X-RateLimit-Reset dev logging and getRepoStats behaviour the Plan 02 ISR check exercises)
    - .planning/phases/11-deploy-smoke-verification/11-CONTEXT.md (D-01, D-02, D-03, D-06, D-07, D-08 — provisioning + verification requirements and Claude's Discretion items)
  </read_first>
  <action>
    First run the local quality gates and confirm each exits 0 with the Phase 9/10 code integrated: `npm run lint`, `npm test`, then `npm run build` (which triggers `prebuild` check-resume-pdf + check-resume-docx and `postbuild` `node scripts/check-placeholders.mjs` automatically — the INFRA-05 placeholder grep). All four must exit 0; if any fails, fix the failure before proceeding (do not mask it). This satisfies DEPLOY-V11-03's local-gate portion.

    Then author `11-PROVISIONING-GUIDE.md` — an owner-facing step-by-step the human follows because Claude cannot perform GitHub/Vercel dashboard auth. Cover: (a) creating a GitHub fine-grained Personal Access Token at github.com/settings/personal-access-tokens — resource owner = the owner's account, repository access = public repositories (read-only), permissions = repository metadata read-only only, NO write scopes, NO private-repo access, recommended short-to-moderate expiry; (b) copying the token value once at creation time (GitHub shows it only once); (c) adding it in the Vercel dashboard at Project Settings -> Environment Variables -> add key `GITHUB_TOKEN`, paste the value, select Production scope (per D-02 — dashboard, not CLI); (d) confirming presence afterward via `vercel env ls` (the key name appears for Production scope — D-03) WITHOUT ever printing or pasting the secret value into any file. State explicitly: the token value lives ONLY in the Vercel Production env scope and the owner's local git-ignored `.env.local` — it must NEVER be written into the repo, README, or any verification document. Implements D-01, D-02, D-03.

    Then author `11-SMOKE-VERIFICATION.md` — a Markdown scaffold with empty result slots for Plan 02 to fill: a section recording `vercel env ls` confirmation that the `GITHUB_TOKEN` key is present in Production scope (key name only, never the value — D-03); a section for the deploy (which production URL deployed, deploy trigger used); a smoke-test section with a table for at least one production project card on https://www.tatibekov.com/projects showing real stats — columns: project name, observed commit count, observed language(s), observed duration (D-06, D-07 — note the mobile-app repos validation-ledger-mobile / LooperMobile / jaytap-mobile / CarEx are the most likely public candidates, *-services / *-agentic repos may be private and degrade to no-strip by design); and an ISR-confirmation section with the exact back-to-back `curl` commands and an evidence slot. For the ISR check (D-08, Claude's Discretion on the exact invocation): provide two `curl -sI https://api.github.com/rate_limit -H "Authorization: Bearer <TOKEN>"` invocations run a few seconds apart, OR `curl -s https://api.github.com/rate_limit` with the token, comparing the `rate.remaining` value — the value must NOT decrement between the two calls when /projects is served from the 86400s ISR cache (a cached page makes zero GitHub API calls). Document that the owner runs these with their token in shell env, and that only the observed `remaining` numbers (not the token) get pasted into the verification doc. Compose the literal commands in the scaffold using a `<TOKEN>` placeholder. Implements D-06, D-07, D-08.
  </action>
  <verify>
    <automated>npm run build && test -f .planning/phases/11-deploy-smoke-verification/11-PROVISIONING-GUIDE.md && test -f .planning/phases/11-deploy-smoke-verification/11-SMOKE-VERIFICATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npm test` exits 0
    - `npm run build` exits 0, and `scripts/check-placeholders.mjs` (via postbuild) prints its clean message and exits 0
    - `11-PROVISIONING-GUIDE.md` exists, is at least 30 lines, and covers GitHub fine-grained PAT creation (read-only public-repo metadata scope), Vercel dashboard Production-scope env-var entry, and `vercel env ls` confirmation
    - `11-PROVISIONING-GUIDE.md` explicitly states the token value must never be written into the repo or any verification document
    - `11-SMOKE-VERIFICATION.md` exists, is at least 25 lines, and contains empty result slots for: `vercel env ls` key-presence confirmation, the deploy, a project-card stats table (commit count + language(s) + duration), and the back-to-back `curl` ISR evidence
    - No real token value appears in either authored file (only a `<TOKEN>` placeholder)
  </acceptance_criteria>
  <done>Local quality gates verified green; the owner has a precise provisioning guide and a ready-to-fill verification scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo / version control | Anything committed to git is public on github.com/beckinfonet/personal-portfolio-web — a secret committed here is permanently leaked |
| documentation files | README.md and the phase verification docs are committed and public — they describe the token, never hold its value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information disclosure | `GITHUB_TOKEN` value in a committed file | mitigate | README, provisioning guide, and verification scaffold describe the token's purpose/scope only; acceptance criteria grep-assert no real token value appears; the scaffold uses a literal `<TOKEN>` placeholder for the curl commands |
| T-11-02 | Information disclosure | `.env.example` re-introducing a tracked env file | mitigate | `git rm .env.example` removes the only tracked env file; `.gitignore` already excludes `.env`, `.env.local`, `.env*.local` so `.env.local` (which holds the real local token) is never committable |
| T-11-03 | Elevation of privilege | Over-scoped PAT | mitigate | Provisioning guide mandates a fine-grained PAT with read-only public-repo metadata scope only (D-01) — no write, no private access; least privilege limits blast radius if the token leaks |
</threat_model>

<verification>
- `grep` confirms README.md contains `GITHUB_TOKEN` and contains neither `.env.example` nor `cp .env.example`
- `git ls-files` confirms `.env.example` is no longer tracked
- `npm run lint`, `npm test`, `npm run build` (with prebuild + postbuild) all exit 0
- Both phase-directory documents exist with the required sections and no secret values
</verification>

<success_criteria>
README env/deploy docs fully inline all three env vars with `GITHUB_TOKEN`
documented (purpose, scope, absence consequence); `.env.example` removed in the
same commit; local quality gates green; owner provisioning guide and
smoke-verification scaffold authored — Plan 02 is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/11-deploy-smoke-verification/11-01-SUMMARY.md`
</output>
