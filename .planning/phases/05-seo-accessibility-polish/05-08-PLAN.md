---
id: 05-08
phase: 05-seo-accessibility-polish
plan: 08
type: execute
wave: 5
depends_on:
  - 05-02
  - 05-03
  - 05-04
  - 05-05
  - 05-06
  - 05-07
files_modified:
  - .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md
autonomous: false
requirements:
  - DEV-03
  - DEV-02
  - A11Y-03
  - SEO-03
status: pending
tags:
  - manual-verification
  - phase-close
  - seo
  - accessibility
must_haves:
  truths:
    - "curl -I http://localhost:3000/ returns 'x-built-with: nextjs-15-react-19' on every route (DEV-03)"
    - "view-source:http://localhost:3000 shows the 6-line lowercase HTML comment in <head> (DEV-02)"
    - "macOS Reduce Motion toggle (System Settings → Accessibility → Display → Reduce Motion) disables slideIn, cursor blink, drawer animation site-wide (A11Y-03)"
    - "Optional: Slack/LinkedIn unfurl preview (via ngrok tunnel) renders the matrix-accent OG card with the correct route.label per route (SEO-03c — defer-to-prod acceptable)"
    - ".planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md is populated with PASS/FAIL/DEFERRED-PHASE-7 verdicts for each gate"
  artifacts:
    - path: ".planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md"
      provides: "Phase 5 manual gate verdicts + final phase status"
      contains: "Verdict:"
  key_links:
    - from: "05-VERIFICATION.md"
      to: "05-VALIDATION.md §Manual-Only Verifications"
      via: "Each verdict references the requirement ID"
      pattern: "(DEV-03|DEV-02|A11Y-03|SEO-03)"
---

<objective>
Wave 5 (manual). Run the 4 manual verification gates that automated tests cannot cover authoritatively: curl-based header verification (DEV-03), view-source HTML comment in real browsers (DEV-02), macOS Reduce Motion OS toggle (A11Y-03), and (optional) third-party unfurl preview via ngrok tunnel (SEO-03c).

This plan closes Phase 5 by populating `.planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md` — the audit trail of pass/fail/deferred verdicts that `/gsd-verify-work` consumes. Phase 5 verdict is PASS only when all 4 mandatory gates are PASS or explicitly DEFERRED-PHASE-7 (per CONTEXT.md `<deferred>` table).

Output: 1 file — `05-VERIFICATION.md`. No source-code changes; this is the human review wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-seo-accessibility-polish/05-CONTEXT.md
@.planning/phases/05-seo-accessibility-polish/05-VALIDATION.md
@.planning/phases/05-seo-accessibility-polish/05-RESEARCH.md
@.planning/phases/04-mobile-responsive/04-VERIFICATION.md

<interfaces>
<!-- Reference for what 05-VERIFICATION.md should look like. -->

From .planning/phases/04-mobile-responsive/04-VERIFICATION.md (the precedent — Phase 4 closed with this file format and the resulting verdict propagated to STATE.md + ROADMAP.md):
- Front-matter: phase, status, verdict, completed_at
- Per-gate sections: Gate ID, requirement ID, what to verify, how to verify, observed result, verdict (PASS / FAIL / DEFERRED-PHASE-N)
- Final §"Phase Verdict" section with PASS / FAIL summary
- Tail §"Carry-forwards" listing items deferred to later phases
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify x-built-with HTTP header on every route (DEV-03)</name>
  <what-built>
    - Phase 1 added `x-built-with: nextjs-15-react-19` to next.config.ts engineerHeaders
    - Plan 05-01 Task 3 added scripts/check-headers.mjs as a source-level smoke check (already passing)
    - This gate is the runtime verification that Next.js actually emits the header in HTTP responses
  </what-built>
  <how-to-verify>
    1. Start the dev server in one terminal: `npm run dev`. Wait for "Ready in Xs".

    2. In another terminal, verify the header on every route:
       ```bash
       for path in / /projects /stack /experience /writing /contact /shipped; do
         echo "=== $path ==="
         curl -sI "http://localhost:3000$path" | grep -i 'x-built-with'
       done
       ```

    3. Expected output: `x-built-with: nextjs-15-react-19` (or close variant — header names are case-insensitive in HTTP) for ALL 7 routes.

    4. Confirm the deferred header is NOT present (per CONTEXT.md D-30):
       ```bash
       curl -sI http://localhost:3000/ | grep -i 'x-portfolio-source'
       ```
       Expected: empty result. The `x-portfolio-source` header value is intentionally deferred to Phase 7 when the production URL is finalized.

    5. Record verdict for DEV-03:
       - PASS: all 7 routes return `x-built-with: nextjs-15-react-19`
       - FAIL: any route is missing the header — open `next.config.ts`, verify `engineerHeaders` array includes `{ key: "x-built-with", value: "nextjs-15-react-19" }`, restart dev server
       - DEFERRED-PHASE-7: `x-portfolio-source` not yet present — expected per D-30; the slot will be filled in Phase 7
  </how-to-verify>
  <resume-signal>Type `pass: dev-03` (all 7 routes return the header) OR describe failures</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 6-line HTML comment in view-source (DEV-02 cross-browser)</name>
  <what-built>
    - Plan 05-05 shipped `<HeadComment />` RSC mounted in app/layout.tsx <head>
    - The comment uses `<noscript dangerouslySetInnerHTML>` per RESEARCH Pattern 9 (Assumption A1)
    - scripts/check-head-comment.mjs verifies source-level presence; this gate verifies real browser rendering
  </what-built>
  <how-to-verify>
    1. Dev server running on `http://localhost:3000`.

    2. Open Chrome and navigate to `view-source:http://localhost:3000`. Search (Ctrl/Cmd + F) for `hello, you found`. Expected: 6 consecutive lines visible in `<head>`:
       ```html
       <!--
         hello, you found the source.
         i build with: typescript, react, nextjs, swift, aws.
         open to: senior engineering roles, ai/agentic systems, mobile.
         reach: beckprograms@gmail.com
         github: beckinfonet
         thanks for looking. — bakytbek
       -->
       ```

    3. Verify the comment is INSIDE `<head>` (search location: scroll up — should appear after the auto-emitted `<meta charSet>` and before the closing `</head>`).

    4. Open Firefox and repeat steps 2-3 (RESEARCH Assumption A1 explicitly requires Firefox verification because the `<noscript>` host preservation can vary between engines).

    5. Optional but recommended: open Safari and repeat (third browser engine).

    6. CLI sanity (one-liner):
       ```bash
       curl -s http://localhost:3000 | grep -c "hello, you found the source"
       ```
       Expected: `1` (the comment appears exactly once in the rendered HTML).

    7. Record verdict for DEV-02:
       - PASS: comment visible in view-source on Chrome AND Firefox (Safari optional)
       - FAIL: comment missing in any browser — Plan 05-05 Task 3 implementation needs revisit; Pattern 9 §Option B has the alternative `<script type="text/x-html-comment">` host
  </how-to-verify>
  <resume-signal>Type `pass: dev-02` (comment visible in Chrome + Firefox) OR describe browser-specific failures</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify macOS Reduce Motion toggle disables animations site-wide (A11Y-03)</name>
  <what-built>
    - Plan 05-03 Task 2 appended the universal-selector reduced-motion reset to globals.css (D-16)
    - Existing targeted rules preserved (D-17): .cursor, .content-block, .breadcrumb-hint, .drawer-sheet, .drawer-backdrop, [cmdk-dialog]
    - 0.01ms timings (NOT 0ms) per Pitfall 7
  </what-built>
  <how-to-verify>
    1. macOS only: System Settings → Accessibility → Display → toggle "Reduce motion" ON. (On Linux/Windows, use OS accessibility settings to enable the equivalent prefers-reduced-motion preference.)

    2. With dev server running, hard-reload `http://localhost:3000` (Cmd+Shift+R / Ctrl+Shift+R). Observe:
       - The 0.25s `slideIn` micro-animation on `.content-block` should NOT play — content body appears instantly
       - The cursor block (8×14px accent) should be VISIBLE (per D-18) but NOT blinking
       - Drawer slide animations (visible at <=960px viewport with hamburger trigger) should be instant — no slide-in transition

    3. Navigate to each route in succession (`/`, `/projects`, `/stack`, `/experience`, `/writing`, `/contact`, `/shipped`). On each route, the prompt-line `slideIn` should not animate.

    4. Open ⌘K command palette. The `[cmdk-dialog]` open animation should be instant.

    5. Resize the viewport below 960px and toggle the hamburger drawer. The drawer should appear/disappear without slide animation.

    6. Toggle Reduce Motion OFF. Hard-reload. Confirm animations RESUME — slideIn plays, cursor blinks, drawer slides. (Sanity check that the CSS isn't unconditionally disabling animations.)

    7. Record verdict for A11Y-03:
       - PASS: all 5 animation surfaces (slideIn, cursor blink, drawer slide, palette open, breadcrumb-hint transition) respect Reduce Motion across all 7 routes
       - FAIL: any animation still plays at full duration — re-verify globals.css universal-selector reset (Plan 05-03 Task 2) and check `npm run check:reduced-motion` (the Wave 0 smoke); if smoke passes but visual fails, the `0.01ms !important` override may have a specificity conflict elsewhere — surface the failing selector
  </how-to-verify>
  <resume-signal>Type `pass: a11y-03` (Reduce Motion disables all animations site-wide) OR describe which animations still play</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="optional">
  <name>Task 4: Optional — Slack/LinkedIn unfurl preview via ngrok (SEO-03c)</name>
  <what-built>
    - Plan 05-02 shipped 8 OG image PNGs (root + 7 routes) via next/og ImageResponse
    - Each card renders the matrix-accent template with route.label
    - This gate validates third-party scraper rendering — defer-to-prod acceptable per CONTEXT.md `<deferred>`
  </what-built>
  <how-to-verify>
    Per RESEARCH §Code Examples §"Local OG verification" — this gate is OPTIONAL because:
    - Localhost is not publicly reachable; tunneling is required
    - Phase 7 DEPLOY-04 covers the production unfurl preview definitively
    - 04-VERIFICATION.md precedent allows DEFERRED-PHASE-7 with manual reviewer authorization

    To run the optional gate:

    1. With dev server running on localhost:3000, install and start ngrok:
       ```bash
       brew install --cask ngrok    # if not already installed
       ngrok http 3000
       ```
       Note the public URL (e.g., `https://abc123.ngrok-free.app`).

    2. Open LinkedIn Post Inspector: https://www.linkedin.com/post-inspector/
       Paste the public URL. Click "Inspect". Expected: matrix-accent OG card with `Bakytbek Tatibekov` + `// Sr. Software Engineer` + `~/portfolio/about.md`.

    3. Repeat for each per-route URL (paste `<ngrok-url>/projects`, `<ngrok-url>/stack`, etc.). Each should show the matching `route.label` in the bottom-left.

    4. Optional: post the URL to a private Slack channel and observe the unfurl. Slack and LinkedIn cache aggressively; clear caches if testing twice within a few minutes.

    5. Record verdict for SEO-03c:
       - PASS: unfurl preview renders correctly on LinkedIn (and optionally Slack) for at least the root route
       - DEFERRED-PHASE-7: tunneling not run in this session — Phase 7 DEPLOY-04 covers final production verification; this is acceptable per the explicit defer-to-prod allowance
       - FAIL: unfurl preview shows broken image / wrong content / nothing — open `app/opengraph-image.tsx` and re-verify Pitfall 1 (no CSS variables), Pitfall 2 (display: flex everywhere), Pitfall 4 (font binary path)

    Default outcome if reviewer skips: DEFERRED-PHASE-7 (matches the `<deferred>` allowance).
  </how-to-verify>
  <resume-signal>Type `pass: seo-03c` (unfurl preview correct) OR `deferred: seo-03c` (skipping in favor of Phase 7) OR describe failures</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Populate 05-VERIFICATION.md with verdicts</name>
  <read_first>
    - Task 1, 2, 3, 4 resume signals
    - .planning/phases/04-mobile-responsive/04-VERIFICATION.md (precedent format)
    - .planning/phases/05-seo-accessibility-polish/05-VALIDATION.md §Manual-Only Verifications + Validation Sign-Off
    - .planning/STATE.md current position
  </read_first>
  <files>.planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md` with this structure:

    ```markdown
    ---
    phase: 5
    slug: seo-accessibility-polish
    status: complete
    verdict: PASS | FAIL
    completed_at: <ISO 8601 today>
    plan_count: 8
    plans_complete: 8
    requirement_coverage:
      - SEO-01: COMPLETE (Plan 05-03)
      - SEO-02: COMPLETE (Plan 05-05)
      - SEO-03: COMPLETE (Plan 05-02)
      - SEO-04: COMPLETE (Plans 05-02 + 05-03)
      - A11Y-03: COMPLETE (Plan 05-03 + this manual gate)
      - A11Y-07: COMPLETE (Plan 05-07; 56/56 axe cells green)
      - DEV-01: COMPLETE (Plan 05-06)
      - DEV-02: COMPLETE (Plan 05-05 + this manual gate)
      - DEV-03: COMPLETE (Plan 05-08; verification only — Phase 1 shipped)
    ---

    # Phase 5: SEO + Accessibility Polish — Verification

    ## Phase Verdict

    **Verdict: <PASS | FAIL>**

    Resolved: <count> manual gates as <PASS-count>/<DEFERRED-count>/<FAIL-count>

    ## Manual Gate Verdicts

    ### Gate 1 — DEV-03: x-built-with HTTP header

    **Verdict:** <PASS | FAIL>

    **Method:** `for path in / /projects /stack /experience /writing /contact /shipped; do curl -sI http://localhost:3000$path | grep -i 'x-built-with'; done`

    **Observed:** <paste the 7 returned header lines OR document the failure>

    **Notes:** `x-portfolio-source` intentionally absent — DEFERRED-PHASE-7 per CONTEXT.md D-30.

    ### Gate 2 — DEV-02: 6-line HTML comment in view-source

    **Verdict:** <PASS | FAIL>

    **Method:** Opened `view-source:http://localhost:3000` in Chrome and Firefox; searched for `hello, you found`.

    **Observed:** <Chrome status; Firefox status; optional Safari status>

    ### Gate 3 — A11Y-03: macOS Reduce Motion disables animations

    **Verdict:** <PASS | FAIL>

    **Method:** Toggled System Settings → Accessibility → Display → Reduce motion ON; hard-reloaded each of 7 routes; observed slideIn, cursor blink, drawer slide, palette open animations; toggled OFF for sanity reverse.

    **Observed:** <which animations stopped vs. continued>

    ### Gate 4 — SEO-03c: Slack/LinkedIn unfurl preview (optional)

    **Verdict:** <PASS | DEFERRED-PHASE-7 | FAIL>

    **Method:** <ngrok tunnel + LinkedIn Post Inspector OR skipped per defer-to-prod allowance>

    **Observed:** <unfurl renders correctly OR explicitly deferred to Phase 7 DEPLOY-04>

    ## Automated Gates (recap from earlier waves)

    All automated checks were green at the close of each plan; this section is a recap, not a re-execution.

    | Gate | Source | Status |
    |------|--------|--------|
    | `npm test` (Vitest, ~5s) | Plans 05-01 through 05-06 | GREEN |
    | `npm run test:contrast` (56 cells) | Plan 05-07 | GREEN |
    | `node scripts/check-og-files.mjs` | Plan 05-02 | GREEN |
    | `node scripts/check-reduced-motion.mjs` | Plan 05-03 | GREEN |
    | `node scripts/check-head-comment.mjs` | Plan 05-05 | GREEN |
    | `node scripts/check-headers.mjs` | Plan 05-01 (Phase 1 header) | GREEN |
    | `node scripts/check-placeholders.mjs` (postbuild) | Phase 1 | GREEN (no TODO strings introduced) |
    | `npm run lint` | Phase 1 | GREEN |
    | `npm run typecheck` (`tsc --noEmit`) | Phase 1 | GREEN |
    | `npm run build` | All plans | GREEN |
    | `npm run knip` | Phase 1 | GREEN (no orphan exports) |

    ## Carry-forwards to Phase 6

    - JSON-LD `description`, `image`, `address` fields — Phase 6 (CONTENT-01 bio, CONTENT-05 resume, profile photo)
    - 3rd-social pick (Mastodon vs Bluesky vs X) — Phase 6 content decision; sameAs filter auto-handles whatever lands
    - `twitter.creator` / `twitter.site` handle — Phase 6 (depends on 3rd-social pick)
    - Real bio for `description` field — Phase 6 (CONTENT-01)

    ## Carry-forwards to Phase 7

    - `x-portfolio-source` HTTP header value — Phase 7 (DEPLOY-* — needs production deploy URL)
    - Slack/LinkedIn unfurl preview against the real production URL — Phase 7 (DEPLOY-04)
    - Lighthouse SEO ≥ 95 on production — Phase 7 (DEPLOY-02)
    - Search Console sitemap submission + indexing — Phase 7 (DEPLOY-03)
    - Real-device iPhone Safari + Android Chrome reduce-motion verification — Phase 7 (DEPLOY-04 + DEPLOY-07; carries forward Phase 4 gates 7+8)

    ## Sign-off

    - [ ] All 4 manual gates resolved (PASS or explicitly DEFERRED-PHASE-7)
    - [ ] STATE.md updated with phase 5 close
    - [ ] ROADMAP.md Phase 5 row marked complete
    - [ ] REQUIREMENTS.md SEO-01..04, A11Y-03, A11Y-07, DEV-01..03 status flipped to "Complete (Phase 5)"

    **Approval:** <Bakytbek's sign-off line>
    ```

    Fill in the actual verdicts from Tasks 1-4 (PASS/FAIL/DEFERRED). The phase verdict is:
    - **PASS** if all 3 mandatory gates (Task 1, 2, 3) are PASS — Task 4 may be DEFERRED-PHASE-7
    - **FAIL** if any of the 3 mandatory gates is FAIL — surface the failure for remediation before phase close

    DO NOT update STATE.md or ROADMAP.md in this task; that's the orchestrator's responsibility post-`/gsd-verify-work`. This task only creates the audit document.

    DO NOT introduce literal `TODO` (uppercase) anywhere in the file (Pitfall 9 / INFRA-05). The file is markdown so it doesn't ship in the build, but the discipline is consistent.
  </action>
  <verify>
    <automated>
    test -f .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md &amp;&amp; grep -q 'verdict:' .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md &amp;&amp; grep -q 'DEV-03' .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md &amp;&amp; grep -q 'DEV-02' .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md &amp;&amp; grep -q 'A11Y-03' .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md &amp;&amp; grep -q 'SEO-03c' .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md &amp;&amp; grep -q 'Carry-forwards to Phase 6' .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md &amp;&amp; grep -q 'Carry-forwards to Phase 7' .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md &amp;&amp; ! grep -E '\bTODO\b' .planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md` exists with valid YAML front-matter
    - File contains explicit verdict (PASS or FAIL) at the phase level
    - File contains a verdict for each of the 4 manual gates (Tasks 1-4)
    - File contains §"Carry-forwards to Phase 6" listing JSON-LD enrichment items + 3rd-social
    - File contains §"Carry-forwards to Phase 7" listing x-portfolio-source value, unfurl preview against production, Lighthouse, Search Console
    - File does NOT contain literal `TODO` (uppercase)
    - Final `npm test && npm run test:contrast` green at the time of file authorship (defensive recap; both should already be green from earlier waves)
  </acceptance_criteria>
  <done>05-VERIFICATION.md authored with verdicts from Tasks 1-4; phase verdict explicitly stated; carry-forwards documented for Phase 6 and Phase 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual reviewer judgment vs. automated gate | The 4 manual gates exist BECAUSE automated gates cannot definitively check the behavior (real OS reduce-motion preference; cross-browser view-source preservation; HTTP header on actual response; third-party scraper rendering). Manual judgment is load-bearing. |
| ngrok tunnel exposure | Optional gate (Task 4) uses ngrok to publicly expose localhost. The tunnel is short-lived and the dev server has no secrets — but the dev server's full surface is reachable for the tunnel duration. Reviewer must close the tunnel after the unfurl preview test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-28 | Repudiation | Phase verdict without audit trail | mitigate | Task 5 explicitly creates `05-VERIFICATION.md` with per-gate verdicts AND a recap of automated gate state. Matches the Phase 4 precedent (`04-VERIFICATION.md` is the gold standard). |
| T-05-29 | Information Disclosure | ngrok tunnel exposure | mitigate | Task 4 is OPTIONAL with explicit DEFERRED-PHASE-7 allowance. Reviewer guidance includes "close the tunnel after the unfurl preview test." Phase 7 DEPLOY-04 covers the production-URL unfurl test definitively. |
| T-05-30 | Spoofing | Manual reviewer attesting without testing | accept | The reviewer is the project owner (Bakytbek). Phase 4 verdict precedent shows the reviewer follows the `<how-to-verify>` instructions in good faith; this is a single-developer project with no adversarial role. |
</threat_model>

<verification>
- All 3 mandatory manual gates (DEV-03, DEV-02, A11Y-03) resolved with PASS or FAIL verdict
- Optional manual gate (SEO-03c) resolved with PASS, DEFERRED-PHASE-7, or FAIL verdict
- `.planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md` exists with valid front-matter and per-gate sections
- Phase verdict (PASS / FAIL) is explicitly stated and is consistent with the gate verdicts:
  - PASS = all 3 mandatory gates PASS (optional may be DEFERRED-PHASE-7)
  - FAIL = any mandatory gate FAIL
- File documents carry-forwards to Phase 6 and Phase 7
- No literal `TODO` (uppercase) in the verification file
- Recap of automated gates documents the green state across `npm test`, `npm run test:contrast`, all 4 smoke scripts, lint, typecheck, build, knip
</verification>

<success_criteria>
Plan 05-08 (and Phase 5 overall) is complete when:
1. DEV-03 manual gate verdict is PASS (`x-built-with: nextjs-15-react-19` returned on all 7 routes via curl)
2. DEV-02 manual gate verdict is PASS (6-line HTML comment visible in view-source on Chrome AND Firefox)
3. A11Y-03 manual gate verdict is PASS (macOS Reduce Motion toggle disables slideIn, cursor blink, drawer slide, palette open across all 7 routes)
4. SEO-03c manual gate verdict is PASS, DEFERRED-PHASE-7, or FAIL (optional — defer-to-prod allowance)
5. `.planning/phases/05-seo-accessibility-polish/05-VERIFICATION.md` is authored with the verdict + carry-forwards
6. Phase 5 verdict is explicitly stated as PASS or FAIL based on the 3 mandatory gates
7. No literal `TODO` (uppercase) introduced in the verification file
8. The orchestrator's downstream `/gsd-verify-work` and STATE.md / ROADMAP.md updates can proceed against the audit trail
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-accessibility-polish/05-08-SUMMARY.md` referencing 05-VERIFICATION.md as the audit-trail artifact
</output>
