---
phase: 04-mobile-responsive
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/04-mobile-responsive/04-VERIFICATION.md
autonomous: false
requirements: [MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04, MOBILE-05, PALETTE-05, A11Y-09]
requirements_addressed: [MOBILE-01, MOBILE-02, MOBILE-03, MOBILE-04, MOBILE-05, PALETTE-05, A11Y-09]
tags: [manual-verification, screenshot-review, print-preview, recruiter-dry-run, phase-4]

must_haves:
  truths:
    - "Cross-viewport screenshot review at 375px / 768px / 1024px confirms drawer + palette + STATUS visibility rules and the persistent top-bar resume button on `/about` above the fold."
    - "Print preview review on all 7 views confirms white background, black text, serif body, hidden chrome, monospace carve-outs, and the URL+email print footer."
    - "5-second recruiter dry-run on 375px localhost is recorded with a pass/fail and any noted friction."
    - "VERIFICATION.md captures every manual gate result with a date, viewport, and observation."
    - "Full automated battery (npm test + lint + typecheck + 3 audit scripts) is green before manual gates begin."
  artifacts:
    - path: ".planning/phases/04-mobile-responsive/04-VERIFICATION.md"
      provides: "Manual verification log for Phase 4"
      contains: "## Manual Gates"
  key_links:
    - from: "04-VALIDATION.md §Manual-Only Verifications (rows 1-9)"
      to: "04-VERIFICATION.md gate evidence rows"
      via: "1:1 mapping for each of 9 manual gates"
      pattern: "Gate #"
---

<objective>
Drive the Phase 4 manual verification flow: confirm the full automated battery is green, then walk through the 9 manual gates from `04-VALIDATION.md` (375px / 768px / 1024px screenshot review of the about route + drawer + palette + STATUS, print preview on all 7 routes, prefers-reduced-motion verification, real-device or 375-DevTools recruiter dry-run) and record evidence in `04-VERIFICATION.md`. This is the gate plan that closes Phase 4 and unblocks `/gsd-verify-work`.

Purpose: Phase 4's surface area is mostly visual and CSS-driven, which means jsdom unit tests cannot prove the contract. The manual gates from VALIDATION.md are the actual exit criterion for the phase. This plan codifies the recording format so Phase 7 (production deploy + recruiter test) can pick up where Phase 4 left off without re-doing the dry-run.

Output: A populated `.planning/phases/04-mobile-responsive/04-VERIFICATION.md` with a result for each of the 9 manual gates, per-route print preview observations, and a final pass/fail summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mobile-responsive/04-CONTEXT.md
@.planning/phases/04-mobile-responsive/04-VALIDATION.md
@.planning/phases/04-mobile-responsive/04-UI-SPEC.md
@.planning/phases/04-mobile-responsive/04-01-PLAN.md
@.planning/phases/04-mobile-responsive/04-02-PLAN.md
@.planning/phases/04-mobile-responsive/04-03-PLAN.md
@.planning/phases/04-mobile-responsive/04-04-PLAN.md

<interfaces>
<!-- Phase 4 surfaces under test (verbatim from 04-VALIDATION.md §Manual-Only Verifications) -->

| # | Behavior | Requirement |
|---|----------|-------------|
| 1 | Resume CTA above the fold at 375×667 on `/` (no scroll) | MOBILE-03 |
| 2 | Drawer slides up from bottom at <=960px | MOBILE-02 |
| 3 | Mobile palette opens as bottom-sheet anchored to viewport bottom; sticky input | PALETTE-05 |
| 4 | STATUS visible only on `/` at <=960px AND only in sidebar at >=961px (no double-render) | MOBILE-04 |
| 5 | Print preview legibility on all 7 routes (white bg, black text, serif body, URL+email footer, no chrome) | A11Y-09 |
| 6 | `prefers-reduced-motion` honored — drawer + palette open without slide | A11Y-09 (motion baseline) |
| 7 | iOS Safari real-device validation: dvh/svh clipping, soft keyboard, address bar | MOBILE-02 / PALETTE-05 |
| 8 | Android Chrome real-device validation | MOBILE-02 / PALETTE-05 |
| 9 | 5-second recruiter dry-run on 375px localhost | MOBILE-01..05 / SHELL-03 |
</interfaces>

<patterns>
@.planning/codebase/CONVENTIONS.md
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated battery and confirm zero failures</name>
  <files>(none — read-only validation)</files>
  <read_first>
    - .planning/phases/04-mobile-responsive/04-VALIDATION.md §"Sampling Rate" (full suite command)
  </read_first>
  <action>
Execute, in this order, and capture the exit code of each:

```bash
npm run lint
npm run typecheck
npm test
node scripts/check-sidebar-redistribution.mjs
node scripts/check-print-rules.mjs
node scripts/check-mobile-palette-css.mjs
npm run build
```

If any step exits non-zero, STOP. Do NOT proceed to manual gates. Report the failing step to the user; the executor for the prior plan should fix the regression before this manual review continues.

If all steps exit 0, proceed to Task 2.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test && node scripts/check-sidebar-redistribution.mjs && node scripts/check-print-rules.mjs && node scripts/check-mobile-palette-css.mjs && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Each of `lint`, `typecheck`, `test`, the three audit scripts, and `build` exits 0.
    - The combined chain `npm run lint && npm run typecheck && npm test && node scripts/check-sidebar-redistribution.mjs && node scripts/check-print-rules.mjs && node scripts/check-mobile-palette-css.mjs && npm run build` exits 0.
    - The total wall-clock for the chain is < 60s on a typical dev machine (Phase 4 spec count is small).
  </acceptance_criteria>
  <done>Full automated battery green; manual gates can begin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual screenshot review at 375 / 768 / 1024 viewports + drawer/palette behavior</name>
  <what-built>Plan 04-01 CSS + Plan 04-02 ExplorerDrawer/TopBar hamburger + Plan 04-03 StatusBlock rehome + Plan 04-04 PrintFooter (mounted in layout). All Phase 4 surfaces are now in place at <=960px and at >=961px.</what-built>
  <how-to-verify>
1. Start dev: `npm run dev` (serves at http://localhost:3000).
2. Open DevTools → Device Toolbar → set viewport to **iPhone SE 375×667**.
3. Visit `/` (about):
   - **Gate 1 (MOBILE-03)**: Confirm the top-bar `↓ resume.pdf` button is visible above the fold WITHOUT scrolling. Take a screenshot.
   - Tap the `☰` hamburger:
     - **Gate 2 (MOBILE-02)**: Confirm the drawer slides up from the bottom, backdrop dims, sheet anchored to bottom edge with `border-radius: 12px 12px 0 0`. Take a screenshot.
     - Confirm the recruiter "↓ resume.pdf" card is rendered inside the drawer.
     - Tap a backdrop area: drawer closes.
     - Reopen, tap a file row (e.g. "Contact information"): drawer closes AND route navigates.
   - Open the palette via the `⌘K` button (or Cmd-K shortcut):
     - **Gate 3 (PALETTE-05)**: Confirm the palette opens as a bottom-sheet anchored to the viewport bottom, search input sticky at top, ~80vh height. Type "contact" — list filters. Take a screenshot.
   - **Gate 4a (MOBILE-04)**: Scroll to the bottom of `/about` — confirm the STATUS block (`● Available for hire`, `uptime: ...`, `tz: ...`) is visible near the bottom of the view body.
4. Visit `/projects`, `/stack`, `/experience`, `/writing`, `/contact`, `/shipped` (all 6 non-about routes at 375px):
   - **Gate 4b (MOBILE-04)**: Confirm STATUS is NOT rendered on any of these routes.
5. Switch viewport to **768×1024 (iPad portrait)**:
   - Confirm the same mobile UX is active: sidebar hidden, hamburger visible, drawer renders as bottom-sheet, palette renders as bottom-sheet, STATUS on `/about` visible.
6. Switch viewport to **1024×768 (iPad landscape / desktop)**:
   - Confirm the desktop UX is restored: sidebar visible (240px), STATUS visible inside sidebar, palette opens as a centered modal at 15vh, hamburger button is hidden.
   - **Gate 4c (MOBILE-04)**: Confirm there is NO double-render of STATUS — it appears in the sidebar but NOT in the about-view body.
7. **Gate 6 (prefers-reduced-motion)**: Open macOS System Settings → Accessibility → Display → Reduce Motion ON. Reload the page at 375px. Open the drawer and the palette: animations should NOT slide; surfaces appear without the 200ms transition. Disable Reduce Motion afterward.

Record each gate result + a short observation in `.planning/phases/04-mobile-responsive/04-VERIFICATION.md` (template provided in Task 4). Type "approved" if all 6 gates pass; describe any issues otherwise.
  </how-to-verify>
  <resume-signal>Type "approved" if Gates 1-4 + Gate 6 pass. If any gate fails, describe the failing gate, the viewport, and the observed vs expected behavior — the executor will replan.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Print preview review on all 7 routes (Gate 5)</name>
  <what-built>Plan 04-01 @media print stylesheet + Plan 04-04 PrintFooter mounted in layout. Print contract: white bg, black text, serif body (Georgia), monospace carve-outs for code-block content, hidden top-bar/sidebar/drawer/palette/breadcrumb/cursor/live-clock, page-break-inside safety on .print-footer + .shell-footer, URL+email footer at end.</what-built>
  <how-to-verify>
At the dev server (`npm run dev`, http://localhost:3000), for EACH of the 7 routes — `/`, `/projects`, `/stack`, `/experience`, `/writing`, `/contact`, `/shipped` — open the browser print dialog (Cmd+P / Ctrl+P) and review the preview pane. For each route confirm:

- **Background**: pure white. No dark theme bleed.
- **Body text**: black, serif (Georgia or "Times New Roman" fallback). No JetBrains Mono on prose.
- **Code-block content** (where applicable):
  - `/stack`: the JSON pre-block stays monospace with tonal hierarchy preserved (greys for muted/punctuation; black for keys/values).
  - `/experience`: hex-hash retains monospace; role + summary use serif.
  - `/projects`: tech-chips stay monospace.
  - All views: prompt-line `$ <command>` stays monospace.
- **Hidden chrome**: NO top-bar, NO sidebar, NO drawer, NO palette overlay, NO breadcrumb, NO skip-link, NO live-clock, NO cursor.
- **Print footer**: at the very bottom of the printable page, an `<aside>` reads `{site URL} · {email}` (e.g. `http://localhost:3000 · beckprograms@gmail.com`). It does NOT split across pages.
- **Layout**: full-bleed (terminal-main is 100% width with 16px padding).

For long views (e.g. populated experience.log, future writing posts) confirm pages flow naturally without `page-break-inside: avoid` cutting view content.

Record each route result + observations in `04-VERIFICATION.md` Gate 5 sub-section (Task 4 template).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 routes print legibly with the contract above. If any route violates the contract, describe the route + the observed deviation — the executor will replan a CSS amendment.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 5-second recruiter dry-run on 375px localhost + populate 04-VERIFICATION.md</name>
  <what-built>Phase 4 complete: at 375px the persistent top-bar resume button is visible above the fold; the about-view STATUS block surfaces the `Available for hire` trust signal; the drawer + palette deliver the full file-tree and verb-list; the recruiter card inside the drawer reinforces the resume affordance.</what-built>
  <how-to-verify>
1. **Recruiter dry-run (Gate 9)**: At 375px viewport on `npm run dev` http://localhost:3000:
   - Either hand the URL to a non-engineer (preferred), OR simulate by mentally clearing context and asking "where is the resume? where is the contact info?" while timing yourself.
   - Time-to-resume: target < 5 seconds (the persistent top-bar `↓ resume.pdf` button should be tap-resolvable instantly).
   - Time-to-contact: open the drawer or scroll to about-view socials/email — target < 10 seconds.
   - Record the result and any friction point in `04-VERIFICATION.md`.
   - Note: this is a DRY RUN. The real production-URL recruiter test is Phase 7 / DEPLOY-04.
2. **Real-device validation (Gates 7 + 8)** — OPTIONAL for Phase 4, REQUIRED for Phase 7:
   - If a physical iPhone (Safari) and Android device (Chrome) are available, repeat the 375px flow on each. Confirm `dvh`/`svh` doesn't clip the bottom-sheet, soft keyboard appears below the palette without obscuring the search input, and the address bar doesn't overlap the top bar.
   - If devices are NOT available, mark Gates 7 + 8 as "deferred to Phase 7" in 04-VERIFICATION.md (per 04-CONTEXT.md `<deferred>` — this is acceptable for Phase 4 close).
3. **Populate `04-VERIFICATION.md`** using this exact structure (create the file if it doesn't yet exist):

```markdown
# Phase 4 — Manual Verification Log

**Phase:** 4 — Mobile-Responsive
**Reviewer:** {your initials}
**Date:** {ISO date}
**Build:** {git rev-parse --short HEAD}

## Automated Battery

- npm run lint: PASS
- npm run typecheck: PASS
- npm test: PASS ({N} tests)
- node scripts/check-sidebar-redistribution.mjs: PASS
- node scripts/check-print-rules.mjs: PASS
- node scripts/check-mobile-palette-css.mjs: PASS
- npm run build: PASS

## Manual Gates

| Gate | Behavior | Requirement | Viewport / Method | Result | Notes |
|------|----------|-------------|-------------------|--------|-------|
| 1 | Resume CTA above the fold on `/` | MOBILE-03 | 375×667 (iPhone SE) | PASS / FAIL | {observation, screenshot ref} |
| 2 | Drawer slides up from bottom | MOBILE-02 | 375 / 768 | PASS / FAIL | {observation} |
| 3 | Mobile palette as bottom-sheet | PALETTE-05 | 375 / 768 | PASS / FAIL | {observation} |
| 4a | STATUS visible on `/` at <=960px | MOBILE-04 | 375 / 768 | PASS / FAIL | {observation} |
| 4b | STATUS NOT on 6 non-about routes at <=960px | MOBILE-04 | 375 (×6) | PASS / FAIL | {observation} |
| 4c | STATUS visible in sidebar only at >=961px (no double-render) | MOBILE-04 | 1024 | PASS / FAIL | {observation} |
| 5 | Print preview on all 7 routes | A11Y-09 | Cmd+P (per route) | PASS / FAIL | {per-route notes below} |
| 6 | prefers-reduced-motion disables drawer + palette slide | A11Y-09 (motion) | macOS Reduce Motion ON | PASS / FAIL | {observation} |
| 7 | iOS Safari real-device | MOBILE-02 / PALETTE-05 | iPhone Safari | PASS / FAIL / DEFERRED-PHASE-7 | {observation or deferral note} |
| 8 | Android Chrome real-device | MOBILE-02 / PALETTE-05 | Android Chrome | PASS / FAIL / DEFERRED-PHASE-7 | {observation or deferral note} |
| 9 | 5-second recruiter dry-run | MOBILE-01..05 / SHELL-03 | 375 localhost | PASS / FAIL | time-to-resume: {Ns}; time-to-contact: {Ns}; friction: {note} |

### Gate 5 — Print Preview Per-Route Detail

| Route | White bg | Serif body | Mono carve-outs | Chrome hidden | Print-footer | Result |
|-------|----------|------------|-----------------|---------------|--------------|--------|
| /            | y/n | y/n | y/n | y/n | y/n | PASS / FAIL |
| /projects    | y/n | y/n | y/n | y/n | y/n | PASS / FAIL |
| /stack       | y/n | y/n | y/n (JSON pre) | y/n | y/n | PASS / FAIL |
| /experience  | y/n | y/n | y/n (hex-hash) | y/n | y/n | PASS / FAIL |
| /writing     | y/n | y/n | y/n | y/n | y/n | PASS / FAIL |
| /contact     | y/n | y/n | y/n | y/n | y/n | PASS / FAIL |
| /shipped     | y/n | y/n | y/n | y/n | y/n | PASS / FAIL |

## Phase 4 Verdict

- All Phase 4 success criteria from ROADMAP §"Phase 4: Mobile-Responsive" satisfied: PASS / FAIL
- Carry-forward items for Phase 5: {list — e.g. axe-core 8-combination contrast audit}
- Carry-forward items for Phase 7: {list — e.g. real-device gates 7-8 if deferred, production recruiter test}

**Date closed:** {ISO date}
**Reviewer signature:** {initials}
```

Save the file at `.planning/phases/04-mobile-responsive/04-VERIFICATION.md`.
  </how-to-verify>
  <resume-signal>Type "approved" if 04-VERIFICATION.md is populated and the Phase 4 verdict reads PASS (with any deferrals correctly tagged for Phase 5 or Phase 7). If verdict is FAIL, describe which gate(s) failed — the planner will create a gap-closure plan via `/gsd-plan-phase 4 --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual review process → 04-VERIFICATION.md | Reviewer-controlled. Honest recording is a process integrity matter, not a security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | T (Tampering) | 04-VERIFICATION.md gate results | accept | Solo developer + Claude workflow; honest recording is a process matter, not a security threat. LOW. |
| T-04-14 | I (Information Disclosure) | Manual print preview leaks | mitigate | If a print preview review on a non-isolated machine could expose data, the reviewer is responsible for closing the preview; CSS already blocks `print-color-adjust: exact` (Plan 04-01 + audit script). LOW. |
</threat_model>

<verification>
- 04-VERIFICATION.md exists at `.planning/phases/04-mobile-responsive/04-VERIFICATION.md` with all 9 manual gate rows populated.
- Each gate has a PASS / FAIL / DEFERRED-PHASE-7 result with a short observation.
- Gate 5 has a per-route detail table covering all 7 routes.
- The "Phase 4 Verdict" section reads PASS with any carry-forwards correctly tagged.
- The full automated battery (Task 1) was green BEFORE manual gates began.
</verification>

<success_criteria>
All five ROADMAP Phase 4 success criteria are now PROVEN:
1. Resume CTA above the fold at 375px on `/` — Gate 1.
2. 240px sidebar collapses below ~960px into hamburger-triggered drawer + STATUS rehomed to about-view + recruiter resume card surfaced via top-bar — Gates 2 + 4a.
3. Mobile palette as bottom-sheet from mobile top bar with same verb list and type-to-filter — Gate 3.
4. `grep -E "display:\s*none" app/globals.css` for sidebar selectors paired with mobile-home rules — automated audit Plan 04-01 Task 3 + manual confirmation across viewports.
5. Print preview on all 7 routes — Gate 5 with per-route detail.

Phase 4 is structurally complete and ready for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-mobile-responsive/04-05-SUMMARY.md` with: pointer to 04-VERIFICATION.md, the populated automated-battery exit codes, the verdict (PASS/FAIL with any deferrals), and a transition note for Phase 5 (any contrast issues observed during manual review go into Phase 5 backlog as Pitfall 8 candidates).
</output>
