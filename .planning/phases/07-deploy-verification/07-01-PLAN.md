---
phase: 07-deploy-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/sitemap.ts
  - app/robots.ts
  - next.config.ts
  - .env.example
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DEPLOY-01
tags:
  - deploy
  - env-var
  - canonical-url
  - headers
user_setup:
  - service: vercel
    why: "NEXT_PUBLIC_SITE_URL is build-time inlined; must be set in Vercel Production scope before redeploy (D-03)"
    env_vars:
      - name: NEXT_PUBLIC_SITE_URL
        source: "Vercel Dashboard -> Project (portfolio-web) -> Settings -> Environment Variables -> Add New -> Production scope only"
        value: "https://www.tatibekov.com"
    dashboard_config:
      - task: "Set NEXT_PUBLIC_SITE_URL=https://www.tatibekov.com with Production scope checked (uncheck Preview + Development)"
        location: "Vercel Dashboard -> Project Settings -> Environment Variables"
must_haves:
  truths:
    - "All 7 routes return HTTP 200 on https://www.tatibekov.com after redeploy"
    - "sitemap.xml emits 7 <loc> entries pointing at https://www.tatibekov.com"
    - "robots.txt sitemap pointer resolves to https://www.tatibekov.com/sitemap.xml"
    - "metadataBase in served HTML resolves OG and canonical URLs to www.tatibekov.com (NOT localhost, NOT vercel.app)"
    - "x-built-with: nextjs-15-react-19 response header is present; x-portfolio-source is absent on every route"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Production-URL fallback in metadataBase resolution"
      contains: "https://www.tatibekov.com"
    - path: "next.config.ts"
      provides: "engineerHeaders array with x-built-with only"
      contains: "x-built-with"
    - path: ".env.example"
      provides: "Production-URL hint comment for developers"
      contains: "www.tatibekov.com"
  key_links:
    - from: "Vercel env var (NEXT_PUBLIC_SITE_URL)"
      to: "app/layout.tsx metadataBase"
      via: "process.env.NEXT_PUBLIC_SITE_URL || fallback (build-time inlined)"
      pattern: "metadataBase.*NEXT_PUBLIC_SITE_URL"
    - from: "Vercel env var (NEXT_PUBLIC_SITE_URL)"
      to: "app/sitemap.ts + app/robots.ts URL emission"
      via: "process.env.NEXT_PUBLIC_SITE_URL at render time"
      pattern: "NEXT_PUBLIC_SITE_URL"
    - from: "next.config.ts engineerHeaders"
      to: "HTTP response headers on every /:path* route"
      via: "headers() async function spread"
      pattern: "x-built-with"
---

<objective>
Flip the production canonical URL to https://www.tatibekov.com, drop the x-portfolio-source header slot per D-04 (closing Phase 5 D-30 carry-forward as RESOLVED-as-dropped), update REQUIREMENTS.md DEV-03 status, and redeploy. After redeploy, verify all 7 routes return HTTP 200 on the canonical host and that sitemap/robots emit www-prefixed URLs.

Purpose: DEPLOY-01 — production deploy live on the custom domain with NEXT_PUBLIC_SITE_URL set; canonical/OG/sitemap URLs all match the production origin so Google can de-duplicate the Vercel-hostname alias automatically.

Output: Source-code changes committed (4 files modified), Vercel env var set in Production scope, redeploy triggered via empty-commit, and curl-based verification confirming all 7 routes are 200 on www.tatibekov.com with x-built-with header present and x-portfolio-source absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-deploy-verification/07-CONTEXT.md
@.planning/phases/07-deploy-verification/07-RESEARCH.md
@.planning/phases/07-deploy-verification/07-PATTERNS.md
@.planning/phases/07-deploy-verification/07-VALIDATION.md

<interfaces>
<!-- Existing exports the executor must NOT change semantics of. Extracted from codebase. -->

From app/layout.tsx (current):
```ts
// Line 12 — fallback string is the only delta in this plan
const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000";
// Use logical OR (||) NOT nullish coalescing (??) — empty-string env vars must
// fall through to the production URL, not produce `new URL("")` → TypeError.
// Phase 1 D-Pitfall D — preserve this comment.

export const metadata: Metadata = {
  metadataBase: new URL(siteUrl),
  // ... other fields (title, description, openGraph, twitter) — DO NOT TOUCH ...
};
```

From app/sitemap.ts (current):
```ts
import { ROUTES } from "@/lib/routes";  // 7 entries, single source of truth
const baseUrl = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";
// Note: uses ?? here; layout.tsx uses ||. Phase 7 leaves this asymmetry alone (D-02 says verify, no change).
```

From next.config.ts (current lines 11-14):
```ts
const engineerHeaders = [
  // x-portfolio-source deferred to Phase 7 per D-13 (revised) — set when public deploy URL finalized.
  { key: "x-built-with", value: "nextjs-15-react-19" }
];
```
After Phase 7 D-04: drop the deferral comment; engineerHeaders stays a single-element array with x-built-with only.

From lib/routes.ts:
```ts
// 7 entries; pathnames: "/", "/projects", "/stack", "/experience", "/writing", "/contact", "/shipped"
```
</interfaces>

<canonical_refs>
- PATTERNS.md §`app/layout.tsx` (lines 54-126) — exact delta for metadataBase fallback + Pitfall D preservation
- PATTERNS.md §`next.config.ts` (lines 29-50) — line-12 deletion (the comment line) + array preserved
- PATTERNS.md §`.env.example` (lines 268-290) — comment-style hint pattern
- RESEARCH.md Pitfall 5 — NEXT_PUBLIC_* requires redeploy to take effect
- RESEARCH.md Code Examples lines 632-660 — canonical redeploy + verification flow
- CONTEXT.md D-01, D-02, D-03, D-04, D-05 — locked decisions for this plan
</canonical_refs>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set NEXT_PUBLIC_SITE_URL in Vercel Production env panel</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-CONTEXT.md (D-03 — Vercel Project Settings -> Environment Variables -> Production scope only)
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Pattern 3 (Pitfall 5 — build-time inlining requires redeploy)
  </read_first>
  <what-built>
    Nothing automatable here — Vercel's env-var panel has no CLI surface available in this workflow. The human user must perform the UI action before this plan can continue.
  </what-built>
  <how-to-verify>
    1. Open https://vercel.com -> Project "portfolio-web" -> Settings -> Environment Variables.
    2. Click "Add New".
    3. Key: NEXT_PUBLIC_SITE_URL
    4. Value: https://www.tatibekov.com
    5. Environments: check Production only (uncheck Preview and Development).
    6. Click Save.
    7. Confirm the row appears in the env-var list with "Production" badge and no other badges.
  </how-to-verify>
  <acceptance_criteria>
    - NEXT_PUBLIC_SITE_URL exists in Vercel Project Settings -> Environment Variables with value `https://www.tatibekov.com` and scope `Production` only.
    - No other scope (Preview, Development) is checked for this variable.
    - Screenshot of the env-var row saved to .planning/phases/07-deploy-verification/vercel/env-var-set.png (create the `vercel/` directory if needed).
  </acceptance_criteria>
  
  <action>
    See <how-to-verify> below — this is a manual human action with no CLI/API surface in scope. The executor agent halts here; the user performs the steps and signals resume.
  </action>
  <verify>
    <automated>MISSING — this checkpoint produces evidence (screenshot or stateful UI change) that the next automated task asserts. See acceptance_criteria for human verification.</automated>
  </verify>
  <done>
- NEXT_PUBLIC_SITE_URL exists in Vercel Project Settings -> Environment Variables with value `https://www.tatibekov.com` and scope `Production` only.
    - No other scope (Preview, Development) is checked for this variable.
    - Screenshot of the env-var row saved to .planning/phases/07-deploy-verification/vercel/env-var-set.png (create the `vercel/` directory if needed).
  </done>
  <resume-signal>Type "env-var set" with the screenshot path, or describe blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Source-code deltas — production fallback URL, drop x-portfolio-source slot, update .env.example</name>
  <read_first>
    - app/layout.tsx (current lines 10-24 — the metadataBase block)
    - next.config.ts (current lines 11-14 — engineerHeaders array with the deferral comment)
    - .env.example (current 2-line file)
    - .planning/phases/07-deploy-verification/07-PATTERNS.md §`app/layout.tsx`, §`next.config.ts`, §`.env.example` (exact deltas with conventions to preserve)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-02, D-03, D-04, D-05
  </read_first>
  <action>
    Three surgical edits, all in a single commit:

    Edit 1 — app/layout.tsx line 12: change the fallback string in the `siteUrl` constant from `"http://localhost:3000"` to `"https://www.tatibekov.com"`. Keep the `||` operator (not `??` — Phase 1 D-Pitfall D). Keep the two-line comment above it verbatim.

    Edit 2 — next.config.ts lines 11-14: delete line 12 entirely (the inline comment `// x-portfolio-source deferred to Phase 7 per D-13 (revised) — set when public deploy URL finalized.`). The `engineerHeaders` array collapses to a single-element array containing only the `{ key: "x-built-with", value: "nextjs-15-react-19" }` entry. Do NOT touch `securityHeaders`, do NOT touch the `headers()` function shape, do NOT add a new matcher.

    Edit 3 — .env.example: append two comment lines at the end (after line 2). Final file shape:
    Line 1: `NEXT_PUBLIC_API_BASE_URL=http://localhost:8080`
    Line 2: `NEXT_PUBLIC_SITE_URL=http://localhost:3000`
    Line 3: `# In production (Vercel Project Settings -> Environment Variables -> Production scope):`
    Line 4: `# NEXT_PUBLIC_SITE_URL=https://www.tatibekov.com`
    Keep dev value uncommented and active on line 2. No shell quoting. No trailing whitespace.

    Commit message (single commit covering all three edits + REQUIREMENTS.md status update from Task 3):
    `chore(07): canonicalize site URL to www.tatibekov.com + drop x-portfolio-source slot (DEPLOY-01, D-02/D-04, closes Ph5 D-30)`
  </action>
  <verify>
    <automated>grep -q 'https://www.tatibekov.com' app/layout.tsx && ! grep -q 'x-portfolio-source' next.config.ts && grep -q 'x-built-with' next.config.ts && grep -q '# NEXT_PUBLIC_SITE_URL=https://www.tatibekov.com' .env.example && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'https://www.tatibekov.com' app/layout.tsx` returns line 12 (the `siteUrl` fallback).
    - `grep 'x-portfolio-source' next.config.ts` returns zero lines.
    - `grep 'x-built-with' next.config.ts` returns one line (the surviving header).
    - `grep '||' app/layout.tsx` shows the `siteUrl =` line still uses `||` (not `??`).
    - `.env.example` has exactly 4 lines (or 4 + trailing newline): two active env vars then two comment lines.
    - `npm run lint` exits 0.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Three files modified with surgical edits matching the deltas; lint + typecheck both green; no other source files touched. NOT YET COMMITTED — Task 3 also lands in the same commit per brownfield discipline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md DEV-03 status to PASS-with-deviation and commit all source changes together</name>
  <read_first>
    - .planning/REQUIREMENTS.md (line 118 — current DEV-03 entry with DEFERRED-PHASE-7 marker; line 279 — traceability table row)
    - .planning/phases/05-seo-accessibility-polish/05-CONTEXT.md §D-30 (the deferral being closed)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md D-04 (RESOLVED-as-dropped semantics)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md two locations:

    Edit 1 — line 118 DEV-03 bullet: replace the trailing clause `; `x-portfolio-source` value DEFERRED-PHASE-7 per CONTEXT.md D-30 (needs production deploy URL)` with `; `x-portfolio-source` slot REMOVED in Phase 7 D-04 (repo is private; Phase 5 D-30 closes as RESOLVED-as-dropped, not "filled"). DEV-03 status: PASS-with-deviation — only `x-built-with` ships.` Keep the `[x]` checkbox and the preserved leading text. Do not change the requirement ID or its order in the list.

    Edit 2 — line 279 traceability-table row: change the Status cell from `Complete (05-08; `x-portfolio-source` value DEFERRED-PHASE-7)` to `Complete (05-08; `x-portfolio-source` slot dropped Ph7 D-04 — PASS-with-deviation)`. Preserve the `DEV-03 | Phase 5 |` prefix.

    Then stage and commit:
    `git add app/layout.tsx next.config.ts .env.example .planning/REQUIREMENTS.md`
    `git commit -m "chore(07): canonicalize site URL to www.tatibekov.com + drop x-portfolio-source slot (DEPLOY-01, D-02/D-04, closes Ph5 D-30)"`

    Do NOT push yet — Task 4 triggers the redeploy with a separate empty commit.
  </action>
  <verify>
    <automated>grep -q 'PASS-with-deviation' .planning/REQUIREMENTS.md && grep -q 'dropped Ph7 D-04' .planning/REQUIREMENTS.md && git log -1 --pretty=%s | grep -q 'DEPLOY-01' && git diff --stat HEAD~1 HEAD | grep -E 'app/layout\.tsx|next\.config\.ts|\.env\.example|REQUIREMENTS\.md' | wc -l | grep -q '^[[:space:]]*4$'</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md DEV-03 bullet (line ~118) contains the substring `PASS-with-deviation` and references `Ph7 D-04`.
    - REQUIREMENTS.md DEV-03 traceability-table row (line ~279) reflects the same deviation note.
    - Latest commit HEAD touches exactly 4 files: app/layout.tsx, next.config.ts, .env.example, .planning/REQUIREMENTS.md.
    - Commit subject contains `DEPLOY-01` and `D-04`.
  </acceptance_criteria>
  <done>
    Single commit lands all four file changes per brownfield-discipline rule (delete + replacement state ship together). Working tree is clean. Branch is one commit ahead of origin/main.
  </done>
</task>

<task type="auto">
  <name>Task 4: Trigger Vercel redeploy via empty commit + push</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Pitfall 5 (env var requires redeploy to inline new value)
    - .planning/phases/07-deploy-verification/07-CONTEXT.md "Claude's Discretion" (recommends git-commit redeploy path for audit-trail consistency with Phase 6 cutover commit FE 2610cae)
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Code Examples lines 632-660 (canonical redeploy command)
  </read_first>
  <action>
    Confirm Task 1 (Vercel env var) is complete before proceeding — if not, halt and re-surface the checkpoint.

    Run, in order:
    `git push origin main`  (pushes the source-code commit from Task 3)
    `git commit --allow-empty -m "chore(07): redeploy with NEXT_PUBLIC_SITE_URL=https://www.tatibekov.com (DEPLOY-01)"`
    `git push origin main`  (pushes the empty redeploy-trigger commit)

    Wait ~60-90 seconds for Vercel to complete the build (the Vercel Deployments tab will show the empty-commit deployment go from Building -> Ready). The build runs INFRA-05 prebuild grep + resume-PDF + resume-DOCX gates + typecheck + Next build; any failure here means the commit's source-code changes themselves failed and need a fix-in-place revert/correction.

    Both pushed commits remain visible in git log for audit-trail consistency with Phase 6 cutover (FE 2610cae).
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q 'redeploy with NEXT_PUBLIC_SITE_URL' && git log -2 --pretty=%s | grep -qE 'canonicalize site URL' && git rev-parse HEAD@{u} 2>/dev/null && [ "$(git rev-list HEAD@{u}..HEAD --count)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - Two commits on origin/main: source-code commit (Task 3) AND empty redeploy-trigger commit (this task).
    - Both commit subjects reference DEPLOY-01.
    - `git status` shows clean working tree.
    - `git rev-list HEAD@{u}..HEAD --count` returns 0 (local is in sync with remote).
    - Vercel Deployments tab shows the latest deployment status as "Ready" (manual eyeball confirmation; record the deployment ID for the SUMMARY).
  </acceptance_criteria>
  <done>
    Two commits pushed to origin/main; Vercel deployment for the empty-commit SHA reaches Ready state without build errors. Site is live at https://www.tatibekov.com with the new env var baked into the JS bundle.
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify all 7 routes return HTTP 200 + sitemap/robots emit www URLs + headers correct</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Code Examples lines 648-660 (canonical curl verification loop)
    - lib/routes.ts (7 pathnames in render order)
    - .planning/phases/07-deploy-verification/07-VALIDATION.md (DEPLOY-01 automated gates)
  </read_first>
  <action>
    Run the following verification commands in sequence against https://www.tatibekov.com (production URL, NOT localhost) and record results in a temporary buffer for the SUMMARY:

    1. Per-route HTTP status check (the 7 pathnames from lib/routes.ts):
       For each of `/`, `/projects`, `/stack`, `/experience`, `/writing`, `/contact`, `/shipped`:
       `curl -s -o /dev/null -w "%{http_code}\n" https://www.tatibekov.com<path>`
       Expected: all 7 return `200`. If any returns 4xx/5xx, halt and investigate (likely a build error or routing regression).

    2. Sitemap content check:
       `curl -s https://www.tatibekov.com/sitemap.xml | grep -c '<loc>'`
       Expected: `7`.
       `curl -s https://www.tatibekov.com/sitemap.xml | grep -c 'www.tatibekov.com'`
       Expected: `>=7` (one per `<loc>`).
       `curl -s https://www.tatibekov.com/sitemap.xml | grep -c 'localhost'`
       Expected: `0`.

    3. Robots.txt check:
       `curl -s https://www.tatibekov.com/robots.txt`
       Expected lines:
       - `User-Agent: *`
       - `Allow: /`
       - `Sitemap: https://www.tatibekov.com/sitemap.xml`

    4. Headers check on a representative route:
       `curl -sI https://www.tatibekov.com/`
       Expected: `x-built-with: nextjs-15-react-19` present.
       Expected: `x-portfolio-source` absent.
       Also confirm `strict-transport-security`, `x-content-type-options`, `x-frame-options`, `referrer-policy`, `permissions-policy` all present.

    5. Spot-check OG/canonical URL in served HTML:
       `curl -s https://www.tatibekov.com/ | grep -iE 'og:url|canonical' | head -5`
       Any URL emitted should reference `www.tatibekov.com` (not localhost, not vercel.app, not apex tatibekov.com).

    Capture all output into a single text file at .planning/phases/07-deploy-verification/deploy-01-curl-evidence.txt for inclusion in the SUMMARY (planner's evidence trail).

    If ANY check fails, halt — do not record success. Investigate (likely env var not actually saved with Production scope, or redeploy didn't complete, or a metadata regression).
  </action>
  <verify>
    <automated>for p in / /projects /stack /experience /writing /contact /shipped; do test "$(curl -s -o /dev/null -w '%{http_code}' https://www.tatibekov.com$p)" = "200" || exit 1; done && test "$(curl -s https://www.tatibekov.com/sitemap.xml | grep -c '<loc>')" = "7" && curl -s https://www.tatibekov.com/sitemap.xml | grep -q 'www.tatibekov.com' && ! curl -s https://www.tatibekov.com/sitemap.xml | grep -q 'localhost' && curl -sI https://www.tatibekov.com/ | grep -qi 'x-built-with' && ! curl -sI https://www.tatibekov.com/ | grep -qi 'x-portfolio-source'</automated>
  </verify>
  <acceptance_criteria>
    - All 7 routes return HTTP 200 (recorded with status code per route).
    - sitemap.xml `<loc>` count equals 7, all URLs start with `https://www.tatibekov.com`, no `localhost` substring anywhere.
    - robots.txt sitemap pointer is `https://www.tatibekov.com/sitemap.xml`.
    - Response headers on `/`: `x-built-with: nextjs-15-react-19` PRESENT; `x-portfolio-source` ABSENT; 5 security headers PRESENT.
    - Evidence file .planning/phases/07-deploy-verification/deploy-01-curl-evidence.txt exists and contains the per-route + sitemap + robots + headers output.
  </acceptance_criteria>
  <done>
    DEPLOY-01 automated gates all green; evidence captured to deploy-01-curl-evidence.txt; SUMMARY can cite this file as the audit trail. Ready for Wave 2 (analytics).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel env-var panel -> Build pipeline | Production env var is inlined into client JS bundle at build time (D-03); compromise of Vercel project settings would let an attacker change the canonical URL. Mitigation: Vercel account has 2FA; access is the user's alone. |
| DNS provider (Vercel DNS) -> Browser | DNS hijack could redirect www.tatibekov.com elsewhere. Mitigation: out of scope for Phase 7; documented as platform-level risk. |
| HTTP response headers | Five security headers + x-built-with engineer header emit on every route; misconfiguration could disable HSTS/X-Frame-Options. Mitigation: this plan only deletes the dead `x-portfolio-source` slot, preserving all security headers verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | x-portfolio-source header slot | mitigate | D-04 drops the slot entirely from `next.config.ts`. Repo is private; pointing engineers at an inaccessible GitHub URL adds no value and signals "private repo exists" needlessly. Task 2 removes the slot + comment; Task 5 verifies absence via curl -sI. |
| T-07-02 | Tampering | NEXT_PUBLIC_SITE_URL value | mitigate | Set in Vercel UI under Production scope only (D-03 / Task 1). NEXT_PUBLIC_* is intentionally public (build-time inlined into client bundle) — not a secret. Risk is misconfiguration, not exfiltration. Task 5 curl verification proves the deployed value matches `https://www.tatibekov.com`. |
| T-07-03 | Denial of Service | Empty-commit redeploy | accept | Triggering a redeploy is identical in risk to any normal deploy. If the build fails (INFRA-05 grep / typecheck), the previous deployment stays live — Vercel does atomic swaps. Task 4 explicitly waits for "Ready" status before declaring success. |
| T-07-04 | Spoofing | Vercel-hostname alias `personal-portfolio-web-orcin.vercel.app` | accept | The alias serves identical content (D-01); relies on `<link rel="canonical">` from `metadataBase` to let Google de-duplicate. Risk is duplicate-content indexing only; if GSC reports duplication, v1.1 fix is conditional noindex in `app/robots.ts` (documented in Deferred Ideas). |
| T-07-05 | Repudiation | Production cutover commit history | mitigate | Two commits land on `main` (source + empty redeploy) per Claude's Discretion recommendation for audit trail; matches Phase 6 Plan 09 cutover precedent (FE 2610cae). |

ASVS L1 posture: PASS. No new auth surfaces, no new endpoints, no PII, no secrets created. The five security headers from Phase 1 remain intact.
</threat_model>

<verification>
- All 5 tasks complete in order (Task 1 manual UI checkpoint blocks Tasks 2-5).
- Task 2 + 3 produce a single commit with 4 file modifications.
- Task 4 produces a second (empty) commit and successful Vercel redeploy.
- Task 5 captures curl-based evidence to deploy-01-curl-evidence.txt and all automated gates green.
- `npm run lint` + `npm run typecheck` both exit 0 (post-Task-2 verify).
- `git status` is clean; HEAD is in sync with origin/main.
- DEPLOY-01 status in REQUIREMENTS.md remains [ ] (close-out plan 07-09 flips it to [x] after full phase completion); DEV-03 status updated to PASS-with-deviation by Task 3.
</verification>

<success_criteria>
This plan succeeds when:
1. https://www.tatibekov.com/sitemap.xml returns 7 `<loc>` entries all pointing at www.tatibekov.com (no localhost, no vercel.app).
2. All 7 production routes return HTTP 200 on curl.
3. `x-built-with: nextjs-15-react-19` header present; `x-portfolio-source` header absent on every route.
4. REQUIREMENTS.md DEV-03 status updated to PASS-with-deviation; Phase 5 D-30 closed as RESOLVED-as-dropped.
5. Two commits land on origin/main (source-code commit + empty redeploy commit); Vercel deployment for the latter reaches Ready state.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deploy-verification/07-01-SUMMARY.md` capturing:
- Vercel deployment ID for the redeploy commit
- Per-route HTTP status table (7 rows)
- Sitemap `<loc>` count and sample URL
- Curl-based header verification (presence of x-built-with, absence of x-portfolio-source, 5 security headers)
- Commit SHAs for the two commits (source + empty redeploy)
- Path to evidence file: .planning/phases/07-deploy-verification/deploy-01-curl-evidence.txt
</output>
