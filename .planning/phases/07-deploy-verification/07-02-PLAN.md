---
phase: 07-deploy-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-production-routes.mjs
  - package.json
  - .planning/phases/07-deploy-verification/lighthouse/.gitkeep
  - .planning/phases/07-deploy-verification/screenshots/375/.gitkeep
  - .planning/phases/07-deploy-verification/gsc/.gitkeep
  - .planning/phases/07-deploy-verification/unfurl/.gitkeep
  - .planning/phases/07-deploy-verification/analytics/.gitkeep
  - .planning/phases/07-deploy-verification/vercel/.gitkeep
autonomous: true
requirements:
  - DEPLOY-01
tags:
  - infrastructure
  - smoke-script
  - evidence-directories
  - wave-0
must_haves:
  truths:
    - "scripts/check-production-routes.mjs exists, is executable, and iterates the 7 routes against NEXT_PUBLIC_SITE_URL"
    - "Running the script against https://www.tatibekov.com after Plan 01 redeploy exits 0 with 7/7 green"
    - "Evidence directories exist under .planning/phases/07-deploy-verification/ for lighthouse/, screenshots/375/, gsc/, unfurl/, analytics/, vercel/"
    - "package.json has a `check:prod` named script invoking the new smoke gate"
  artifacts:
    - path: "scripts/check-production-routes.mjs"
      provides: "DEPLOY-01 readiness gate — batch HTTP 200 check across 7 routes"
      min_lines: 30
      contains: "process.exit(1)"
    - path: "package.json"
      provides: "Named script `check:prod` for canonical invocation"
      contains: "check:prod"
    - path: ".planning/phases/07-deploy-verification/lighthouse/.gitkeep"
      provides: "Evidence directory for PSI screenshots (Plan 04)"
    - path: ".planning/phases/07-deploy-verification/screenshots/375/.gitkeep"
      provides: "Evidence directory for 375px shell screenshots (Plan 08)"
    - path: ".planning/phases/07-deploy-verification/gsc/.gitkeep"
      provides: "Evidence directory for Search Console screenshots (Plan 05)"
    - path: ".planning/phases/07-deploy-verification/unfurl/.gitkeep"
      provides: "Evidence directory for Slack/LinkedIn unfurl screenshots (Plan 07)"
    - path: ".planning/phases/07-deploy-verification/analytics/.gitkeep"
      provides: "Evidence directory for Vercel Analytics dashboard screenshots (Plan 03/07)"
    - path: ".planning/phases/07-deploy-verification/vercel/.gitkeep"
      provides: "Evidence directory for Vercel UI screenshots (Plan 01 env-var, Plan 03 analytics toggle)"
  key_links:
    - from: "scripts/check-production-routes.mjs"
      to: "lib/routes.ts (inlined 7 pathnames with back-pointer comment)"
      via: "hard-coded array per PATTERNS.md (`.mjs` cannot import `.ts` without loader)"
      pattern: "Mirrors lib/routes.ts"
    - from: "package.json scripts.check:prod"
      to: "node scripts/check-production-routes.mjs"
      via: "npm run check:prod"
      pattern: '"check:prod"'
---

<objective>
Ship the Wave 0 infrastructure that every subsequent Phase 7 plan depends on: a new `scripts/check-production-routes.mjs` smoke gate (DEPLOY-01 automated check that all 7 routes return HTTP 200 on the production URL), six evidence directories under `.planning/phases/07-deploy-verification/` for committed PSI screenshots / 375px shell screenshots / GSC screenshots / unfurl previews / Vercel Analytics dashboard / Vercel UI captures, and a named `npm run check:prod` script wiring.

Purpose: Without the evidence directories existing in-repo, downstream plans cannot place their screenshot evidence under `.planning/phases/07-deploy-verification/`. Without `check-production-routes.mjs`, the DEPLOY-01 automated gate is missing. The Nyquist validation contract (07-VALIDATION.md Wave 0 Requirements) explicitly lists these as blockers.

Output: One new `.mjs` smoke script (mirrors BE sibling `check-backend.mjs` pattern), package.json updated with one new named script, six `.gitkeep` files preserving the empty evidence directories under git tracking, single commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-deploy-verification/07-CONTEXT.md
@.planning/phases/07-deploy-verification/07-RESEARCH.md
@.planning/phases/07-deploy-verification/07-PATTERNS.md
@.planning/phases/07-deploy-verification/07-VALIDATION.md

<interfaces>
<!-- Existing sibling smoke-script analogs the executor must mirror. -->

From scripts/check-resume-pdf.mjs / check-resume-docx.mjs / check-placeholders.mjs (FE skeleton):
```js
#!/usr/bin/env node
// scripts/<name>.mjs — <REQ-ID> readiness gate.
// <one-line description>
// Exit 0 on full pass, 1 on any miss.
// ... checks with failures[] accumulator ...
if (failures.length > 0) {
  console.error(`\n✗ <NAME>: <SUMMARY>:`);
  for (const f of failures) console.error(`  ${f}`);
  process.exit(1);
}
console.log(`✓ <NAME>: <success summary>`);
```

From portfolio-services/scripts/check-backend.mjs (BE sibling — closest pattern match):
```js
const BASE = process.env.PROD_API_URL ?? 'http://localhost:8080';
const fails = [];
for (const e of ENDPOINTS) {  // ENDPOINTS hard-coded inline
  try {
    const res = await fetch(`${BASE}${e.path}`);
    if (res.status !== 200) { fails.push(`${e.path}: HTTP ${res.status}`); continue; }
    console.log(`✓ ${e.path}`);
  } catch (err) {
    fails.push(`${e.path}: ${err.message}`);
  }
}
```

From lib/routes.ts (single source of truth, 7 entries):
```ts
ROUTES = [
  { pathname: "/", ... },
  { pathname: "/projects", ... },
  { pathname: "/stack", ... },
  { pathname: "/experience", ... },
  { pathname: "/writing", ... },
  { pathname: "/contact", ... },
  { pathname: "/shipped", ... }
]
```
The new script CANNOT import lib/routes.ts directly (`.mjs` rejects `.ts` imports without a loader). Inline the 7 pathnames with a back-pointer comment per PATTERNS.md recommendation (BE-script `ENDPOINTS` precedent).
</interfaces>

<canonical_refs>
- PATTERNS.md §`scripts/check-production-routes.mjs` (lines 388-484) — full template + conventions + .mjs/.ts import gotcha + risk note
- PATTERNS.md §"Shared Patterns / Smoke-script-as-gate skeleton" (lines 487-516)
- RESEARCH.md §Pattern 3 / Pattern 4 — verification curl loop the script automates
- VALIDATION.md §Wave 0 Requirements (lines 69-75)
- CONTEXT.md "Claude's Discretion" — recommends committed evidence directories matching Phase 5 OG image precedent
</canonical_refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check-production-routes.mjs and wire `npm run check:prod`</name>
  <read_first>
    - scripts/check-resume-pdf.mjs (FE skeleton — shebang + comment header + failures[] + exit pattern)
    - scripts/check-placeholders.mjs (FE precedent for env-var fallback handling)
    - ../portfolio-services/scripts/check-backend.mjs (BE sibling — closest pattern match, inline-array precedent)
    - lib/routes.ts (7 pathnames in order — to mirror inline)
    - .planning/phases/07-deploy-verification/07-PATTERNS.md §`scripts/check-production-routes.mjs` (template + risk note + Option 1 recommendation: hard-code 7 paths)
    - package.json (current scripts block — alphabetical placement of `check:prod`)
  </read_first>
  <action>
    Create `/Users/beckmaldinVL/development/personal-portfolio/portfolio-web/scripts/check-production-routes.mjs` with the following structure (no code fence; write actual file content per PATTERNS.md Option 1 — inline the 7 pathnames with a comment back-pointer):

    - Line 1: shebang `#!/usr/bin/env node`
    - Lines 2-5: header comment block per skeleton — name, requirement ID (DEPLOY-01), what it enforces (HTTP 200 on all 7 routes on production URL), exit-code semantics (0 on full pass, 1 on any miss).
    - Define `const BASE = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";` (use `??` matching BE-sibling and existing FE script-style; `||` would also work — pick `??` for sibling consistency per PATTERNS.md).
    - Define `const ROUTES = ["/", "/projects", "/stack", "/experience", "/writing", "/contact", "/shipped"];` with a one-line comment ABOVE it: `// Mirrors lib/routes.ts pathnames — keep in sync if ROUTES changes (.mjs cannot import .ts without a loader).`
    - Initialize `const failures = [];`
    - Iterate ROUTES with top-level `for...of` using `await fetch(BASE + path, { method: "HEAD", redirect: "manual" })`. On success (status < 400) log `console.log("✓ " + path + " → " + res.status);`. On status >= 400 push `failures.push(path + ": HTTP " + res.status)`. Wrap in try/catch; on catch, push `failures.push(path + ": " + err.message)`.
    - Final block: `if (failures.length > 0) { console.error("\n✗ check-production-routes: " + failures.length + " failure(s):"); for (const f of failures) console.error("  " + f); process.exit(1); }`
    - Closing line: `console.log("\n✓ check-production-routes: all " + ROUTES.length + " routes green on " + BASE);`

    Make executable: not strictly needed (runs via `node`), but no `chmod` required since invocation is `node scripts/check-production-routes.mjs`.

    Then edit package.json scripts block. Insert `"check:prod": "node scripts/check-production-routes.mjs"` after the existing `"check:mobile"` entry (preserve alphabetical-by-namespace ordering: `check:mobile`, then `check:prod`). Do NOT modify any other script. Do NOT introduce trailing commas (JSON, not JSONC).

    Conventions to preserve from sibling scripts (PATTERNS.md "Shared Patterns / Smoke-script-as-gate skeleton"):
    - Symbol convention: `✓` (success), `✗` (failure summary).
    - No `chmod +x` required; invocation is always `node scripts/...mjs`.
    - Top-level await is fine (ESM `.mjs`).
    - No throws; `process.exit(1)` for failure, implicit exit 0 for success.
    - Do NOT add `--legacy-peer-deps` or any additional npm flags.
  </action>
  <verify>
    <automated>test -f scripts/check-production-routes.mjs && head -1 scripts/check-production-routes.mjs | grep -q '#!/usr/bin/env node' && grep -q 'NEXT_PUBLIC_SITE_URL' scripts/check-production-routes.mjs && grep -q 'Mirrors lib/routes.ts' scripts/check-production-routes.mjs && grep -q 'process.exit(1)' scripts/check-production-routes.mjs && grep -c '"/' scripts/check-production-routes.mjs | (read n; test "$n" -ge 7) && jq -r '.scripts."check:prod"' package.json | grep -q 'node scripts/check-production-routes.mjs' && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `scripts/check-production-routes.mjs` with shebang on line 1.
    - File contains exactly 7 pathnames in the inline ROUTES array (`/`, `/projects`, `/stack`, `/experience`, `/writing`, `/contact`, `/shipped`) in render order.
    - File contains the comment `Mirrors lib/routes.ts` (single-source-of-truth back-pointer per PATTERNS.md).
    - File uses `process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"` for BASE.
    - File contains `process.exit(1)` for the failure path.
    - package.json `scripts.check:prod` equals `"node scripts/check-production-routes.mjs"`.
    - `npm run lint` exits 0 (eslint may or may not lint `.mjs` files in `scripts/` — verify project config does not flag).
    - Running `NEXT_PUBLIC_SITE_URL=https://www.tatibekov.com node scripts/check-production-routes.mjs` after Plan 01 redeploy exits 0 with 7/7 `✓` lines.
  </acceptance_criteria>
  <done>
    Smoke script created; `npm run check:prod` is a wired named script; lint green; ready to be invoked after Plan 01 redeploy completes (Plan 01 Task 5 will exercise it via the underlying curl loop; this plan does NOT depend on Plan 01 completion since the script is just code — the env-var execution happens later).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create six evidence directories with .gitkeep placeholders</name>
  <read_first>
    - .planning/phases/07-deploy-verification/07-CONTEXT.md "Claude's Discretion" (recommends committed evidence dirs matching Phase 5 OG image precedent)
    - .planning/phases/07-deploy-verification/07-RESEARCH.md §Recommended Project Structure (lines 244-277) — directory list
    - .planning/phases/07-deploy-verification/07-VALIDATION.md §Wave 0 Requirements (evidence directories enumerated)
  </read_first>
  <action>
    Create six directories under `.planning/phases/07-deploy-verification/` with a `.gitkeep` file (empty file) inside each so git tracks them. Directory names and purposes:

    1. `lighthouse/` — PSI dashboard screenshots per route × 7 (Plan 04 evidence target).
    2. `screenshots/375/` — DevTools 375px viewport screenshots per route × 7 (Plan 08 evidence target).
    3. `gsc/` — Google Search Console verification + sitemap-submitted + coverage screenshots (Plan 05 evidence target).
    4. `unfurl/` — Slack + LinkedIn unfurl preview screenshots (Plan 07 evidence target — Phase 5 SEO-03c carry-forward).
    5. `analytics/` — Vercel Analytics dashboard screenshot for `resume_download` event (Plan 03 + Plan 07 evidence target).
    6. `vercel/` — Vercel UI env-var-set screenshot (Plan 01 Task 1 evidence target — already referenced) + Analytics toggle screenshot (Plan 03).

    Use `mkdir -p` for parent creation (handles the nested `screenshots/375/` case). Use `touch` to create `.gitkeep` files. Single command sequence:

    `mkdir -p .planning/phases/07-deploy-verification/{lighthouse,screenshots/375,gsc,unfurl,analytics,vercel}`
    Then `touch` a `.gitkeep` in each of the six leaf directories.

    Do NOT add a README to any directory; the planning artifacts (07-VERIFICATION.md and per-plan SUMMARY.md files) document what goes in each directory.

    Do NOT add `.gitignore` entries — these are intentionally tracked dirs.
  </action>
  <verify>
    <automated>for d in lighthouse screenshots/375 gsc unfurl analytics vercel; do test -f .planning/phases/07-deploy-verification/$d/.gitkeep || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - Six leaf directories exist under `.planning/phases/07-deploy-verification/`: `lighthouse/`, `screenshots/375/`, `gsc/`, `unfurl/`, `analytics/`, `vercel/`.
    - Each contains a `.gitkeep` (empty file).
    - `git status` shows all six `.gitkeep` files as untracked (ready to add).
    - No additional files (README, .gitignore, etc.) in any of the directories.
  </acceptance_criteria>
  <done>
    Six tracked evidence directories ready for downstream plans to drop screenshots into. Disk cost is negligible (<1KB of placeholders + later <2MB of PNGs).
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit Wave 0 infrastructure</name>
  <read_first>
    - git status (verify only Task 1 + Task 2 files are staged)
  </read_first>
  <action>
    Stage and commit all Wave 0 infra changes in a single commit:

    `git add scripts/check-production-routes.mjs package.json .planning/phases/07-deploy-verification/lighthouse/.gitkeep .planning/phases/07-deploy-verification/screenshots/375/.gitkeep .planning/phases/07-deploy-verification/gsc/.gitkeep .planning/phases/07-deploy-verification/unfurl/.gitkeep .planning/phases/07-deploy-verification/analytics/.gitkeep .planning/phases/07-deploy-verification/vercel/.gitkeep`

    `git commit -m "chore(07): wave 0 infra — check-production-routes smoke gate + evidence dirs (DEPLOY-01)"`

    Do NOT push — Plan 01 Task 4 controls the redeploy timing; this commit can ride along on Plan 01's `git push` or push independently as long as it lands before the first downstream evidence-capture task.

    NOTE: This plan runs in Wave 1 ALONGSIDE Plan 01 (no inter-plan code dependency — Plan 01 touches `app/layout.tsx`, `next.config.ts`, `.env.example`, `.planning/REQUIREMENTS.md`; Plan 02 touches `scripts/`, `package.json`, six `.gitkeep` files — zero overlap). The two plans may commit independently; both can push to origin/main without merge conflict.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q 'wave 0 infra' && git diff --stat HEAD~1 HEAD | grep -E 'scripts/check-production-routes\.mjs|package\.json|\.gitkeep' | wc -l | (read n; test "$n" -ge 8)</automated>
  </verify>
  <acceptance_criteria>
    - Single commit with subject containing `wave 0 infra` and `DEPLOY-01`.
    - Commit touches exactly 8 files: 1 new script, 1 modified package.json, 6 new .gitkeep files.
    - Working tree clean.
  </acceptance_criteria>
  <done>
    Wave 0 infra committed. Plans 03-09 can now reference the evidence directories and the `check:prod` script with confidence the paths exist in tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/check-production-routes.mjs` runtime -> Production URL | Script fetches HEAD against 7 public routes; no auth required; output is local stdout/stderr only. |
| Evidence-directory writes | Future screenshots will be PNG files; non-executable; no XSS / no script injection vector via image content (the screenshots are evidence, not served HTML). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | Inlined ROUTES array in `check-production-routes.mjs` | accept | The 7 pathnames are duplicated from `lib/routes.ts` because `.mjs` cannot import `.ts` cleanly (PATTERNS.md Risk section). Mitigation: explicit back-pointer comment "Mirrors lib/routes.ts — keep in sync." Drift risk is low (routes.ts hasn't changed since Phase 1). If a future phase adds an 8th route, the smoke gate will silently skip it until updated — acceptable for v1 verification scope. |
| T-07-07 | Information Disclosure | Script output | accept | Script logs route pathnames + HTTP status to stdout. No headers, no body content, no env vars echoed. Routes are public; no leak surface. |
| T-07-08 | Denial of Service | HEAD-fetching 7 routes in a loop | accept | 7 requests with no retry / no parallelism / no rate-limiting; well below any Vercel rate threshold for a single client IP. |

ASVS L1: PASS. No new auth, no PII, no secrets handling, no new endpoints.
</threat_model>

<verification>
- Task 1: smoke script created with all required structural elements; `npm run check:prod` invokes it.
- Task 2: six evidence directories with `.gitkeep` files; tree is git-trackable.
- Task 3: single commit lands 8 files.
- `npm run lint` exits 0 (post-Task-1 verify embedded in automated check).
- The smoke script's end-to-end behavior is exercised in Plan 01 Task 5 via the underlying curl loop AND directly via `npm run check:prod` after that plan's redeploy.
</verification>

<success_criteria>
This plan succeeds when:
1. `scripts/check-production-routes.mjs` exists with the canonical skeleton (shebang, header comment, BASE from env, inline ROUTES with back-pointer, failures[], exit 1 / exit 0).
2. `npm run check:prod` is a wired named script in package.json.
3. Six evidence directories exist with `.gitkeep` placeholders so downstream plans can place screenshots in them.
4. Single commit lands all 8 files.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deploy-verification/07-02-SUMMARY.md` capturing:
- Path to new smoke script + line count
- package.json `check:prod` script wiring confirmation
- List of six evidence directories with `.gitkeep` files
- Commit SHA
- Note that the script's end-to-end behavior gets exercised in Plan 01 Task 5 and standalone in Plan 09 close-out
</output>
