---
phase: 10-projects-ui-enrichment
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/project-stats.ts
  - lib/project-stats.test.ts
autonomous: true
requirements: [LIST-02, LIST-03, LIST-04, LIST-07, DETAIL-02, DETAIL-03, DETAIL-04, DETAIL-05, DETAIL-07]

must_haves:
  truths:
    - "buildStripModel(stats) returns a StripModel with commitCount, 1-3 abbreviated languages, and a duration string when stats are present"
    - "buildStripModel(null) returns null so a project with no stats renders no strip"
    - "buildPanelModel(stats) returns a PanelModel with a top-5 language byte breakdown, an other-bucket pct, a duration line, and a relative last-active line"
    - "buildPanelModel(null) returns null so a project with no stats omits the Tech highlights block"
    - "topLanguages drops every language under 1% of total bytes before selecting the top-N"
    - "formatDuration produces 'Nmo' under a year and 'Ny Nmo' at a year or above"
    - "relativeTime produces today / N days ago / N months ago / N years ago"
  artifacts:
    - path: "lib/project-stats.ts"
      provides: "Pure formatter helpers: topLanguages, formatDuration, relativeTime, monthYear, buildStripModel, buildPanelModel + StripModel/PanelModel types + LANG_ABBREV map"
      min_lines: 90
      exports: ["topLanguages", "formatDuration", "relativeTime", "monthYear", "buildStripModel", "buildPanelModel", "StripModel", "PanelModel"]
    - path: "lib/project-stats.test.ts"
      provides: "Exhaustive unit tests for every pure helper with injected now"
      contains: "describe"
  key_links:
    - from: "lib/project-stats.ts"
      to: "lib/types.ts"
      via: "import type { GitHubRepoStats } from \"./types\""
      pattern: "import type \\{ GitHubRepoStats \\} from \"./types\""
---

<objective>
Build `lib/project-stats.ts` — the pure formatter module that converts a `GitHubRepoStats` object into the two view-models the Phase 10 UI renders: `StripModel` (for the collapsed `/projects` card strip) and `PanelModel` (for the expanded Tech highlights panel). Ships with exhaustive co-located unit tests.

Purpose: This module is the single contract between the Phase 9 data layer (`getRepoStats`) and the Phase 10 render layer (Plan 02's `project-row.tsx`). Defining and testing it first means Plan 02 implements against fixed, verified types — no scavenger hunt. The helpers are pure (no I/O, no React, no `node:` imports) so they are server-and-client-safe and exhaustively unit-testable, satisfying the LIST-09 / DETAIL-09 unit-coverage burden up front.

Output: `lib/project-stats.ts` (pure helpers + `StripModel` / `PanelModel` types + `LANG_ABBREV` map) and `lib/project-stats.test.ts` (full branch coverage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-projects-ui-enrichment/10-CONTEXT.md
@.planning/phases/10-projects-ui-enrichment/10-RESEARCH.md
@.planning/phases/10-projects-ui-enrichment/10-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. The GitHubRepoStats input shape is FIXED (Phase 9). -->
<!-- StripModel / PanelModel are the OUTPUT contracts this plan defines for Plan 02. -->

From lib/types.ts (input — do NOT modify):
```typescript
export interface GitHubRepoStats {
  createdAt: string;   // ISO 8601 — earliest created_at across repos
  pushedAt: string;    // ISO 8601 — latest pushed_at across repos
  languages: Record<string, number>;  // merged language byte map
  commitCount: number; // summed commit count
}
```

Output contracts this plan creates (from 10-RESEARCH.md Code Examples):
```typescript
export interface StripModel {
  commitCount: number;
  languages: string[];   // 1-3 abbreviated names, >=1% floor applied
  duration: string;      // "4mo" / "1y 2mo"
}
export interface PanelModel {
  commitCount: number;
  breakdown: { name: string; pct: number }[];  // full GitHub names, top 5, rounded pct
  otherPct: number;                            // sum of ranks 6+ (post-floor), rounded
  durationLine: string;   // "In development since Jan 2026 — 4mo"
  lastActive: string;     // "Last active 3 days ago"
}
```

Analog to mirror — `lib/experience-duration.ts` (already in context via 10-PATTERNS.md):
- Leading JSDoc documenting accepted shapes + return contract.
- `now?: Date` (or `now = new Date()`) injection parameter on every date-dependent export.
- UTC date methods (`getUTCFullYear()` / `getUTCMonth()`) for timezone-stable output.
- `${years}y ${months}mo` / `${months}mo` branch structure.
- `// Used by app/components/...` trailer comment naming the consumer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing unit tests for all pure helpers</name>
  <read_first>
    - lib/experience-duration.test.ts (the exact co-located test analog: `import { describe, test, expect } from "vitest"`, one describe per function, injected fixed `now`)
    - lib/experience-duration.ts (the pure-formatter analog being mirrored)
    - lib/types.ts lines 88-98 (the GitHubRepoStats input shape — FIXED)
    - .planning/phases/10-projects-ui-enrichment/10-RESEARCH.md ("Code Examples" + "Common Pitfalls" sections — the helper signatures and Pitfall 6 percentage-floor behavior)
    - .planning/phases/10-projects-ui-enrichment/10-CONTEXT.md (D-06, D-07, D-08, D-09 — abbreviation rule, full-name-in-panel rule, <1% floor)
  </read_first>
  <action>
    Create `lib/project-stats.test.ts` with explicit `import { describe, test, expect } from "vitest"` (match the `lib/` test convention — do not rely on globals). Import every helper from `./project-stats`. Write one `describe` per exported function, one `test` per branch. Every time-dependent assertion MUST inject a fixed `new Date(...)` — never call the real clock.

    Cover these branches:
    - `topLanguages`: total === 0 returns []; a language at exactly 1% is kept; a language under 1% (e.g. 0.4%) is dropped; result is sorted by bytes descending; `limit` slices correctly (limit 3 and limit Infinity).
    - `formatDuration`: under one year produces `Nmo` (e.g. createdAt 4 months before now → `"4mo"`); a year or above produces `Ny Nmo` (e.g. 14 months → `"1y 2mo"`); sub-month range guarded to `"0mo"`.
    - `relativeTime`: same-day → `"today"`; 1 day → `"1 day ago"`; under 30 days → `"N days ago"`; 1 month → `"1 month ago"`; under 12 months → `"N months ago"`; 1 year → `"1 year ago"`; multi-year → `"N years ago"`.
    - `monthYear`: an ISO date in January 2026 → `"Jan 2026"`.
    - `buildStripModel`: `null` input → `null` (LIST-07); a valid stats object → StripModel where `languages` is the top-3 abbreviated names (assert `TypeScript` maps to `TS`, `JavaScript` to `JS`, and a language absent from the map — e.g. `Swift` — renders verbatim, per D-06), `commitCount` passes through, `duration` is the formatted string.
    - `buildPanelModel`: `null` input → `null` (DETAIL-07); a valid stats object → PanelModel where `breakdown` holds up to 5 entries with FULL GitHub language names (assert `TypeScript` stays `TypeScript`, not `TS`, per D-07) and rounded integer `pct`, `otherPct` is the rounded sum of ranks 6+, `durationLine` starts with `"In development since "`, `lastActive` starts with `"Last active "`.

    Tests MUST fail at this point because `lib/project-stats.ts` does not exist yet. Commit with message `test(10-01): add failing tests for project-stats pure helpers`.
  </action>
  <verify>
    <automated>npx vitest run lib/project-stats.test.ts 2>&1 | grep -qE "Cannot find module|Failed to load" && echo RED-OK || (echo "expected RED — module missing" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `lib/project-stats.test.ts` exists and uses `import { describe, test, expect } from "vitest"`.
    - Every date-dependent test passes a literal `new Date(...)` as the `now` argument — no test reads the real clock.
    - The file contains one `describe` block per exported helper (`topLanguages`, `formatDuration`, `relativeTime`, `monthYear`, `buildStripModel`, `buildPanelModel`).
    - `npx vitest run lib/project-stats.test.ts` fails because `./project-stats` cannot be resolved (RED state — module not yet created).
    - A git commit exists with a message beginning `test(10-01):`.
  </acceptance_criteria>
  <done>The test file is committed, asserts every branch listed above with injected fixed dates, and the test run fails on the missing `./project-stats` module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement lib/project-stats.ts so all tests pass</name>
  <read_first>
    - lib/project-stats.test.ts (the failing tests from Task 1 — the implementation must satisfy every assertion)
    - lib/experience-duration.ts (the pure-formatter analog: JSDoc header, `now` injection, UTC date methods, branch-structured return)
    - lib/types.ts lines 88-98 (GitHubRepoStats input shape)
    - .planning/phases/10-projects-ui-enrichment/10-RESEARCH.md ("Code Examples" — buildStripModel / buildPanelModel / topLanguages / formatDuration / relativeTime reference implementations; Pitfall 6 — percentage handling)
    - .planning/phases/10-projects-ui-enrichment/10-CONTEXT.md (D-06 abbreviation rule, D-07 full-name-in-panel, D-08 strip top-3 + <1% floor, D-09 panel <1% floor + other bucket)
  </read_first>
  <action>
    Create `lib/project-stats.ts` as a pure module. Import only `import type { GitHubRepoStats } from "./types"` — NO `node:` imports, NO React, NO import of `lib/github.ts` (it pulls `node:fs/promises` and would poison the client bundle in Plan 02). Open with a JSDoc header documenting the module purpose and the `now` injection seam, ending with a `// Used by app/(terminal)/projects/page.tsx + app/components/project-row.tsx` trailer.

    Export the `StripModel` and `PanelModel` interfaces exactly as given in the `<interfaces>` block above.

    Export a `LANG_ABBREV: Record<string, string>` map. Per D-06, derive membership from the languages GitHub actually returns for the portfolio's repos — at minimum `TypeScript: "TS"`, `JavaScript: "JS"`, `Python: "Py"`. Languages already short (`Swift`, `CSS`, `HTML`, `Shell`, `Go`) are intentionally NOT in the map — D-06 says any language absent from the map renders its full GitHub name verbatim. Add a comment stating this rule.

    Implement these pure exports:
    - `topLanguages(languages: Record<string, number>, limit: number): { name: string; bytes: number; pct: number }[]` — compute total bytes; if total is 0 return `[]`; map to `{ name, bytes, pct }` where `pct = (bytes / total) * 100`; filter to `pct >= 1` (the <1% floor — D-08/D-09); sort by `bytes` descending; slice to `limit`.
    - `formatDuration(createdAtISO: string, now?: Date): string` — compute whole-month difference using UTC methods (`getUTCFullYear` / `getUTCMonth`); guard a negative or sub-month result to `0`; return `${years}y ${rem}mo` when years > 0 else `${months}mo`.
    - `relativeTime(iso: string, now?: Date): string` — day-difference math; branches: `< 1` day → `"today"`, `=== 1` → `"1 day ago"`, `< 30` → `"N days ago"`, then month branches (`1` → `"1 month ago"`, `< 12` → `"N months ago"`), then year branches (`1` → `"1 year ago"`, else `"N years ago"`).
    - `monthYear(iso: string): string` — return the short month name + 4-digit year using UTC methods (e.g. `"Jan 2026"`).
    - `buildStripModel(stats: GitHubRepoStats | null, now?: Date): StripModel | null` — return `null` when `stats` is null (LIST-07); otherwise return `{ commitCount, languages: topLanguages(stats.languages, 3).map(l => LANG_ABBREV[l.name] ?? l.name), duration: formatDuration(stats.createdAt, now) }`.
    - `buildPanelModel(stats: GitHubRepoStats | null, now?: Date): PanelModel | null` — return `null` when `stats` is null (DETAIL-07); otherwise compute `surviving = topLanguages(stats.languages, Infinity)` (full set, post-floor), take `top5 = surviving.slice(0, 5)` with FULL GitHub names (D-07 — never abbreviate in the panel), `otherPct = Math.round(surviving.slice(5).reduce((a, l) => a + l.pct, 0))`, `breakdown = top5.map(l => ({ name: l.name, pct: Math.round(l.pct) }))`, `durationLine = \`In development since ${monthYear(stats.createdAt)} — ${formatDuration(stats.createdAt, now)}\``, `lastActive = \`Last active ${relativeTime(stats.pushedAt, now)}\``.

    Pitfall 6: percentages are computed from the raw total before the floor; after dropping <1% languages the displayed percentages may not sum to 100 — that is correct and intentional, do not re-normalize and do not add a test asserting a 100% sum.

    All tests from Task 1 must now pass. Commit with message `feat(10-01): implement project-stats strip + panel formatters`.
  </action>
  <verify>
    <automated>npx vitest run lib/project-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/project-stats.ts` exists, imports ONLY `import type { GitHubRepoStats } from "./types"` — `grep -E "from \"(node:|.*github)" lib/project-stats.ts` returns no matches.
    - The file exports `topLanguages`, `formatDuration`, `relativeTime`, `monthYear`, `buildStripModel`, `buildPanelModel`, `StripModel`, `PanelModel`, and `LANG_ABBREV`.
    - `npx vitest run lib/project-stats.test.ts` exits 0 with all tests green.
    - `npx tsc --noEmit` exits 0 (strict mode clean).
    - `buildStripModel(null)` returns `null` and `buildPanelModel(null)` returns `null` (verified by the Task 1 tests).
    - `LANG_ABBREV` contains `TypeScript`, `JavaScript`, `Python` keys; the file carries a comment stating absent languages render verbatim.
    - A git commit exists with a message beginning `feat(10-01):`.
  </acceptance_criteria>
  <done>`lib/project-stats.ts` is committed, all Task 1 tests pass, `tsc --noEmit` is clean, and the module has zero `node:` / `lib/github.ts` imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API → `lib/github.ts` → this module | GitHub-supplied language names and counts arrive here as a plain `GitHubRepoStats` object. This module performs no I/O — it only transforms already-fetched data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | `lib/project-stats.ts` import graph | mitigate | Module imports ONLY `import type { GitHubRepoStats } from "./types"`. It must never import `lib/github.ts` (which pulls `node:fs/promises` and reads `GITHUB_TOKEN`); doing so would let Plan 02's client island transitively pull a server module into the client bundle. Acceptance criteria grep enforces zero `node:` / `github` imports. |
| T-10-02 | Tampering / Injection | GitHub language-name strings | accept | Language names are plain strings transformed and returned as data; no HTML construction, no `dangerouslySetInnerHTML`. React auto-escapes them at render time in Plan 02. No action needed in this pure module. |
</threat_model>

<verification>
- `npx vitest run lib/project-stats.test.ts` — all helper branches green.
- `npx tsc --noEmit` — TypeScript strict clean.
- `grep -E "from \"(node:|.*github)" lib/project-stats.ts` — no matches (boundary integrity for Plan 02).
- `npm test` — full Vitest suite still green (no regressions).
</verification>

<success_criteria>
- `lib/project-stats.ts` exports `topLanguages`, `formatDuration`, `relativeTime`, `monthYear`, `buildStripModel`, `buildPanelModel`, plus `StripModel` / `PanelModel` types and the `LANG_ABBREV` map.
- Every date-dependent helper accepts an injectable `now` and uses UTC date methods.
- `buildStripModel(null)` and `buildPanelModel(null)` both return `null`.
- The <1% language floor is applied before top-N selection in both models.
- Module has zero `node:` and zero `lib/github.ts` imports.
- All unit tests green; `tsc --noEmit` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/10-projects-ui-enrichment/10-01-SUMMARY.md`.
</output>
